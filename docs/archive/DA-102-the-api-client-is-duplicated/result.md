# DA-102 · Result

**Closed 2026-09-23.** Completed. The two throwing reads of `src/ui/live.ts` — a thread and a repository's diff — read a refused answer through the store's own `refusal()` and put the server's sentence after the thread's id or the repository, keeping the sentence the reader got: `the thread <id> could not be read: <server message>`, `the diff of <repo> could not be read: <server message>`, and the status code only when the body is not a refusal, which `refusal()` itself falls back to. Where the reader lives was the decision the card asked for first: `refusal()` is exported from `store.ts` and imported by `live.ts`; two call sites did not justify a module of its own, which would have been one function with two callers. The 404 on a diff still means the repository left the review and goes to `applyRepositoryDiff(repo, null)`, untouched. The rule is written in `08-ui.md` › Store: a failure the reader is told about carries the server's message, and a background read — `/api/config`, the sessions list, `/api/activity` — stays silent. The `EventSource` stub of `tests/ui-stream.test.ts` moved to `tests/helpers/event-source.ts`, so `tests/ui-live.test.ts` drives `startLive()` with the same stub; DA-96 then extended it.

**Verification.** `tests/ui-live.test.ts` › "a live read the server refuses": "puts the server's sentence in the toast for a thread, after the thread's id", "puts the server's sentence in the toast for a diff, after the repository", "falls back to the status when the body is not a refusal", "takes a 404 on a diff as the repository leaving, not as a failure". Mutation probes by the worker on `38b35d7`, `git status --porcelain` empty before and after, `npx vitest run tests/ui-live.test.ts`: the thread's read back to the status code, exit 1, 1 failed of 37 (the thread case); the diff's read back to the status code, exit 1, 1 failed of 37 (the diff case); the 404 special case removed, exit 1, 1 failed of 37 (the 404 case). Verdict names base → batch head `1cef835`: `vitest list` 562 → 566, the four above added, none removed. Worker gates on `1cef835`: `backslop lint` exit 0 (0 errors, 1 warning), `bun run lint` exit 0 (190 files), `bun run typecheck` exit 0, and under `/tmp/da-perf.lock` `bun run test` exit 0 (40 files, 629/629), `bun run test:bun` exit 0 (629/629), `bun run test:ui` exit 0 (98 passed); `bun run perf` declined by its load guard at 6.54 per core. The first locked runs at load 102 and 99.8 exited 1 on timeouts; each of the 11 red names was run alone twice on `1cef835` and twice on `5a5f8e8`, 44 runs, all exit 0, and none of those files imports `src/ui` or the new test helper. Worker gates on the track tip `927d582`: `backslop lint` 0, `bun run lint` 0, `bun run typecheck` 0, `bun run test` 0 (41 files, 637/637), `bun run test:bun` 0 (637/637), `bun run test:ui` 0 (104 passed), `bun run perf` declined at 2.69 per core. Review: one isolated pass over `5a5f8e8..227bdf0` for the whole track, critical 0, major 2, minor 3, plus two legacy observations, all closed on the branch. **Integration into `main`:** `38b35d7`, `b6968ec` and `1cef835` cherry-picked as one commit onto DA-100.1's; `CHANGELOG.md` conflicted with the core track's entries and was merged in the order `backslop merge-changelog` gives, both sides kept, the other files applied without a conflict. The integration gates ran once on the merged tree after the last task of the track; the numbers are in the `result.md` of the track's last task, `focus-ring-below-non-text-contrast` in the archive.

**Documentation in the same pass.** `docs/reference/08-ui.md` › Store (the refusal rule, the silent background reads, where `refusal()` lives) and the UI tests paragraph on the shared `EventSource` stub; `CHANGELOG.md`, `### Fixed`.

**Filed, not closed.** [DA-102.1](../../backlog/minor/DA-102.1-toast-lifetime-shorter-than-a-refusal.md), `minor`: a server's refusal sentence gets the same 2.2 seconds in the toast as any short answer; the toast's lifetime is the handoff's, so it waits in `minor/`.
