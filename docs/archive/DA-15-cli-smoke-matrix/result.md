# DA-15 · Result

**Closed 2026-09-05.** Completed. `scripts/smoke.sh <cli command…>` (POSIX sh, Git Bash friendly; the command's words may not contain spaces) runs the ADR-006 scenario against any channel in a temporary root: small synthetic review, `review new`, `diff --json` (the anchor is a real inserted line from it), `serve` in the background on a free port — ready only when the server printed its own address and `/api/review` answers, so a foreign server on the same port cannot pass for ours; a death whose stderr names a taken address retries the next port, any other death stops the run with the command, the real exit code, stdout and stderr — totals of `/api/review` compared with `diff --json`, the page served, `comment --role human`, `list --json`, `list --unanswered --json` (1 before the reply, 0 after), `reply` by an agent, `resolve --role human --note`, `list --status resolved --json` (two replies), `export --status all` as markdown and JSON, the server stopped and its port silent. JSON is read by `jq` when present (exit 4 = no rows; 2, 3, 5 = reader failure, which stops the run) or by a small Node script otherwise. CI job `smoke`: node 22 × ubuntu/macos/windows, bun × ubuntu/macos, binary × ubuntu/macos built in the same job with the new `bun run build -- --target current` (`scripts/build.ts` gained `--target <platform>-<arch>|current`); Windows is `continue-on-error` until DA-45. The glossary gained `smoke matrix`.

Review by an isolated session, three rounds: 1 major (a `serve` that died on start was retried as a taken port and reported with a wrong port and the function's return code) and 3 minor (six binaries built per runner; a failed JSON reader read as zero rows; the README sentence), then two small items (stdout in the failure report; one `process` import style) — closed in `d6e3c74` and `35b1b61`; the readiness-by-printed-address rule came out of a probe that occupied the port with another server.

**Verification.** Locally on darwin-arm64, all three channels green (`node dist/cli.js` 4.1 s, `bun src/cli/index.ts` 2.8 s, `./dist/diffalanche-darwin-arm64` 4.4 s) plus `node dist/cli.js` in a PATH without `jq`. Mutation probes after commit: `Bun.file` inserted in the `serve` path of the CLI → the node channel fails in 2.8 s with `serve exited instead of serving the review`, exit code 2 and the `TypeError` on stderr, the bun channel passes; `--role human` → `--role agent` on the comment step → the "1 unanswered before the reply" check fails; a broken `jq` filter → the reader failure stops the run with exit 5 and its stderr; a readiness pattern the server never prints → the 30 s report shows the address on stdout. The CI job is verified by reading and by running its commands locally; it has not run on GitHub (nothing is pushed). Gates on the worker branch at `35b1b61` and on `main` after integration: `backslop lint` no errors; `bun run lint` 116 files; `bun run typecheck` three projects; `bun run test` 246 tests; `bun run test:bun` 246 tests; `bun run perf` green after one red run (long tasks 2, 8.7 ms at load 13.5 — the flap is a property of the shared machine, recorded for DA-32).

**Documentation in the same pass.** `docs/reference/11-perf.md` (The smoke matrix), `06-cli.md` (the two channels, `--target`), `scripts/README.md`, `README.md`, `docs/GLOSSARY.md`, `CHANGELOG.md`.
