# DA-18 · Result

**Closed 2026-09-05.** Completed. `src/server/events.ts`: `createEventStream()` keeps a ring of the last 200 frames `{ id, event, data }`; `forwardEvents` puts every watcher event out under its own name, `forwardActivity` every feed line as `activity`; `GET /api/events` replays from `Last-Event-ID` — or sends one `reload` frame (with the newest id) when the client is further behind than the ring reaches or holds ids of a server that has since restarted, because half a replay is worse than none — writes through one queue so a frame never interleaves with the 15 s heartbeat, and ends on client abort or `close()`. `GET /api/repos/:repo/diff`, `GET /api/comments/:id`, `GET /api/warnings`, and `GET /api/activity` (the feed backlog, newest last, same shape as the `activity` frames) are what the UI fetches after an event. The change set is announced before `diff.json` is written. The budget line "Update after an edit in one repository" is measured by the harness the way it happens (an edit in one fixture repository → `diff-changed` on `/api/events` → the repository's new diff fetched) and printed with DA-25 named instead of a verdict — 220 ms on a quiet machine, 100 ms of it the debounce — and the gate regenerates a fixture an older generator left behind. Bun channel, fixed on the way (the unit suite now runs on Bun's runtime, DA-13.1): the data-directory name filter dropped every CLI write under Bun's coalesced notifications (a signal now reloads `current`, `review.json`, `comments.json` and compares); Bun's 10 s idle timeout killed idle SSE streams (`idleTimeout: 0` in `runtime.ts`); the recursion probe answered before Bun's watch was armed (it writes every 50 ms until heard); `startWatcher` waits for every tree's baseline; `recursive: false` walks the trees for notifications that cannot be trusted, used by the tests under Bun.

Review by an isolated session, two rounds over this task: 1 major (a partial replay, silently) and 4 minor (the native watch's readiness — decided as a documented limit, since arming it would mean writing into a reviewed repository; a flag cleared by a read that did not cover the write; a misplaced doc comment; the `csrf()` wording, corrected against the pinned Hono source) plus one reference mismatch — closed in `7e0d88e`, `364b71b`.

**Verification.** Gates on the worker branch at `364b71b` and on `main` after integration: `backslop lint` no errors; `bun run lint` 126 files; `bun run typecheck` three projects; `bun run test` 297 tests on Node and 297 on Bun's runtime (six consecutive green runs after the watcher fix; before it, three to six failures a run). `tests/events.test.ts`: frames in order with ascending ids, replay of exactly the missed frames, the reload on a gap and on a restart, the heartbeat, the shutdown ending every client, the activity backlog with the CLI's reply and its author. Mutation probes after commit: replay replaced by an empty list → the reconnect test fails; heartbeat removed → the keep-alive test fails; `close()` not ending clients → the shutdown test fails; the gap condition forced true → the reload test fails; the counter reduced to a boolean → the concurrent-invalidation test fails; the data-directory rule back to name matching → the `dataIgnore` test fails; the baseline resolved before the first walk → its test fails; the activity route answering `[]` → its test fails. Perf: the number of record is the quiet run (78.8 ms / 0 / 7.2 ms / 7.6 ms / 15.9 ms, update 220 ms); runs at load average 20–82 on the shared machine were red on the two scrolling lines, which DA-16.1 holds.

**Documentation in the same pass.** `docs/reference/07-server.md` (the stream, the reload frame, the fetch routes, who may write), `05-watcher.md` (Bun's notifications, the walk, readiness, the budget), `11-perf.md` (the update measurement), `CHANGELOG.md`.
