# DA-25 · Result

**Closed 2026-09-05.** Completed. One `EventSource` on `GET /api/events` for the life of the page (`src/ui/live.ts`): an event names, the page fetches, the store is patched; the review is read again only on `session-changed` or a `reload` frame. Reconnect and `Last-Event-ID` are the browser's own (ADR-005); the client adds the `reload` handling, the connection state (`watching` / `reconnecting` / `connecting` in the sidebar footer), and the activity ring read from `GET /api/activity` on every open, merged by id. Patching by identity is in `src/ui/patch.ts`: a repository's new change set is merged file by file and a file whose patch, status, counts and omission are unchanged keeps the object it was rendered from, so its memoised card is not re-rendered; a repository in which nothing changed comes back as the object it already was. A hunk is the same hunk when its `@@` header and body match; the others take the accent border and `updated 12s ago` from one five-second clock in the store. The composer is left alone unless the patch is of its own file, and then re-validated: when the edit took its line away the form drops to the file anchor keeping what was typed. Threads are patched from `GET /api/comments/:id`; an agent's reply raises a toast. The AGENT ACTIVITY panel is handoff section 4, collapsed by default, `N live` over the watcher's two-minute window. The update line of the budget table is a gate now: the harness scrolls the card into view and measures from the edit to the painted frame with the appended line in the card's own text.

Two findings from this task: DA-25.1 (the stream's head was not flushed until the first heartbeat — fixed on the same branch) and DA-25.2 (the gate hangs after its first run under concurrent gates — closed by the orchestrator). Review by an isolated session on the whole ui-c track, two rounds: 1 major (the session-switch budget line measured from the parsed response rather than from the press — now the whole wait, over budget, and waiting for DA-24.1), 7 minor and 3 nits on the first (the page's own session writes re-reading the review and clearing the draft; the removal path of a repository leaving a composer open; stale hunk marks across a switch; a first-run screen showing zeros before the scan answered; a contrast pair in a selected range; `⇧⇧` unable to close; a reduced-motion test passing on nothing), 4 minor on the second (the self-write marker as a map with an age; the docs the fixes changed; the `⇧⇧` close in the e2e). All closed in `1fdd141`, `a97f3e1`, `e160464`, `62dd871`, `9f1c3d8`, `fce474c`, `16b8aaf`; the last also fixed the reading-position anchor landing on the sticky header on a page that cannot scroll further. Finding DA-25.3 (a test reading `sessionStorage` directly, red on Bun) came from the e2e worker's gates and was closed in `9f1c3d8`.

**Verification.** `tests/ui-live.test.ts` (19), `e2e/live.spec.ts` (2): a reply from `diffalanche reply` reaches the rail, the counter, the toast and the feed without a reload; an edit above the reading position marks its hunk, moves the reading position by 0 px, leaves a form open on another file with its text, and — asserted with a `MutationObserver` over both cards — changes only the edited card's DOM. Mutation probes after commit: `mergeRepository` never keeping an object → 4 tests fail; `hasNewLine` always true → 2; `changedHunks` marking nothing → 2 (one probe first went green because biome had reformatted the mutated line; re-run against the real text). Perf on a quiet machine, three single-run processes on a regenerated fixture: update after an edit 253 / 243 / 242 ms against 300; the CPU line is DA-16.1's.

**Documentation in the same pass.** `docs/reference/08-ui.md` (live update, the feed, the footer state), `docs/reference/11-perf.md` (the update line as a gate), `docs/GLOSSARY.md` (*live stream*), `.impeccable/surfaces/src-ui-app-tsx.md`, `CHANGELOG.md`; README's live-update paragraph added by the approver at integration.
