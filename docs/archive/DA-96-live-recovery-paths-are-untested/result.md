# DA-96 · Result

**Closed 2026-09-23.** Completed: the three recovery paths of `src/ui/live.ts` are pinned as they are, with no change to what they do. Where the tests live was the card's first decision: unit tests, `tests/ui-live-recovery.test.ts`, over one shared `EventSource` stub, not a Playwright spec. All three paths are `live.ts`'s own code and the stub reaches each on purpose, the `CLOSED` half included; the browser's reconnect and `Last-Event-ID` are not this module's (ADR-005, and `tests/events.test.ts` holds the server half), and a spec that dropped the stream would wait on the browser's retry timer. The reason is in the test's header, which points at `08-ui.md` › UI tests, where it is written out. The stub is `tests/helpers/event-source.ts`, lifted there by DA-102 and extended here with `readyState` and the static `CLOSED`; `tests/ui-stream.test.ts`, `tests/ui-live.test.ts` and the new file share it. The footer that keeps saying `watching` once the browser has closed the stream is pinned as the card asked and filed as DA-96.1.

**Verification.** `tests/ui-live-recovery.test.ts`: "a reload frame" › "reads the review again" (a second `GET /api/review`); "an error on the stream" › "says reconnecting while the browser is retrying" and "leaves the footer as it was once the browser has given up"; "a frame whose read failed" › "puts the failure in the toast, and the frame after it is still applied". Mutation probes by the worker on `afa84bf`, `git status --porcelain` empty before and after, `npx vitest run tests/ui-live-recovery.test.ts`, each exit 1 with only its own case red: the `reload` listener deleted reddened "reads the review again" (1 of 4); `reconnecting` set unconditionally reddened "leaves the footer as it was once the browser has given up"; the queue's `.catch` removed reddened "puts the failure in the toast, and the frame after it is still applied", with an unhandled rejection beside it. Worker gates on `e828b52`, the head of this task's batch: all exit 0 — `backslop lint`, `bun run lint`, `bun run typecheck`, `bun run test` (637), `bun run test:bun` (637), `bun run test:ui` (98 passed) — and `bun run perf` declined by its load guard at 3.78 per core. Worker gates on the track tip `927d582`: `backslop lint` 0, `bun run lint` 0, `bun run typecheck` 0, `bun run test` 0 (41 files, 637/637), `bun run test:bun` 0 (637/637), `bun run test:ui` 0 (104 passed), `bun run perf` declined at 2.69 per core. Review: one isolated pass over `5a5f8e8..227bdf0` for the whole track, critical 0, major 2, minor 3, plus two legacy observations, all closed on the branch. **Integration into `main`:** `afa84bf` cherry-picked onto DA-102's commit; `CHANGELOG.md` and `docs/reference/08-ui.md` auto-merged without a conflict. The integration gates ran once on the merged tree after the last task of the track; the numbers are in the `result.md` of the track's last task, `focus-ring-below-non-text-contrast` in the archive.

**Documentation in the same pass.** `docs/reference/08-ui.md` › UI tests (why unit tests and not a browser spec, and the shared stub); `CHANGELOG.md`, `### Added`.

**Filed, not closed.** [DA-96.1](../DA-96.1-closed-stream-still-says-watching/task.md), `minor`: once the browser has closed the stream the footer still says `watching`; this task pinned that behaviour and did not change it, as its Out of scope says.
