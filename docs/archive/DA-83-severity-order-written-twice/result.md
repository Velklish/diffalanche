# DA-83 · Result

**Closed 2026-09-18.** Completed. `SEVERITY_ORDER` is gone from `src/core/domain/counters.ts`; `worstSeverity` walks `SEVERITIES`, imported by value from `../storage/types.ts` on an import line that already existed. The domain no longer keeps a second copy of `critical, warning, nit, question`, so a fifth severity can no longer be accepted by the schema and the CLI while `worstSeverity` returns `null` for a scope whose only open comment carries it and every badge of that scope paints as carrying no finding. The comment above the function is a pointer to `04-domain.md` rather than a retelling of the SPEC decision. The copy in `scripts/synth.ts:458` was left alone: it is in the task's Out of scope.

**Verification.** `grep -rn '"critical", "warning", "nit", "question"' src/` returns one line — `src/core/storage/types.ts:59`. The new case `filters and counters > paints a scope with the first severity of storage's list that is present` drives the property off `SEVERITIES` itself rather than off a written-out list, so a fifth value is covered on the day it is added. Two mutation probes by the worker on `a31fcaf`, tree clean before and after each: the list reordered in the domain → exit 1, 16/18 (the earlier counters case went red as well), and the probe the task itself named — a fifth value in `Severity` and `SEVERITIES` with the domain's old list in place → exit 1, 17/18. Worker gates on `a1734c7`, clean tree: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 — 477 tests over 35 files. Integration into `main`: `a31fcaf` cherry-picked onto the DA-90 commit, plus the `docs/reference/04-domain.md` half of the review commit `a1734c7` (its `tests/comments.test.ts` half belongs to DA-101); `CHANGELOG.md` merged by entry. Gates on the integration commit: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run typecheck` exit 0; the full six-gate run of the merged tree is recorded on the last commit of this track (DA-88).

**Documentation in the same pass.** `docs/reference/04-domain.md` — the counters section says that "worst" is the order of storage's `SEVERITIES` and what a drift between two lists costs the badges. Review moved the citation: the order is set by `docs/SPEC.md` section 3, decision 7 — the same authority the comment over `SEVERITIES` itself cites — and not by `03-storage.md`, which does not state it. `CHANGELOG.md`, `### Fixed`.
