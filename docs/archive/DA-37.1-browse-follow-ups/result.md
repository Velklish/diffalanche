# DA-37.1 · Result

**Closed 2026-09-24.** Completed; the three gaps DA-37 left are each fixed. **The browsed file follows the working tree:** when `applyRepositoryDiff` finds the browsed file's patch changed — edited, entered the change set, or left it — and the view is on the working tree, the file is read again in place, with no `reading…` between the two texts, and an older read that answers after a newer one is dropped; the base is not read again, and a file the change set cannot see is not followed. The rows keep their numbers, not their text: an edit above the reader moves the text by the lines it added (written in [08-ui.md](../../reference/08-ui.md)). **A drag re-renders only the blocks it touches:** the rows are cut into memoised blocks of 200. **A thread outside every hunk is reached:** `revealThread` asks the patch whether it shows the thread's line on its side; a collapsed card is opened again, and only a line the patch does not show and no `↑ N lines` has brought in opens the file in browse mode at that line — on the base for an old-side thread. [07-server.md](../../reference/07-server.md) said browse mode does not follow a live edit; this acceptance corrected the sentence to what the task made true.

**Verification.** Worker record `gates-ui2-2.json` on `da3fba0`, the whole track, on a clean detached gate worktree under the lock — gates 7, green 6 (`test` 853/853, `test:bun` 853/853, `test:ui` 151/151, `lint` 0, `typecheck` 0, `perf` update 299/304/298 ms; red: `backslop lint`, five errors, none from code — three card quotes of other tasks this acceptance turned into `quote:before:` and two mentions of DA-55.8, whose card is on `main`). The drag, measured in Chromium through Playwright on a committed 15 000-line file (364 KB) under the lock, base `660190b` and branch alternated: script per step 14.28–14.51 ms before, 1.29–1.36 after (CDP `Performance.getMetrics`, 40 steps, two runs a side), style and layout unchanged; a step to the next painted frame, median 36.2–38.6 ms before (6 runs), 32.1–34.8 after (9 runs), against 16.6–16.7 for a step that changes nothing — what remains is paint and composite, not broken down. The drag has no functional verdict: a render count is not observable from the page. Mutation probes, tree clean before and after each, each exit 1: the re-read made unreachable at `1a2969b` → `tests/ui-live.test.ts` 40 of 41, red "… > reads the file again when its patch changed, and keeps the old text until the new is there", and `e2e/browse.spec.ts -g "follows an edit"` red; `browse(thread.path)` removed → `-g "outside every hunk"` red; at `fcf4214` the collapsed card's reopening removed → `threads.spec.ts -g "collapsed card"` red "a thread on a collapsed card opens the card, not browse mode", and the patch's answer forced to "inside" → both "outside every hunk" verdicts red. Review: two isolated passes over the track — critical 0 in both; the second pass's major on this task (the browse fallback fired on a collapsed card) closed in `c0feb3e`, the comment length in `b6211e5`. Evidence: `evidence-ui2-final.md`.

**Integration into `main`.** `96e03d2 1a2969b b6211e5 c0feb3e` cherry-picked without commit onto DA-40.1's commit and squashed into one commit. Git merged `CHANGELOG.md`, `08-ui.md` and `src/ui/store.ts` with no conflict; `CHANGELOG.md` was then set by hand as for every task of the track: `main`'s file, and the branch's final entry once, at the top of Unreleased / Fixed, in the branch's layout. One change is this acceptance's: the sentence in 07-server.md above, whose link `archive` moved.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (the browsed file follows the working tree, the blocks, a thread outside the hunks), [07-server.md](../../reference/07-server.md), `CHANGELOG.md`, `### Fixed`.
