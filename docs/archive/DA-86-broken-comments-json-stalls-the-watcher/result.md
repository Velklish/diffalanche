# DA-86 · Result

**Closed 2026-09-18.** Completed. `reloadComments` catches the read of `comments.json` where it happens, sets the comment baseline back to `null` — reusing the existing re-baseline path — and returns, so the chain reaches `reloadMetadata` and `reloadSessions` and the session events keep arriving while the file stays broken. Of the three candidates for reporting, the first was taken: `onError` hears about it **once per transition into the broken state**, the marker being the baseline itself, so a server prints one line rather than one per burst. The switch of the `current` pointer onto a session whose file is already broken is such a transition too, so `snapshotComments` takes the reporter there; start-up stays silent, having no baseline to lose, and `serve` refusing to start on unreadable data is DA-64's subject. `enqueue`'s guarded reporting is now the shared `report(error)`; without sharing it, an `onError` that throws — the suite's own does — would have aborted the reload chain at exactly the point this fix keeps running. The chain's other steps were checked rather than assumed: `reloadMetadata` goes through `readSessionOrNull` and `reloadSessions` through `snapshotSessions`, and both catch. `src/core/storage/*` needed no change.

**Verification.** Two new cases. `a comments.json that cannot be read > stops the comment events and leaves the rest of the chain running`: for both shapes the read throws on — `{ not json` and a `version` outside `READABLE_VERSIONS` — the current session is renamed and a task is created or closed under `reviews/`, and both `session-changed` and `sessions-changed` still arrive; the failure count grows by exactly one per broken state, which pins the reporting decision; and once the file is repaired what is in it becomes the baseline rather than two hundred `comment-added`. `… > is a transition when the pointer moves onto it, and says so once` does the same for the session switch. Mutation probes by the worker: `index.ts:300` with `return;` replaced by `throw error;` — exit 1, 29 of 30 passed, the first case reddened; `index.ts:248` with the reporter dropped from the switch — exit 1, 31 of 32, the second reddened. Worker gates on `abdd300`, clean tree: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (469/469), `bun run test:bun` exit 0 (469/469). **Integration into `main`:** `659e5cd` and `abdd300` cherry-picked onto the DA-74 commit, together with this task's share of the pack's review-fix commit `cc8db8d` — the `onFailure` parameter of `snapshotComments` and the reporter passed at the session switch; `CHANGELOG.md` merged by entry and `src/core/watcher/index.ts` resolved by hand, where this task's new `report` helper and DA-74's shortened doc-comment landed on the same lines. Gates on the merged tree of the whole watcher track: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (35 files, 487 tests), `bun run test:bun` exit 0 (35 files, 487 tests). `bun run perf` is measured once through the run's lock on the committed head; its numbers and the machine's load are in the approver's gate record `gates-approver-watcher.json`.

**Documentation in the same pass.** `docs/reference/05-watcher.md` — the guarantee at the comment-baseline paragraph covers the metadata and session-list half explicitly and says the failure is reported once. `CHANGELOG.md`, `### Fixed`.
