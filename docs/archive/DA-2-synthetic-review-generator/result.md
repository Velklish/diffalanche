# DA-2 · Result

**Closed 2026-09-05.** Completed. `scripts/synth.ts` (`bun run synth -- --out <dir> [--seed n] [--small]`) builds the synthetic review: 21 repositories under `repos/<group>/<repo>`, each a git working tree with a base commit and working-tree edits, one untracked file per repository, a clean sibling worktree of the first repository and a nested submodule inside it (source under `sources/`, outside the scanned roots), and a `.diffalanche/` data directory with `config.json` (`roots: ["repos"], depth: 2`) and one session `synth` (`review.json`, 200 comments over all four anchor levels and all four severities). Content is seeded pseudo-random TypeScript, C#, Python, Go, and Markdown in whole units; git author, committer, dates, and config are pinned so two runs are byte-identical outside `.git`. The generator plans edits, measures with `git diff --numstat`, and tops up until the change set is exact; a postcondition throws otherwise, and a foreign `--out` (non-empty, no `.diffalanche/` marker) is refused. Small profile: 3 repositories, 20 files, 2 000 lines, 20 comments.

The Verification criterion was reworded at acceptance: the task said "`git diff --stat` sums to 300 files and 30 000 lines (±1 %)", but `git diff` does not show untracked files and staging them with `--intent-to-add` would write to the index, which SPEC section 11 forbids. The target is the change set of SPEC section 3 (tracked plus untracked), which is what SPEC section 6 and the glossary call the synthetic review: `git diff` 279 files / 27 894 lines, untracked 21 / 2 106, change set exactly 300 / 30 000. `current` is not written: SPEC section 7 names the pointer but not its content; DA-8 defines it, consumers pass `--review synth` until then.

Review by an isolated session, two rounds: round one closed with DA-1; round two found one major (unguarded `rmSync` of `--out`) and four minor (postcondition and planner floor, `current` explanation, Node runtime pinned in CI, README wording), all fixed in `ec586ac`. Known gap, accepted: a plan overshooting the profile by less than the context loss of git matching (about 25 %) is masked by the top-up and caught by neither test nor postcondition; the planner no longer can overshoot by construction.

**Verification.** Gates on the worker branch and again on `main` after integration: `npx github:Velklish/backslop#v0.3.1 lint` no errors; `bun run lint` 11 files; `bun run typecheck` exit 0; `bun run test` 2 files, 11 tests passed. Determinism: two full-profile runs, `diff -r --exclude=.git` empty over 343 files; `--seed 7` produces a different tree. Full profile generation 3.9 s on the development machine (Apple Silicon). Mutation probes after commits `aeddc82` and `ec586ac`: top-up disabled → count assertions fail (1 500 vs 2 000 lines); `Date.now()` in `stamp()` → determinism test fails; anchor off by one → line-anchor test fails; no line anchors → guard fails; empty listing → determinism test stayed green, fixed by a length guard in `aff4018`; guard removed / guard always passes / marker not accepted → the three `--out` tests fail on their respective side; plan tripled → generation throws the postcondition.

**Documentation in the same pass.** `docs/reference/11-perf.md` (first content: layout, options, profiles, the change set reading, determinism, the `--out` refusal), `docs/reference/README.md` (11-perf linked), `scripts/README.md` (scripts table; `synth.ts` uses only `node:` modules so Vitest under Node can import it), `README.md`, `CHANGELOG.md`, `tsconfig.json` (`allowImportingTsExtensions`).
