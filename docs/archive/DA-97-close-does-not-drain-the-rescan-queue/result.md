# DA-97 · Result

**Closed 2026-09-18.** Completed. `closed` was read before the work rather than during it — in the debounce callback and at the head of a queued item — so an item already inside its own `await` ran to the end, holding the session lock and writing `diff.json` after the caller had been told the watcher was closed. On the teardown paths that is a rescan re-creating `reviews/<name>/` under a directory just removed, a failure into `onError` attributed to whatever runs next, or a lock directory the next writer waits out for 30 s. Of the two candidates the first was taken: **`Watcher.close` answers a promise** and awaits the queue after setting `closed`, clearing the timers and closing the watches. A separate `drain()` beside a synchronous `close` is two ways to close, and the second is the one a caller forgets. The chain never rejects — `enqueue` already turns a failure into `onError` — so the drain needs no catch of its own. Carried through every caller: `src/server/serve.ts:127` on the failed-listen path, where the wait has to happen before the throw, and `src/server/serve.ts:139` in the server's own close, plus `afterAll` and the takeover case in `tests/watcher.test.ts`; those two lines are the whole of this pack's change to `serve.ts`. The walk of `src/core/watcher/tree.ts` is deliberately outside the promise: its `tick` can be in flight when `close` returns, it reads and writes nothing into the data directory, and closing it is a different decision about a different loop.

**Verification.** New case `closing a watcher > waits for the rescan in flight, so nothing is written after it resolves`: a watcher over its own copy of the session, a rescan provoked and caught at the moment its event goes out — which is emitted from inside the rescan, before `diff.json` is written — then `close` awaited. The cache it left on disk already holds the new file, and the data directory, removed right after, is still gone once the suite's own watcher has done a whole round trip. No sleep: the waits are the event and that round trip. Mutation probe by the worker on `89e8d26` with `await queue;` removed from `src/core/watcher/index.ts:403` — exit 1, 35 of 36 passed, this case reddened. Worker gates on `89e8d26`, clean tree: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (473/473), `bun run test:bun` exit 0 (473/473). **Integration into `main`:** `2e3c297` cherry-picked onto the DA-92 commit; `CHANGELOG.md` merged by entry. Gates on the merged tree of the whole watcher track: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (35 files, 487 tests), `bun run test:bun` exit 0 (35 files, 487 tests). `bun run perf` is measured once through the run's lock on the committed head; its numbers and the machine's load are in the approver's gate record `gates-approver-watcher.json`.

**Documentation in the same pass.** `docs/reference/05-watcher.md`: what closing now guarantees — no write into the data directory follows a resolved close — what it looked like without it, and what the promise does not cover. `CHANGELOG.md`, `### Fixed`.
