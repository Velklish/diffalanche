# DA-36 · Result

**Closed 2026-09-24.** Completed. While a comment is typed, the form lists the five nearest past comments of every session from `GET /api/suggest`, asked once typing pauses for 200 ms (`SUGGEST_DEBOUNCE_MS`) and only for a text with a word of four characters or more; `↑` / `↓` choose a row and `TAB` takes its text and severity. The `AUTO · <prediction>` chip, chosen by default, resolves the severity when the comment is sent; `comments.json` records who chose it in `severitySource` (`auto` | `manual` | `confirmed:<author>`, absent read as `manual`); threads show `auto` and, once an agent agrees, `labelled by <agent>`, and `show` prints the same; `reply --confirm-severity` is the agent's agreement. Two minor entries stay in `minor/` with their Scope filled: [DA-36.1](../../backlog/minor/DA-36.1-suggestion-choice-not-announced.md) (screen-reader announcements: the chosen row, the 503 sentence, `AUTO` going away) and [DA-36.2](../../backlog/minor/DA-36.2-auto-labels-vote-in-suggestions.md) (severities the model chose vote again in later suggestions). Nothing was pushed. Decisions this acceptance records:

- **`severitySource` does not raise `SCHEMA_VERSION`** — the orchestrator's ruling during the night: a raised version would make every earlier build refuse `comments.json`. The price, that an earlier build rewriting the file drops the field and its comments read as `manual`, is written in [03-storage.md](../../reference/03-storage.md#schema-versions) "Schema versions" and `docs/SPEC.md` section 7.
- **A 503 from `/api/suggest` is temporary for the UI** — the orchestrator's ruling during the night: `modelAway` holds the server's sentence, `AUTO` is out of reach, and the model is asked again after a pause of 10 s doubling to 60 s; the first vote brings `AUTO` back. The server's error code is unchanged: DA-41's route answers 503 `model`, "being put in place", while it downloads, and the page reads that 503 like any other.
- **`TAB` takes a row only once `↑` / `↓` chose it** — the orchestrator's ruling during the night, a departure from the design handoff for keyboard and screen-reader users, written in [08-ui.md](../../reference/08-ui.md): a `Tab` on to `Comment` never replaces a draft.
- **The `AUTO` send waits for the vote at most 10 s** (`AUTO_WAIT_MS`), then sends `warning` with source `auto`; it sends only for the form it was sent from — a form closed while it waited sends nothing — and the field is read-only while it waits.
- **`reply --confirm-severity` turns `auto` into `confirmed:<author>`**, and refuses a comment that is not `auto` (`severity-not-auto`) and an empty author (`invalid-author`), writing nothing. `POST /api/comments` refuses `confirmed:` with 400: only a reply makes it.
- **A request already in flight for the same text is reused**, and the query is cut to 12 000 encoded bytes at a character boundary (`SUGGEST_QUERY_LIMIT`), because a header of about 16 KiB is refused with 431 on Node and on Bun: 3000 Cyrillic characters were refused, 2600 passed.
- **The e2e suites need no model.** The suggestion tests of `e2e/composer.spec.ts` stub `/api/suggest` with `page.route`; the `AUTO` sends of the other suites wait for their toast up to `SEND_DEADLINE_MS` (60 s), a deadline on a hang.
- **`review delete` is DA-40's**, not this task's.

**Verification.** Worker records of run 0923, all clean, under `/tmp/da-perf.lock`: `gates-wave3.json` on `7576bd9` — gates 7, green 7 (vitest 797 on Node and on Bun, Playwright 117, perf in budget); `gates-wave3-2.json` on `b576ac6` — gates 7, green 7 (vitest 802 on Node and on Bun, Playwright 118, `perf` all seven lines in budget, "Update after an edit" 292 ms) and `bun run test:e2e` 11 passed; `gates-wave3-3.json` on `1fff4f6`, which changed only UI files since — gates 7, green 5: `test` 804/804, `test:ui` 118, `test:bun` one red, `tests/watcher.test.ts > a comments.json that cannot be read > stops the comment events and leaves the rest of the chain running` ("the watcher never caught up", 20 s deadline — the watcher family DA-60 hardened, where a waited-for event may be lost rather than late, a finding filed on another track of the run), and `perf` "Update after an edit" 306 ms against 300, inside the base's 302–353 (DA-39.1, DA-60.1); neither re-run on the worker's side, by the orchestrator's ruling. Mutation probes, each reddening its expected verdict: `src/core/domain/comments.ts:301` on `7576bd9` (2 of 44), `src/ui/store.ts:1285` on `b576ac6` (1 of 11), `src/ui/store.ts:1259` on `1fff4f6` (1 of 13). Verdict names `d8788bf` → `1fff4f6`: none removed. The card's Playwright checks are `e2e/composer.spec.ts`: suggestions listed on typing and taken with `TAB`, an `AUTO` send stored as `auto` and relabelled by an agent's `--confirm-severity`, and the form while the model is away. Review: two isolated passes — pass one over `d8788bf..7576bd9` critical 2, major 5, minor 4, all closed in `b576ac6`; pass two over `7576bd9..b576ac6` all eleven closed and three new (major 1, text typed while the send waited was lost; minor 2), closed in `1fff4f6` and read by the orchestrator in the diff. Evidence: `evidence-da36-wave3.md`, `evidence-da36-r1-wave3.md`.

**Integration into `main`.** `c63f983 7576bd9 b576ac6 1fff4f6` cherry-picked without commit onto `66b749b` (DA-41) and squashed into one commit. One conflict, `CHANGELOG.md` in `c63f983`: the interim steps took the branch's file, and the final file is `main`'s with exactly the branch's DA-36 entry, 21 lines, at the top of its Added section above DA-41's. `backslop merge-changelog --ours 66b749b --theirs 1fff4f6 --base d8788bf` reported ours 82, theirs 82, merged 83 and would have broken the file: it put a blank line between the entries of every section, 60 of them across the file, and the DA-36 entry at the end of Unreleased's Fixed section instead of Added; its output was not used. Merged by git without conflict, meeting DA-41 and DA-34.2: `README.md`, `docs/SPEC.md`, `06-cli.md`, `07-server.md`, `09-ml.md`, `skills/diffalanche-review/references/cli.md`, `src/server/suggest.ts` (DA-41's embedder progress on standard error beside DA-36's `SuggestAnswer`). The page's reading of a 503 — the status and the server's sentence through `refusal()` — takes DA-41's `ModelError` answer, 503 `{ error: "model", message }`, as it is; [08-ui.md](../../reference/08-ui.md#commenting) already names DA-41's "being put in place". Of the 46 files DA-36 changed outside `docs/backlog`, `docs/archive` and `CHANGELOG.md`, 39 equal `1fff4f6`, and 7 — the seven above — differ from it only by the lines `main` wrote since `d8788bf`; no file outside DA-36's 49 differs from `66b749b`. Integration gates on `8ca0619` — this commit before these sentences, which change nothing else — clean: `backslop lint` 0, `bun run lint` 0 (244 files), `bun run typecheck` 0; under `/tmp/da-perf.lock` 01:02:36–01:05:59Z, one-minute load 7.3–22.8 on 8 cores: `bun run test` 0 (55 files, 822/822), `bun run test:bun` 0 (822/822), `bun run test:ui` 0 (118/118), `bun run test:e2e` 0 (11/11), `bun run perf` 0 — every line in budget, "Update after an edit in one repository" 288 ms at the median of 277, 288 and 319 against 300. The two reds of the worker's record on `1fff4f6` did not recur. `bun run typecheck` on the final commit from a detached checkout: 0.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (Commenting, Keys, the keyboard table, a model that is away), [03-storage.md](../../reference/03-storage.md) (Schema versions), [04-domain.md](../../reference/04-domain.md), [06-cli.md](../../reference/06-cli.md), [07-server.md](../../reference/07-server.md), [09-ml.md](../../reference/09-ml.md), [10-skills.md](../../reference/10-skills.md), `docs/SPEC.md` section 7, `DESIGN.md`, `README.md`, `skills/diffalanche-apply/SKILL.md`, `skills/diffalanche-apply/references/cli.md`, `skills/diffalanche-review/references/cli.md`, `CHANGELOG.md`.
