# DA-113 · Result

**Closed 2026-09-23.** Completed: both entries are fixed. One line per entry:

- **DA-76.2** — fixed. `mergedPatch(patch, status)` takes the entry's status, so a `modified` entry whose one surviving patch parses as `add` or `delete` is drawn as `modify`, in the two columns it is sized for; the one-column question is `oneColumn(status)` in `src/ui/measure.ts`, asked by both `codeColumnChars` and `mergedPatch`, so the card cannot be sized from one kind and drawn from another again. The library's rule was read, not assumed: react-diff-view 3.3.3 draws one column when `monotonous = add || delete`. Tests: `tests/ui-type-change.test.ts` › "a type change with one half omitted" › "is drawn in two columns, like the entry it is" (fixture `ADDED` with status `modified`) and "is drawn in as many columns as the card is sized for, whichever half survived" (every shape, drawn against sized). Mutation probe by the worker on `e828b52`: `patch.ts:19` back to `files.length === 1` reddened both cases, exit 1, 2 of 11.
- **DA-56.2** — fixed. `tests/design-contrast.test.ts` › "the contrast of the marks that carry a state without being text" holds the non-text marks to 3:1 (WCAG 1.4.11) in both themes: the history mark, the tick, the unpicked tick and the focus ring, each against the grounds it is drawn on. `DESIGN.md` › Shapes and Neutral says which dots and dividers are decoration and so outside the bound, and `src/ui/styles.css` says the same beside `.dot`. The check found the focus ring — then `accBd` — at 1.63–2.00:1 in both themes; at this commit its four pairs are the check's recorded exceptions, so a new pair under 3:1 fails and so does one of them clearing it, and the colour was filed as [DA-56.7](../../backlog/triage/DA-56.7-focus-ring-below-non-text-contrast.md) (major), fixed in this run, which removed the exceptions and added the `accBd` edge of a thing in play to the group.

**Verification.** Worker gates on `e828b52`, the head of this batch: all exit 0 — `backslop lint`, `bun run lint`, `bun run typecheck`, `bun run test` (637), `bun run test:bun` (637), `bun run test:ui` (98 passed) — and `bun run perf` declined by its load guard at 3.78 per core. That the non-text group turns red when a mark falls under 3:1 was probed by the worker on `91d92c4`: `accBd` back to `#3f4a80` / `#aab2d8` in `tokens.css` reddened both theme cases, exit 1 ("state frame on panel 1.86 / 2.00"). Worker gates on the track tip `927d582`: `backslop lint` 0, `bun run lint` 0, `bun run typecheck` 0, `bun run test` 0 (41 files, 637/637), `bun run test:bun` 0 (637/637), `bun run test:ui` 0 (104 passed), `bun run perf` declined at 2.69 per core. Review: one isolated pass over `5a5f8e8..227bdf0` for the whole track, critical 0, major 2, minor 3, plus two legacy observations, all closed on the branch; its finding that the ring was still under 3:1 on a chosen segment and beside a chosen file was DA-56.7's to close. **Integration into `main`:** `ae80572` and `e828b52` cherry-picked as one commit onto DA-96's; `CHANGELOG.md` conflicted with the core track's entries at `ae80572` and was merged in the order `backslop merge-changelog` gives, both sides kept, and `e828b52` then applied without a conflict. The core track had already rewritten one link line in this card and in DA-76.2's entry to DA-114's place in the archive; neither file was touched by the branch, so main's text stands. The integration gates ran once on the merged tree after the last task of the track; the numbers are in the `result.md` of the track's last task, `focus-ring-below-non-text-contrast` in the archive.

**Documentation in the same pass.** `docs/reference/08-ui.md` ("The card is sized and drawn from one answer", and the two contrast groups of `tests/design-contrast.test.ts`), `DESIGN.md` › Shapes and Neutral; `CHANGELOG.md`, `### Fixed` (DA-76.2) and `### Added` (DA-56.2).
