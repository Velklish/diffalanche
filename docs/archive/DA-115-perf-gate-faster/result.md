# DA-115 · Result

**Closed 2026-09-24.** Completed: the perf gate takes about 45 s instead of 66, and says where its time goes. Every repetition prints `wall per step, ms: …` on stderr and the gate prints its build's, each repetition's and its own wall time; that breakdown showed the scroll was 10.0–10.3 s of a 12.9–13.2 s repetition (setup per process about 0.8 s, `build:ui` 0.4 s, so "one process" and "skip the build" were not taken). The harness now launches Chromium with `--disable-frame-rate-limit` (`BROWSER_ARGS` in `perf/harness.ts`, held by `tests/perf.test.ts`): the 600 frames of the scroll start when the one before is done instead of on a 60 Hz tick — scroll 4.9–5.3 s, repetition 8.6–9.2 s, gate 65.8 → 44.9 s. **CPU per frame changes meaning:** the same code reads 0.5–0.9 ms a frame lower unpaced (median 0.6, seven paired comparisons of 2026-09-24), so readings from before and after are not comparable; SPEC section 6, 11-perf and a note in ADR-013 say so. **The pulse is out of the product:** unpaced, the endless `dcpulse` of the live dots composited on about three cores while the page sat idle and moved "Update after an edit" by +40/+59 ms; `.dot.pulse` and `@keyframes dcpulse` are gone from `src/ui`, the footer's and the activity panel's dots stand still (colour and word), `dcin` is the only keyframe (DESIGN.md's One-Keyframe Rule), and with the pulse gone the idle page costs 0.00 CPU-s/s unpaced, so the harness measures the page as it is (no `reducedMotion`). **No budget and no allowance changed:** `git diff cd3debe cc7db19 -- perf/budgets.ts` is empty. The track was cut to this card by the owner on 2026-09-24 ("finish what is in work, take nothing new"): DA-82, DA-56.1, DA-69.1 and DA-56.5 were not taken and stay where they were.

**Decisions.** The owner's, 2026-09-24: `--disable-frame-rate-limit` accepted with CPU per frame changing meaning (~11:17 MSK); the pulse removed from the product entirely (~11:22 MSK); the CPU-per-frame budget kept at 9.5 ms over the proposed 8.8 and 8.5, and `RUNNER_ALLOWANCE` kept at 2.1 (~12:00 MSK); and, shown that "Switching sessions" swings 79–111 ms unpaced with the machine's state (stable at 80–85 at 60 Hz) and reddened one unpaced gate in four, the flag landed as is over landing without it or turning finite animations off in the harness (~12:58 MSK) — the swing recorded in 11-perf and filed as DA-115.3. In the new mode 9.5 is about 13 % over the day's worst reading instead of the 4 % it was set at, so a regression of up to about 1 ms passes the local gate: the owner's choice, not a defect. The orchestrator's: `e2e/shell.spec.ts > nothing pulses for a reader who asked for less motion` left the verdict set — it pinned the pulse the owner took out; its successors are "nothing on the page animates without end" and "a reader who asked for less motion gets arrivals without their travel". `docs/design/prototype.dc.html` still draws `dcpulse` and was left as the designer's file; the deviation is recorded in HANDOFF, as DA-102.1 and DA-96.1 did.

**Verification.** By the worker on the branch, every series under `/tmp/da-perf.lock` (`perf2-evidence.md`, `da115-raw-samples.txt`, `da115-raw-samples-2.txt`; not re-measured here). The condition of the card: two valid `bun perf/compare.ts` runs of the final configuration against the old way (`21cf286`, the base plus the timing lines), nine a side, 08:42–08:56 UTC at 1.1–2.4 runnable per core — six lines `no difference` in both, CPU per frame −0.7 (±0.6, better) and −0.6 (±0.6, no difference); a third run declined (`unable to measure`, another process's load 4.5/5.1 per core). The resolution table of 11-perf ("What the gate resolves") measured again for the new way, two worktrees of one commit, 24 a side; the 60 Hz table kept and marked so. Three later compares of the tip against `cd3debe` (09:12–09:50 UTC) put "Switching sessions" at +31.5, +26.8 and +22.9 ms (the second `worse`, not confirmed by the third), the other lines `no difference` — DA-115.3. Mutation probes: an infinite animation appended to `src/ui/styles.css` reddens exactly "nothing on the page animates without end"; the flag taken out of `perf/harness.ts` reddens exactly "launches Chromium with the frame-rate limit off, so the scroll is not paced at 60 Hz" (`handover-perf2-2.json`). Worker gate record on `cc7db19`, clean, `backslop gates --require-clean --keep-going` in a detached worktree, 09:58–10:02 UTC (`gates-perf2-2.json`): gates 7, green 7 — `backslop lint` 0, `lint` 0 on 256 files, `typecheck` 0, `test` 902/902, `test:bun` 902/902, `test:ui` 152/152, `perf` 7/7 (CPU per frame 7.9 ms against 9.5, session switch 76.5 from 75.9/68.8/79.8/101.6/76.5, update 296 from 298/296/318/291/290; the gate took 42.3 s). Earlier series, replaced: `8689795` 5/7 (a wall-clock watcher verdict and the perf update line, both proved not this track's), `8b12ec8` 7/7, `5bf8800` 6/7 (perf: switch 128.7, update 327 at 2.4 per core; the rerun 86.2 / 311, the base 92.8 / 323). Verdict names `cd3debe` → `cc7db19`: unit 901 → 902 (one added), e2e 151 → 152 (one removed with approval, two added). Review: one isolated pass over `cd3debe..8b12ec8` — critical 0, major 0, minor 3 (the renamed rule in `src/ui/styles.css` comments, the scroll's share against its own samples, the flag guarded by no verdict), all closed in `5bf8800`. The integration gate of this acceptance is `gates-approver-perf2.json`.

**Integration into `main`.** `21cf286 6b99da6 8689795 8b12ec8 5bf8800 cc7db19` cherry-picked without commit onto `94ffa73`, with no conflict. The branch filed DA-115.3 at Order 800 from a base whose queue ended at DA-115 (790); `94ffa73` gave DA-42 Order 800, so after this archive DA-115.3 moved to 790, right after DA-54.6 (780). The tree outside `docs/backlog` and `docs/archive` is the branch's `cc7db19`, file for file.

**Documentation in the same pass.** [11-perf.md](../../reference/11-perf.md#where-a-runs-time-goes) (where a run's time goes, the unpaced harness, time before and after, the ceiling and the allowance, the unpaced resolution table beside the 60 Hz one, the session switch's swing); [08-ui.md](../../reference/08-ui.md#the-activity-panel-and-the-footer) (No pulse, the idle page's cost); `docs/SPEC.md` section 6 (CPU per frame taken unpaced); [ADR-013](../../adr/adr-013-perf-gate-off-ci.md) (the note on the unpaced harness); `DESIGN.md` (One-Keyframe Rule); `docs/design/HANDOFF.md` (§1.3 and "Анимации"); `.impeccable/design.json`; `README.md` (about 45 s); `CHANGELOG.md`, `### Changed`.

**Open.** The final page's CPU per frame on a quiet machine is not measured: 8.0–8.4 ms at 1–2 runnable per core against 7.3–7.6 for the earlier shapes of the page, unexplained. The unpaced resolution table was taken at 1.0–2.5 per core, not on DA-110's quiet machine. The flag is unverified on Linux (DA-115.1). 9.5 ms is about 13 % over the worst reading (the owner's choice). "Switching sessions" swings unpaced with the machine's state (DA-115.3).

**Filed, not closed.** [DA-115.3](../../backlog/queue/DA-115.3-unpaced-switch-swings-with-the-machine.md) (major, queue, Scope 11-perf) — unpaced, the session switch swings 79–111 ms with the machine's state and sits at its 100 ms budget. [DA-115.1](../../backlog/minor/DA-115.1-runner-allowance-unpaced-harness.md) (minor, Scope 11-perf) — the runner allowance rests on a 60 Hz reading; re-measure CPU per frame on the first CI run after the push and recompute `RUNNER_ALLOWANCE` by the rule. [DA-115.2](../../backlog/minor/DA-115.2-cli-stdout-enotconn-reported.md) (minor, Scope 06-cli) — a reader that goes away can surface as `ENOTCONN`, which the CLI reports with exit 2 where it swallows `EPIPE`.
