# DA-25.2 · Result

**Closed 2026-09-05.** Completed as a change of shape, with the cause recorded rather than found. Reproduced on a quiet machine (load average 3.6, no other session measuring): `bun run perf` printed `run 1/3` and stopped, 0 % CPU, no browser child, one idle connection to its own server, and the server itself answering `GET /api/review` in 30 ms — so not a lock, not a stale fixture, not contention. Probes written to stderr between the harness's steps placed the stall in the **second** browser of the process: once after `page.goto` with `waitForFunction(__perf.ready, timeout 120 s)` never returning and never timing out; once, with the CDP session detached before `context.close()`, after `ready` with the next step never returning. Playwright's timeouts not firing and the process idle at 0 % point at the client's transport under Bun after a whole measurement, not at the page. `chromium.launch()` cycles on their own, and inside `withServer`, complete (the finding's own evidence), and a process that measures once and exits completed every time today, for every worker. The gate now runs each repetition as `perf/run.ts --runs 1` in a process of its own, with its own server and browser, and reads the measurement back from stdout; `perf/run.ts` itself is unchanged, so `--runs 3` in one process still reproduces the stall for whoever takes the cause further.

Two facts from the day for that person: `bun run perf 2>&1 | tail` buffers everything until exit, so a running gate looks identical to a hung one — write to a file; and a `pkill -f perf/gate.ts` in one session kills every session's gate on the machine.

**Verification.** `bun run perf` on `main` after the change: three repetitions in 3 min 20 s, CPU per frame 7.3 / 7.0 / 6.9 ms, long tasks 0, first render 98 / 77 / 83 ms, update 236 / 250 / 241 ms, session switch 114 / 91 / 95 ms; exit 0. Before the change, the same tree, the same machine: `run 1/3` and a hang killed after eight minutes, twice.

**Documentation in the same pass.** `docs/reference/11-perf.md` (the gate), `CHANGELOG.md`.
