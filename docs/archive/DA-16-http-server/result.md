# DA-16 · Result

**Closed 2026-09-05.** Completed. `src/server/serve.ts` exports `startReviewServer({ config, ui?, verbose? }) → { url, port, review, close }`: creates the data directory, scans the root into `diff.json` through the core `scanReview`, starts the watcher, listens on `127.0.0.1` only, names a taken port in one sentence; a root without a current session starts anyway, and the start-up read is a warm-up — any failure is one line on stderr and the refusal reaches the request as the documented status. `src/server/review.ts` builds and serialises the document once and keeps it: a rescan hands over the change set, a comment event re-reads comments and counters alone, `session-changed` and `warnings` rebuild. Routes: `GET /api/review` → `ReviewDocument` (`ReviewBundle & { session, comments, counters }` in `src/core/types.ts`, which stays a leaf of pure types the UI project compiles without Node types; `hunks` empty on this route by design) or 404 `no-current-session`; `GET /api/sessions`; `GET /api/config`; `GET /api/scan` (repositories with `hasChanges`, for the first-run screen); unknown `/api/*` → 404; everything else the built UI with `index.html` fallback; `--verbose` request logging to stderr. `src/server/errors.ts` maps `DomainError` codes to 404 (`no-current-session`, `no-such-session`, `no-such-comment`) or 400, `StorageError` to 500 `error: "storage"`. The spike's `buildReviewBundle` and the UI stopgap are gone; `diffalanche serve` runs this server (`--verbose` added); `src/core/change-set.ts` is the one walk (`sameBase` exported), and a cache whose `base` is not the session's is read again. Finding DA-16.1 (the scrolling budget red on `main` under load) taken by the next UI track.

Review by an isolated session, two rounds on this task: a critical (the CLI did not compile against the new server — closed by the merge that wired `serve`), 1 major (a broken data file prevented start-up) and 3 minor (whole-document invalidation on comment events; the probe run per watcher; no test for the 500); all closed in `5fd4fff` and `29ed635`.

**Verification.** Gates as for DA-12; `tests/server.test.ts` 15: the document with totals, session, comments, counters; hunks absent from the response and present in `diff.json`; the same bytes twice from the cached payload; sessions; config; scan with the clean worktree at `hasChanges: false`; the page for a deep link; 404 for an unknown route; "UI is not built"; the 404 body without a session; a broken `comments.json` → 500 `storage` through a running server; a path in `current` → 500; ephemeral port; unreachable from another address; the taken-port sentence; a comment event keeping `repositories` by identity. Mutation probes: start-up rethrowing a StorageError → the 500 test fails; the watcher baseline rethrowing → the same test fails through the watcher; whole-document rebuild on a comment event → the identity test fails. Perf gate: red on the scrolling lines on this branch and on `main` alike at load average 30–40 (DA-16.1); the diff touches no file under `src/ui`, `perf/`, `vite.config.ts`, `scripts/synth.ts`.

**Documentation in the same pass.** `docs/reference/07-server.md` (rewritten), `08-ui.md` (the bundle paragraph), `06-cli.md` (`serve` row and paragraph), `CHANGELOG.md`.
