# DA-96.1 · Result

**Closed 2026-09-24.** Completed. A stream the browser has closed says `disconnected`, and the footer offers a way back. When `onerror` finds the stream `CLOSED`, the sidebar footer shows a still `crit` dot, `disconnected` and, at the end of the same line, `reconnect` — a word of the footer's mono line in `accTx`, recorded in `DESIGN.md` as the footer action, so the footer keeps its height. The press makes a new `EventSource` through the store, as a task switch does, and reads the review again once it is open, since a new stream has no `Last-Event-ID` to replay what it missed; the browser's own reconnects of a stream still do not read it again. The press says `connecting` at once and moves the ring to the footer's own line, where the new state is read, since the button unmounts once the stream is back. The footer is not a live region — a decision in [08-ui.md](../../reference/08-ui.md). The verdict that pinned the old footer, `tests/ui-live-recovery.test.ts > an error on the stream > leaves the footer as it was once the browser has given up`, was renamed with the orchestrator's approval to `… > says disconnected once the browser has given up, and reconnect reads the review again`. The card's `live.ts` quote guarded exactly the `onerror` this changed, so it became a `quote:before:` snapshot at this acceptance. The finding [DA-96.2](../../backlog/minor/DA-96.2-sidebar-footer-under-status-bar-at-top.md) stays in `minor/` with its Scope filled: at scroll 0 the sidebar footer, where `disconnected` is said, sits under the status bar — the base `660190b` has it too. Decisions this acceptance records:

- **A browser that gave up on the stream is a state of its own, `disconnected`, with a way to reconnect** — the orchestrator's ruling during the night. `docs/design/HANDOFF.md` (§1.3, the sidebar footer) and `.impeccable/design.json` were updated with it so the handoff and `DESIGN.md` do not disagree; the owner may reverse it. The dot, the word and where the ring goes after the press are the worker's, written in 08-ui.

**Verification.** Worker record `gates-ui2-2.json` on `da3fba0`, the whole track, on a clean detached gate worktree under the lock — gates 7, green 6 (`test` 853/853, `test:bun` 853/853, `test:ui` 151/151, `lint` 0, `typecheck` 0, `perf` update 299/304/298 ms; red: `backslop lint`, five errors, none from code — three card quotes this acceptance turned into `quote:before:` and two mentions of DA-55.8, whose card is on `main`). On `75b459a` the worker's series had `test:bun` red once on `tests/events.test.ts > the live stream > carries a reply written by the CLI, and the activity line with its author` ("no reply-added within 20000 ms"), a file this task does not touch, which the orchestrator places with a finding of the core3 track, not yet on `main`; green in the final series. Mutation probes, tree clean before and after each, each exit 1: `CLOSED ? "disconnected" : "reconnecting"` → `CLOSED ? "reconnecting" : "reconnecting"` at `567289a` → `tests/ui-live-recovery.test.ts` 3 of 4, red on the renamed verdict, and `e2e/live.spec.ts -g "gave up"` red "a stream the browser gave up on says disconnected, and reconnect brings it back"; `foot.current?.focus()` removed at `d85aaab` → the same e2e red on `toBeFocused`. Review: two isolated passes over the track — critical 0 in both; this task's notes closed in `d85aaab` and `0a553af`. Evidence: `evidence-ui2-final.md`.

**Integration into `main`.** `567289a d85aaab 75b459a 0a553af` cherry-picked without commit onto DA-36.1's commit and squashed into one commit. Git merged `CHANGELOG.md`, `08-ui.md`, `src/ui/store.ts` and `src/ui/styles.css` with no conflict; `CHANGELOG.md` was then set by hand as for every task of the track: `main`'s file, and the branch's final entry once, at the top of Unreleased / Fixed, in the branch's layout. From this commit on, the two `reconnect` verdicts DA-56.8's `e2e/focus.spec.ts` carries have the footer they read.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (`disconnected`, and `reconnect`), `docs/design/HANDOFF.md` (§1.3), `DESIGN.md` (the footer action), `.impeccable/design.json`, `CHANGELOG.md`, `### Fixed`.
