# DA-74 · Result

**Closed 2026-09-18.** Completed. The name-based half of the burst decision moved out of the `burstIsIgnored` closure into the exported `dropsVerdicts(paths, cache)` in `src/core/watcher/index.ts`, so a burst of `[".git"]` can be exercised without the runtime that collapses the name. Of the task's three candidates the second was taken: the cache is cleared for anything the watch reports inside `.git` — the bare directory included — and `changesWhatGitIgnores` and `insideGitDir` keep their own meanings. The first would have made `changesWhatGitIgnores` say something untrue about the name it matches; the third is close to having no cache at all. The cost is that a plain commit or branch switch now discards that repository's verdicts too, so a build writing into `dist/` pays one extra `git check-ignore` afterwards — one process against the four of a rescan. **The `.gitignore`-reported-as-its-containing-directory case was looked for and not reproduced**, which the task file required a record of either way: a probe with `fs.watch` and `recursive: true` over a temporary tree, armed by repeated writes until an event came back, then one write to `sub/.gitignore`. Node v25.2.1 reported `sub/.gitignore`; Bun 1.3.14 reported `sub/.gitignore` on one run and delivered no name at all on a second — the "Bun's watch goes quiet" behaviour `docs/reference/05-watcher.md` already records, not a collapsed name. Neither runtime named the containing directory on macOS, so the fix is **not** extended to that shape and the hypothesis stays open rather than being closed on the strength of the `.git` case.

**Verification.** New case `what a repository's watch reports > drops the kept verdicts when a burst names git's directory in any shape`: a verdict is stored for `dist/out.js`, a burst whose only name is `.git` drops it, `.git/index`, `.gitignore`, `src/.gitignore` and `.git/info/exclude` do the same, and an ordinary burst leaves the answer where it is. It is a unit check, so it fails on both runtimes rather than only on the one that collapses the name. Mutation probe by the worker: `index.ts:673` with `|| insideGitDir(path)` removed — exit 1, 27 of 30 passed, 3 reddened including this one. A second probe in the other direction, the naive over-reporting edition with `dropsVerdicts` replaced by `cache.clear(); return true;` — exit 1, 26 of 30, 4 reddened including this one, so the check is one-sided neither way. Worker gates on `abdd300`, clean tree: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (469/469), `bun run test:bun` exit 0 (469/469). **Integration into `main`:** `d6cdd5d` cherry-picked onto the DA-88 commit, together with this task's share of the pack's review-fix commit `cc8db8d` — the two shortened doc-comments over `ignoredPaths` and `burstIsIgnored`, the one over `dropsVerdicts`, and the reflowed paragraph in the reference; `CHANGELOG.md` merged by entry, `main`'s entries kept. Gates on the merged tree of the whole watcher track: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (35 files, 487 tests), `bun run test:bun` exit 0 (35 files, 487 tests). `bun run perf` is measured once through the run's lock on the committed head; its numbers and the machine's load are in the approver's gate record `gates-approver-watcher.json`.

**Documentation in the same pass.** `docs/reference/05-watcher.md` — the paragraph on the kept answers states the rule the code has, with the reason the bare `.git` is in it and what it costs. `CHANGELOG.md`, `### Fixed`.
