# DA-60.2 · Result

**Closed 2026-09-24.** Completed: the event was **late, not lost**, and the load did not cause it — a session lock another test left behind did. The verdict `tests/watcher.test.ts` › a comments.json that cannot be read › "stops the comment events and leaves the rest of the chain running" ran out of its 20 s because its `settle` came 24.2–27.0 s after the write: the rescan behind it waited for the lock of the session `synth`, which the rescan before it had taken and could not release — its release was refused with `EACCES`, because the listing test earlier in the file ("keeps what it knew when `reviews/` cannot be listed") had `chmod`ed the fixture's `reviews/` to `0o000` while the fixture's watcher was still inside the lock — so the lock stayed until its 30-second lease ran out. The fix is in the test: the listing test copies `reviews/synth/review.json` into a data directory of its own and makes that one unreadable, never one a running watcher writes. An overdue wait now reports afterwards whether it was late or never came: `settle` of `tests/watcher.test.ts` and `next` and `waitFor` of `tests/events.test.ts` keep what they waited for when they give up, and each file prints at its end, per recorded wait, when its event came or that it never did. No deadline, timeout or verdict name changed. This acceptance makes that verdict the core3 track's: a red of it in a later gate is not "the watcher's night red" any more but a regression of this fix, until proved otherwise.

**Verification.** Instrumented probe rounds by the worker on `55316eb`, before any change, whole `test:bun` suite under `/tmp/da-perf.lock` with `yes` burners: 6 of 18 runs red, at load 12–220 when the run began, every red the same verdict and the same 22nd `settle` — 24167, 24953, 25853, 26865, 26998 and 25777 ms — while the other twelve took 0.2–0.6 s and the ~160 other settles of each run 0.2–1.8 s; no wait ran past 120 s. In the three reds of round 2 the rescan waited 25489, 26487 and 26305 ms for the lock of `synth`; round 3 traced the lock and caught the holder's release throwing `EACCES` in the red run only (1 of 6). Round 4, the fix applied, 8 burners, load 5.5–137.3: 6/6 exit 0, 861/861, settle 22 in 236–528 ms, no failed release. Mutation probe after the commit, tree clean before and after: the old `chmod` on the fixture's `reviews/` put back (`tests/watcher.test.ts:769`) → 8 loaded `test:bun` runs, runs 1–7 green, run 8 exit 1, 860/861, red on this verdict with `Error: the watcher never caught up`, and the kept report printed `settle 22: the diff-changed naming settle-22.ts came 26603 ms after the write — late`. Worker gate records of run 0923 on clean detached worktrees under the lock: `gates-core3-3.json` on `3a76fe9` — gates 7, green 6 (`test` 861/861, `test:bun` 861/861, `test:ui` 121 passed, `perf` 7/7, update after an edit 285 ms); `gates-core3-4.json` on `d456c14`, the whole track — gates 7, green 6 (`test` 863/863, `test:bun` 863/863, `test:ui` 121, `perf` 7/7, update 292 ms, `lint` 0, `typecheck` 0); the one red in both, `backslop lint`, is DA-55.8's and DA-60.3's quote blocks, turned into `quote:before:` when DA-55.8 closed. The card's second verdict, the CLI reply's `reply-added` in `tests/events.test.ts`, took 8–380 ms in all 18 runs and did not reproduce: filed as DA-60.4. Review: the second isolated pass, over `ce57b2d..3a76fe9`, and the orchestrator's read of the diff. Evidence: `evidence-core3-b3.md`, `handover-core3-3.json`.

**Integration into `main`.** `3a76fe9` cherry-picked without commit onto DA-55.9's commit. Git merged `tests/watcher.test.ts` with no conflict; `CHANGELOG.md` was kept as `main`'s file, and the branch's DA-60.2 entry added once at the top of Unreleased / Fixed, in the branch's layout.

**Documentation in the same pass.** [11-perf.md](../../reference/11-perf.md#waits-in-the-suites) (a wait that runs out says whether its event was late or never came; the red read by it; a test that takes permissions away takes them from a data directory of its own); `CHANGELOG.md`, `### Fixed`.

**Filed, not closed.** Both `minor`, in `minor/` with their Scope filled: [DA-60.4](../../backlog/minor/DA-60.4-cli-reply-frame-red-not-reproduced.md) (hypothesis) — the CLI reply's `reply-added` ran out of 20 s at load 32 and did not come back in 18 loaded runs; [DA-60.5](../../backlog/minor/DA-60.5-settle-returns-on-another-write.md) — `settle` in `tests/watcher.test.ts` returns on any `diff-changed` of the other repository, not on its own write's.
