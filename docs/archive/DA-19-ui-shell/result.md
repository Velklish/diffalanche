# DA-19 · Result

**Closed 2026-09-05.** Completed. `src/ui` is the Phase 1 shell over the spike: every token of both themes as CSS variables in `tokens.css` (`:root` dark, `:root[data-theme="light"]` light, `color-scheme` per theme), the theme toggle in the header persisted in `localStorage` and applied by an inline script in `index.html` before the first paint; local woff2 fonts (Instrument Sans, JetBrains Mono with cyrillic subsets) with the handoff's fallback stacks; the layout — header 52, sidebar 308, centre, right column 392, status bar 30, minimum width 1560 with the page scrolling sideways below it — as sticky regions in one document scroll so the perf harness keeps working; overlay and toast primitives, `dcin` and `dcpulse`; the logo from markup and the favicon as an inline SVG; a zustand store with the handoff's slices plus a `review` slice for the server bundle and derived counters computed once when the bundle lands; a loading skeleton (real header, placeholder sidebar rows, one empty card) and a failure line when `GET /api/review` fails; `src/ui/types.ts` mirroring SPEC section 7. The TypeScript configuration is split into a Node-side project (core, cli, server, scripts — no DOM), a UI project (DOM, no Node types), and a tools project (tests, perf, e2e); TypeScript refuses `noEmit` projects behind `references` (TS6310), so `bun run typecheck` runs the three with `tsc -p` in one command instead of a project reference — the task's wording is superseded by that, recorded in `docs/reference/08-ui.md`. A stopgap in `src/server/review.ts` served `session` and `comments` from the data directory until DA-16 replaces it, with the `current` pointer accepted only as a session name.

Review by an isolated session, three rounds over the track: round one found 4 major (sidebar skeleton unused, no failure path, a dead `mounted` rule, the stopgap untested) and 6 minor; all closed in `203865b` and `3b0ce1b` (a traversal test rewritten after a mutation probe showed it proved nothing).

**Verification.** Gates on the worker branch at `ebb4b92` and on `main` after integration: `backslop lint` no errors; `bun run lint` 82 files; `bun run typecheck` clean on three projects; `bun run test` 106 tests; `bun run test:ui` 18 Playwright tests (shell: both themes against committed darwin baselines, theme surviving a reload, 308 and 392 px at 1560, sideways scroll at 1400, no request outside `127.0.0.1`); `bun run perf` green (78.8 ms / 0 / 7.7 ms / 5.7 ms / 13.9 ms at load 10). Mutation probes: sidebar width changed → widths test fails; a font pointed at `fonts.gstatic.com` → the network test fails; the traversal check removed → the rewritten test fails. Not run in CI: `test:ui` stays outside `gates` until DA-28.

**Documentation in the same pass.** `docs/reference/08-ui.md` (rewritten: page, tokens, store, loading, tests), `docs/reference/07-server.md` (the stopgap), `README.md`, `CHANGELOG.md`, `tsconfig*.json`, `.gitignore`.
