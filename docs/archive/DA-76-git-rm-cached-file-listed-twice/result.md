# DA-76 · Result

**Closed 2026-09-19.** Completed. A repository's change set is built from two sources, and `git rm --cached` makes both of them name the same path: the entry leaves the index, so the diff reports a deletion, and the file stays on disk, so `ls-files --others` reports an untracked file. Both are correct and the change set carried the path twice — the counters doubled it, the UI rendered two cards under one React key, and a comment on it could not be anchored at all, because the lookup takes the first match and that entry had no new-side lines. The decision the task asked for: **one entry per path, and the entry kept is the diff's deletion.** That is what the change is. The case this is usually reached by is `git rm --cached .env` on a secret committed by accident, where the content leaving the repository is exactly what a reviewer needs to see; listing it again as an addition would say the opposite. That the file is still on disk is a warning rather than a second entry: `creds.txt is deleted from the base and still on disk: it was untracked out of it`. The de-duplication sits where the two sources meet, in `readRepositoryChange`, rather than in the UI or in `findFile`: every consumer keys by path and takes the first match, so one entry per path is what makes all of them right at once. No change under `src/ui` belongs to this task and none was made.

**Verification.** A test builds the repository the card describes, runs `git rm --cached`, and asserts one entry, `status: "deleted"`, the warning, and that the entry keeps the hunk an anchor needs; review added the anchor itself to that verdict — `captureAnchor` on the old side returns the deleted line, and on the new side refuses with `lines on the old side only` rather than with "no hunks in the change set". Removing the de-duplication reddens it. The card's reproduction was re-run against the plumbing `diff-index` that DA-65 introduced, to be sure this task was still about what it said: the deletion is reported identically. Worker gates on `fbaf0e1`, clean tree: backslop lint 0, `bun run lint` 0 (176 files), `bun run typecheck` 0, `bun run test` 0 (35 files, 469 tests), `bun run test:bun` 0 (35, 469); `bun run perf` exited 1 on two lines at load average 33.2 and was re-measured at integration. **Integration into `main`:** `9c62587` cherry-picked onto the DA-66 commit, plus the three parts of `4c4e681` that belong to this task — the triage card `DA-76.1`, the paragraph in `docs/reference/02-git.md` that points at it, and the anchor assertions with the `captureAnchor` import they need. No conflict; `CHANGELOG.md`, `src/core/git/index.ts`, `docs/reference/02-git.md` and `tests/git.test.ts` auto-merged. `9c62587` carries a second task under a DA-76 subject — the CHANGELOG entry, the reference paragraph and the three verdicts of DA-95 — and those went to the DA-95 commit instead, which is where their subject is. Gates of the merged tree: see `docs/archive/DA-98-scan-routes-fan-out-unbounded/result.md`; after this commit `backslop lint` 0 and `bun run typecheck` 0.

**Documentation in the same pass.** The `head` paragraph of `docs/reference/02-git.md`, which asserted that the two sources never name the same file, now says what happens when they do, and a paragraph beside it names the case the de-duplication does not cover; the doc comment in `src/core/git/index.ts` that asserted the same; a `### Fixed` entry in the CHANGELOG. Finding DA-76.1 — a file that changes type is two patches for one path from the diff alone — filed against this task and left in `docs/backlog/triage/`.
