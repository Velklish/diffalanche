# DA-100.2 · Result

**Closed 2026-09-23.** Completed, with the first of the card's two candidates. The restore waits until a switch under way has ended — `Overlay` reads the store's `switching` and restores one macrotask after it clears — and is owed only while the ring is still on `<body>`, so a ring the reader moved during the wait stays where they put it. Both live paths then find the opener already disabled or gone and hand the ring to DA-100.1's fallback: creating a task from select mode lands on the new task's `SCOPE` pill, and `Change base` → `Apply` on the no-changes screen lands on the `BASE` pill. Before relying on the flag the worker checked that both actions hold it across the read: `applyBase` and `createSession` set `switching` before their request and clear it after the review is read (`src/ui/store.ts`). The other candidate — watching the restored control for a lost ring — was rejected: it rests on `focusout` firing for a removed or disabled control, which Chromium does and no other engine was measured for, and on a timer whose length is a guess. The cost is written in `08-ui.md` beside the promise: the overlay primitive reads one store flag, and an action that closes an overlay and then loads without `switching` would slip past it.

**Verification.** Playwright on both live paths, not a synthetically removed opener: `e2e/scope.spec.ts` › "after a task is made from select mode the ring is on its SCOPE pill", `e2e/empty.spec.ts` › "a base that brings changes leaves the ring on the BASE pill", and for the ring the reader moved, `e2e/empty.spec.ts` › "a ring moved while the base is being applied stays where it was put" (it holds `/api/review`, moves the ring to the header's search, releases the answer and asserts the ring stayed). Mutation probes by the worker on `761f52c`, `git status --porcelain` empty before and after, Playwright over `empty.spec.ts` and `scope.spec.ts`: the wait removed (`Overlay.tsx:90` → `if (true)`) reddened exactly the two live-path cases, exit 1, 16 of 18 passed; the guard for a moved ring deleted (`Overlay.tsx:106`) reddened only the moved-ring case, exit 1, 17 of 18. Verdict names base → track tip `927d582`: `vitest list` 562 → 574, Playwright `--list` 95 → 104, none removed. Worker gates on `927d582`: `backslop lint` exit 0 (0 errors, 2 warnings — this entry and DA-56.7 in triage, the approver's), `bun run lint` exit 0, `bun run typecheck` exit 0, and under `/tmp/da-perf.lock` `bun run test` exit 0 (41 files, 637/637, load 38.9), `bun run test:bun` exit 0 (637/637, load 44.1), `bun run test:ui` exit 0 (104 passed); `bun run perf` declined by its load guard at 2.69 per core. On `91d92c4` the perf gate did measure: every line in budget except "Update after an edit in one repository", median 430 ms against 300 (runs 448 / 430 / 311), and the base `5a5f8e8` is red on that same line the same day (572 / 321 / 349 ms, measured by another track); this task does not touch the update path. Review: one isolated pass over `5a5f8e8..227bdf0` for the whole track, critical 0, major 2, minor 3, plus two legacy observations; this task's share — the guard for a moved ring had no test, the reason DA-100.1's cases break the opener by hand is now that `Escape` starts no switch, and `08-ui.md` did not list the live-path cases — was closed in `a17a83b`, and `761f52c` then named the search button exactly, because `getByRole` matched a substring of a repository row and strict mode failed. **Integration into `main`:** `fbd9380`, `0e792c6`, `a17a83b` and `761f52c` cherry-picked as one commit onto DA-113's; `CHANGELOG.md` and `docs/reference/08-ui.md` auto-merged without a conflict. The integration gates ran once on the merged tree after the last task of the track; the numbers are in the `result.md` of the track's last task, `focus-ring-below-non-text-contrast` in the archive.

**Documentation in the same pass.** `docs/reference/08-ui.md` › the overlay's focus restore (the wait for a switch, the moved ring, the cost of reading one store flag) and the UI tests paragraph on the live-path cases; `CHANGELOG.md`, `### Fixed`, where DA-100.1's entry no longer says the two paths still lose the ring.
