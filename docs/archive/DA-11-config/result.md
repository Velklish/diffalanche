# DA-11 · Result

**Closed 2026-09-05.** Completed. `src/core/config/` loads `config.json` from the data directory, applies the SPEC section 7 defaults (`roots: ["."]`, `depth: 2`, `port: 4880`), validates types and ranges (`port` a whole number 1…65535 from the file and from `--port` alike, `depth` ≥ 0, `lsp.<language>` a non-empty command array), merges the command-line overrides (`root`, `dataDir`, `port`), and exports a typed `Config` with absolute `root`, `dataDir`, and `roots` (resolved against the root; `--root` and `--data-dir` against the current directory). `user` falls back to `git config user.name` from the root, then the OS user name. Errors are `StorageError { file, field }`, the same type storage raises, with the shared field readers moved to `src/core/storage/fields.ts`. `config.json` is only read. The glossary `warning` row now names both sources (scan and storage).

Review (one report with DA-8): two minor findings on config — `--port` skipped validation of the file's `port` and of itself; `asLsp` validated twice — fixed in `2359409`, with the reference wording on who owns `config.json`.

**Verification.** Gates on the worker branch and on `main` after integration: `backslop lint` no errors; `bun run lint` 56 files; `bun run typecheck` clean; `bun run test` 78 tests in 12 files (config suite: defaults, a wrong `port` names file and field, `--port` above the file and validated as `command line: --port`, `user` from git and from the OS). Mutation probes after commit: override priority removed → its test fails; `user` fallback always OS → the git-branch test fails while the OS-branch test stays green; `asPort` accepting anything → the range test fails. Perf gate: red on long tasks under load average 11–15 with three worker sessions, green on the next run; the diff touches nothing under `src/ui`, `src/server`, `perf/`.

**Documentation in the same pass.** `docs/reference/03-storage.md` (Config section; storage owns session files, config owns `config.json`), `docs/reference/README.md`, `docs/GLOSSARY.md`, `CHANGELOG.md`.
