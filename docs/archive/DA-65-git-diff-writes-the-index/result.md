# DA-65 · Result

**Closed 2026-09-19.** Completed. The change set comes from `git diff-index -p -M <base>` instead of `git diff`. The porcelain refreshes the index on its way out, which takes `.git/index.lock` and rewrites `.git/index`: a write to a reviewed repository on every scan, and a race with a `git add` running there at the same moment. `-M` is passed because the plumbing inherits none of the porcelain's defaults and rename detection is one of them. `-c diff.autoRefreshIndex=false` was measured as an alternative that also stops the write, and rejected because it keeps those implicit defaults, where the module's rule is that the tool names what it wants ([ADR-012](../../adr/adr-012-git-trust-model.md)). Moving to the plumbing also moved a guarantee: `diff-index` has textconv and the external driver off unless asked for, so the `--no-textconv` flag is belt-and-braces there and it is the enumerated pin that makes a later `--textconv` fail loudly. ADR-012 says so, measured both ways. The guard changed with it, which is the larger half of this task. `git status --porcelain` was the whole of it and is blind to an index rewrite, a ref update, a commit in a repository that started clean, and a subcommand that writes. `tests/git.test.ts` now snapshots `.git/index` byte for byte, the commit HEAD names and every ref of each of the six fixtures, before and after a full read in all three base modes; `tests/scope-scan.test.ts` pins the subcommands a scan runs to `config`, `diff-index`, `ls-files` and `rev-parse`.

**Verification.** Output equality against the porcelain was checked on the shapes the parser lives on — two renames, a binary addition and deletion, two mode-only changes, a deletion, a quoted non-ASCII name and a tab-padded one — with the index both clean and stat-dirty: 43 lines, 9 files, `diff -q` silent. The index rewrite was reproduced on git 2.54.0 with hashes either side. Two mutation probes by the worker, both on the committed tree. Putting `git diff` back reddens the write guard — the test the card asks for before calling this closed. Inserting `gitOrNull(cwd, ["fetch", "--quiet", "origin"])` into the base resolution reddens the argv assertion with `+ "fetch"`, the case the old guard missed entirely. A trap is worth recording because it nearly closed this task on nothing: the first guard was **green** with the porcelain restored. A file touched to *now* is racily clean — git re-hashes it, finds the content unchanged and writes nothing. The fixture's tracked files are aged to 2026-01-01 on purpose, and only then does the guard see anything. The card's statement that `git status --porcelain` rewrites the index itself does not reproduce on git 2.54.0; with a stat-dirty index it left the index untouched. `--no-optional-locks` is in the snapshot regardless, because it is correct on both versions. Worker gates on `fbaf0e1`, clean tree: backslop lint 0, `bun run lint` 0 (176 files), `bun run typecheck` 0, `bun run test` 0 (35 files, 469 tests), `bun run test:bun` 0 (35, 469); `bun run perf` exited 1 on two lines at load average 33.2 and was re-measured at integration. **Integration into `main`:** `e2a9820` and `b5c1686` cherry-picked onto the DA-87 commit, plus the two hunks of `4c4e681` whose subject is this task rather than the DA-98 it was committed under — the comment over `STATUS` in `src/core/git/patch.ts`, which had gone on naming `git diff` after the move to the plumbing, and the comment over `snapshot()` in `tests/git.test.ts`, the write guard's own. No conflict on either commit; `CHANGELOG.md` auto-merged by entry. Gates of the merged tree: see `docs/archive/DA-98-scan-routes-fan-out-unbounded/result.md`; after this commit `backslop lint` 0 and `bun run typecheck` 0.

**Documentation in the same pass.** `docs/reference/02-git.md`: the command table, a section on why the change set comes from the plumbing and what the guard now covers. A `### Fixed` entry in the CHANGELOG. The four statements that said the module writes nothing — `src/core/git/run.ts`, `src/core/git/index.ts`, `02-git.md` and `docs/SPEC.md` section 11 — needed no edit: they are true now.
