# DA-33.1 · SPEC decision 10 already states the npm answer that open question 1 still asks

- **Scope:** 09-ml
- **Created:** 2026-09-23
- **Parent:** DA-33
- **Cost:** minor

## Evidence

Finding discovered while working on DA-33. Decision 10 of `docs/SPEC.md` section
3 answers the question section 12 keeps open — a reader of the decision takes
the download as settled:

<!-- quote:../../SPEC.md -->
10. **Suggestions from history.** A multilingual embedding model ships with the tool (118M parameters, about 120 MB in int8) and works offline with nothing to install. It runs in the server process, so suggestions are available from the CLI as well. The binary embeds the model; the npm package downloads it into a cache on first run. A generative 0.5B model (about 400 MB quantized) is downloaded on demand with `model pull`. There is no fine-tuning: "in your style" comes from retrieving similar past comments and using them as examples.
<!-- /quote -->

while question 1 of section 12 still reads "download on first run, or a separate
`@diffalanche/model` package?".

When the owner decides on ADR-014, the acceptance pass makes the two agree:
decision 10 says what was accepted and links the ADR, and question 1 goes.
