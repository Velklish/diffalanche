# DA-33 · Result

**Closed 2026-09-23.** Completed. `src/core/ml/embed` loads `multilingual-e5-small` (int8 ONNX, `Xenova/multilingual-e5-small` at `761b726dd34f`) with `onnxruntime-node` 1.30.0 and `@huggingface/tokenizers`, embeds one text per run with pool threads that do not spin, and gives the same vector on Node and on Bun; `diffalanche model status [--json]` names the model, its revision and its cache directory and says whether the files are there; `bun run model:fetch` puts the pinned files in the user cache and checks their SHA-256 for development and CI, which caches them; `tests/snapshots/embedding-e5-small.json` records the vectors of four texts and so pins the model, its prefix and its pooling. [ADR-014](../../adr/adr-014-embedding-model-and-npm-delivery.md) carries the measurements and is **Accepted**: the owner accepted it as proposed on 2026-09-23, a decision given to the orchestrator of run 0923 and relayed in the acceptance brief — 1A multilingual-e5-small at `761b726dd34f`, 2A onnxruntime-node 1.30.0 with pool threads that do not spin, 3B no model on darwin-x64, 4A the npm channel downloads the model on first use into the user cache, 5B the runtime's native files travel the same way, 6A one text per run, a memory ceiling of 768 MiB — with the one-second budget not yet measured on a quiet machine. That measurement is [DA-33.2](../../backlog/queue/DA-33.2-embedding-budget-on-a-quiet-machine.md) (major, queued): 200 comments after warm-up at intra-op threads 1, 4 and 6, five runs each on a quiet machine, and a miss goes back to the owner. `docs/SPEC.md` decision 10 now says what was accepted and links the ADR, and open question 1 of section 12 is removed. Nothing embeds yet: the index (DA-34), `suggest` (DA-35) and the delivery (DA-41) follow, and [DA-34.1](../DA-34.1-embedding-run-blocks-server-event-loop/task.md) (major), the server's event loop during a run, is queued beside DA-34.

- **DA-33.1** — fixed by this acceptance: decision 10 of `docs/SPEC.md` states the accepted delivery and links ADR-014, and question 1 of section 12 is gone; the Windows question that was 2 is now 1, and DA-45's citation says so.

**Verification.** Worker gates on `4d7da39`, the track tip: `backslop lint` 0, `bun run lint` 0 (198 files), `bun run typecheck` 0; `bun run test` and `bun run test:bun` 0 on the second pass, 639/639 each — the first pass had one wall-clock red each (`tests/watcher.test.ts` › "rescans the edited repository alone and has the new hunk in diff.json in time", 609.98 against 534.10 ms; `tests/synth.test.ts` › "produces byte-identical trees for the same seed", timed out at 5000 ms), each green alone twice on the branch and once on `5a5f8e8`, and neither test changed since the base. `bun run test:ui` 0 (95) and `bun run perf` 1 on `b2749bf`, four lines over at load 13.8–19.8: nothing on the perf path imports a changed module (`grep` exit 1), the UI bundle is byte-identical to the base's by SHA-256, and the update line is red on the base as well. Mutation probe on `4d7da39`: without the prefix at `embedder.ts:59` "the model on node gives the vectors recorded for this model and its prefix" goes red, exit 1, 10 of 11. Review: one isolated pass over `5a5f8e8..b2749bf`, critical 0, major 3, minor 7; #1 and #4–#10 closed in `4d7da39`; #2 (perf) is answered by the reasoning above and the integration gate below; #3 (the one-second budget) is closed by the owner's acceptance and DA-33.2. The reviewer's fourth Decide item, who writes the model on the user's side, is DA-41's by ADR-014's Consequences. **Integration into `main`:** the five commits cherry-picked as one onto `54b2c28`. `CHANGELOG.md` conflicted at `042a715` with the entries of DA-56.2 and DA-96 and was resolved by hand with both sides kept, because `backslop merge-changelog` put entries under a new `### Fixed` heading and left a `backslop:conflict` mark on DA-53.1's; `docs/SPEC.md` and `docs/reference/06-cli.md` merged cleanly. Every file the branch touched outside the acceptance's own is identical to `4d7da39` or differs by main's hunks alone (`docs/reference/06-cli.md`). Integration gates on the merged tree (`74ea756`, this commit before these numbers were written in), all exit 0: `backslop lint`, `bun run lint` (200 files), `bun run typecheck`, and under `/tmp/da-perf.lock` 23:04:52–23:07:42 MSK `bun run test` (43 files, 669/669), `bun run test:bun` (669/669), `bun run test:ui` (104 passed) and `bun run perf`, which measured at a load of 17.2 falling to 11.1 over 8 cores and held all seven lines: first render 95.1 ms, long tasks 0, CPU per frame 8.1 ms, comment form 22.2 ms, file jump 12.4 ms, session switch 68 ms, update after an edit 281 ms (medians of 3). Reviewer's #2 needed no base comparison: no line was red.

**Documentation in the same pass.** ADR-014 (status, Deciders, the Decision's opening and the budget's consequence), `docs/README.md` (ADR index), `docs/SPEC.md` (decision 10, section 12), `docs/reference/09-ml.md`, `CHANGELOG.md` (the DA-33 entry), and DA-45's citation of section 12; from the branch, `docs/reference/06-cli.md`, `09-ml.md`, `11-perf.md`, `docs/reference/README.md` and `README.md`.
