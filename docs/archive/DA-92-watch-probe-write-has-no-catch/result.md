# DA-92 · Result

**Closed 2026-09-18.** Completed. The write at the end of `probeRecursiveWatch` was the one detached promise in the module: `void writeFile(…)` with no handler, so a rejection escaped the `try` that wraps the probe — the `void` cuts it out of the promise that `try` awaits — and both supported runtimes end the process on an unhandled rejection. The probe is on the start-up path of every server, so a write that failed there aborted `diffalanche serve` mid-start instead of answering the question the probe exists to answer. Of the two candidates, **`.catch(() => done(false))`** was taken rather than the silent `.catch(() => undefined)` of the neighbouring line: a filesystem that is already refusing writes will refuse the repeated ones too, so swallowing the first only buys a `PROBE_TIMEOUT_MS` wait before the same answer, and answering at once puts the server on the walk that much sooner. `done` tolerates a second call, so a rejection arriving after a successful `done(true)` changes nothing, and the probe's contract is unchanged: a boolean, never a throw. The three neighbours the task names were examined rather than assumed — the two `void write(…)` in `src/server/events.ts` are safe, since `write` attaches its own rejection handler at `events.ts:158-165`, while `void stream.close()` is detached in exactly the way this task is about and is caught in the same pass (`src/server/events.ts:180`).

**Verification.** New case `watching a tree > answers instead of throwing when the probe's first write fails`: the probe is exported past the memo of `supportsRecursiveWatch` — which `tests/watcher.test.ts` has already filled with `true` for the process — and takes the writer it uses, so the failure is driven rather than waited for. With a writer that rejects, the probe answers `false` and the runner stays up. Mutation probe by the worker with the new `.catch` removed: exit 1 with `Unhandled Rejection: Error: ENOSPC`, all 33 verdicts of the file passing and the run red — which is what the task predicted and what makes the point; that probe has no verdict name to record, so it is in the run's notes rather than in the handover record. Worker gates on `89e8d26`, clean tree: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (473/473), `bun run test:bun` exit 0 (473/473). **Integration into `main`:** `0ffd9a4` cherry-picked onto the DA-85 commit; `CHANGELOG.md` merged by entry. Gates on the merged tree of the whole watcher track: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (35 files, 487 tests), `bun run test:bun` exit 0 (35 files, 487 tests). `bun run perf` is measured once through the run's lock on the committed head; its numbers and the machine's load are in the approver's gate record `gates-approver-watcher.json`.

**Documentation in the same pass.** `docs/reference/05-watcher.md`, the paragraph on the probe: every write it makes is caught, and a disk that fills between the `mkdir` and the write answers "no" at once instead of ending the process. `CHANGELOG.md`, `### Fixed`.
