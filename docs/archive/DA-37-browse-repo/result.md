# DA-37 · Result

**Closed 2026-09-23.** Completed. A repository of the review can be read outside its diff: the sidebar's `all files` tab lists every file of each repository, the unchanged ones marked `unchanged`; one of them — or the file of any card through `Browse repo`, `B`, or a `file · unchanged` hit in global search — opens whole and read-only in place of the review, numbered from 1, with `not in this review` and `working tree` / `base <sha>` segments, and `← back to review` or `B` returns to the exact place, because the review is folded away rather than unmounted. `GET /api/repos/:repo/tree` and `GET /api/repos/:repo/file?path=&rev=` answer it inside the task's scope, reading only a path git lists. `↑ N lines` puts the working tree's lines above a hunk into the diff, twenty a press. Decisions this acceptance records: a line outside the change set is anchored from the file at write time, in the same `{ lineContent, hunk, before, after }` shape a line of the diff has, `hunk` being the header of the context window; the window's other side is numbered from the hunk bound — a hunk that starts at or above the line counts whole, since a window that begins inside one begins in its trailing context (the lookup into the hunk's lines was dead by the mutation probe and is gone); the old side of a rename is read from the base under its old name; `↑ N lines` is clamped to the gap above the hunk and ignores a press while the file is on its way. Left open in `minor/`: [DA-37.1](../../backlog/minor/DA-37.1-browse-follow-ups.md) (browse follow-ups), [DA-37.2](../DA-37.2-old-side-thread-under-new-line/task.md) (an old-side thread under the new line), [DA-37.4](../../backlog/minor/DA-37.4-all-files-tab-at-scale.md) (the `all files` tab at scale, hypothesis), and [DA-37.5](../../backlog/minor/DA-37.5-first-render-costs-eight-ms-more.md), filed by this acceptance: first render 93.9 → 102.1 ms in the worker's alternation, three runs a side above the load ceiling, against a 500 ms budget — and not shown by the acceptance's own alternation below.

- **DA-37.3** — closed by this acceptance on the owner's decision of 2026-09-23, given to the orchestrator of run 0923 directly: an agent's `comment --line` anchors a line outside the change set the way the UI does, through the same file source (`fileSourceAt`) — the working tree for `--side new`, the resolved base for `--side old` — in the same shape; `line-not-in-diff` stays for a line the file does not have and now names how many lines it has. Recorded as the amendment of 2026-09-23 to [ADR-004](../../adr/adr-004-agent-contract.md); `docs/SPEC.md` sections 8 and 9, `docs/reference/04-domain.md`, `06-cli.md`, the `diffalanche-review` skill and its CLI reference say so. Held by `tests/cli-comments.test.ts` › `comment on a line the change set does not carry` › `anchors it from the file, in the shape a line of the diff has (ADR-004)` and `still refuses a line the file does not have`; without the source in `src/cli/commands/comment.ts` both go red, 2 of 19 (worker's probe on `7a39004`).

**Verification.** Worker gates on the track tip `a572e79`, clean (`gates-browse-4.json`): gates 7, green 6 — `backslop lint` 0, `bun run lint` 0 (206 files), `bun run typecheck` 0, `bun run test` 0 (44 files, 678/678), `bun run test:bun` 0 (678/678), `bun run test:ui` 0 (104/104); `bun run perf` 1, declined at 3.84 per core over the 2.5 ceiling. The worker's alternation on `a572e79` against `5a5f8e8`, three `perf/run.ts` processes a side at a load of 30.69 falling to 16.67: update 288 / 285 ms and switching 69.8 / 81.6 inside the spreads, first render 93.9 / 102.1 outside both — DA-37.5. Mutation probes: on `943b91d` the `ls-files` guard of `src/core/git/browse.ts` removed turns 3 of 14 in `tests/browse.test.ts` red, and an off-by-one in `src/ui/context.ts` 2 of 7 in `tests/ui-context.test.ts`; on `7a39004` the old loop bound in `src/core/domain/anchors.ts` turns red `a comment outside the change set numbers the other side from the hunk when the window starts inside one`, 1 of 15. Review: two isolated passes — over `5a5f8e8..00dce9f` critical 0, major 6, minor 6; over `5a5f8e8..7a39004` all twelve closed and five new (major 1, minor 4), closed in `2c39394`, `a57a39a`, `0c91b41` and `a572e79` and read by the orchestrator in the diff. **Integration into `main`:** `943b91d 3ed9de2 a491782 00dce9f b66e6ef 7a39004 0c91b41` squashed onto `3b338a9`. `CHANGELOG.md` conflicted with main's DA-56.2, DA-96 and DA-33 entries and was resolved by hand, because `backslop merge-changelog` normalised about sixty blank lines across the file, put a false `backslop:conflict` mark on DA-53.1's entry, opened a second `### Fixed` and wrote DA-111's entry twice; the file's diff against `3b338a9` is now the branch's own diff against `5a5f8e8`, line for line, with nothing removed. `e2e/shell.spec.ts-snapshots/shell-light-darwin.png` conflicted as binary — main's DA-56.7 ring on `Change base` and this task's `all files` tab each changed it — and was regenerated on the merged tree: 188 px apart from the branch's baseline, the ring alone, and `shell.spec.ts` 11/11 on it. `docs/reference/04-domain.md` and the `AddOptions` JSDoc keep DA-37.3's lines out of this commit; `tests/ui-context.test.ts` passes `"modified"` to `mergedPatch`, whose status argument main's DA-113 added. Of the 68 files the track touched outside the archive, the backlog and `CHANGELOG.md`, 44 are identical to `a572e79` and 19 differ by main's hunks alone; the other five are the regenerated baseline, `09-ml.md`'s opening paragraph (DA-39), `tests/ui-context.test.ts`, and `GlobalSearch.tsx` with `e2e/search-text.spec.ts` (DA-38's integration fix). Integration gates on `68c1468` — the four commits before these numbers were written in — all exit 0: `backslop lint`, `bun run lint` (217 files), `bun run typecheck`, and under `/tmp/da-perf.lock` 23:39:35–23:42:01 MSK `bun run test` (47 files, 722/722), `bun run test:bun` (722/722), `bun run test:ui` (114/114). The first series, on `f9691e9` before DA-38's fix (23:25:19–23:29:05 MSK), had `test:ui` 112/113, red on that focus defect, and `bun run perf` exit 1, measured at a load of 17.78 falling to 13.38 on 8 cores: first render 150 ms, long tasks 0, CPU per frame 8.7 ms, comment form 24.8 ms, file jump 11.5 ms, session switch 82.8 ms, update after an edit 306 ms over 300 (medians of 3). Alternated with `3b338a9` in one hold (23:35:56–23:37:24 MSK, load 10.9–13.6, three `perf/run.ts` processes a side, base / tip medians): first render 103.9 / 92.3 ms (spreads 36.9 / 13.5), session switch 68.6 / 70.9 ms, update 279 / 299 ms (spreads 7 / 34) — a tendency, filed under DA-39, not proof; the worker's alternation on the track alone had update 288 / 285. `perf` was not run again on `68c1468`: nothing under `perf/` reaches global search (`grep` exit 1), the only code that changed after the series.

**Documentation in the same pass.** `README.md`, `docs/GLOSSARY.md` (`browse mode`), `docs/reference/02-git.md`, `04-domain.md`, `07-server.md`, `08-ui.md`, `CHANGELOG.md`; the minor entries DA-37.1, DA-37.2, DA-37.4 and DA-37.5.
