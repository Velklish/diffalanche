# DA-51 · Result

**Closed 2026-09-05.** Completed. Impeccable, the design skill installed at the Claude Code user level, now reads four artifacts of this repository: `PRODUCT.md` (through the skill's `init` flow — users, purpose, positioning against the four tools of SPEC section 2, operating context, capabilities and constraints, brand commitments from the handoff, the explicit absence of customers or benchmarks, five product principles, two accessibility needs with no standard invented; the draft was confirmed by the owner before it was written), `DESIGN.md` (through `document` in scan mode: 56 colour tokens of both themes byte-identical to `src/ui/tokens.css`, typography roles, radii, spacing, 25 components, the eight canonical sections, the north star "The Quiet Control Room", the Flat Workspace Rule and the Countable-Is-Mono Rule, colour names; the Insights screen of Phase 4 left out; the owner confirmed the qualitative language), `.impeccable/design.json` (shadows, motion, focus rings, breakpoints, component snippets, the narrative — what the DESIGN.md format cannot hold), and `.impeccable/surfaces/src-ui-app-tsx.md` (the review screen in Operate mode, written through `surface-brief.mjs`, resolving from every UI component). The design detector hook is on for the repository (`.impeccable/config.json`), `AGENTS.md` requires the context loader before any `src/ui` edit and `audit` + `polish` before reporting, and `tests/design-tokens.test.ts` guards the rule that `DESIGN.md` and `tokens.css` change together.

Deviation from the task text: no harness manifest is committed. `hook-admin.mjs on` writes manifests only when the skill sits inside the project; with a user-level install it enables the config and prints "No installed provider skill folders found to repair", and the manifests it would write hardcode project-local paths. README gives the exact JSON for Claude Code, Cursor, and Codex with the user-level path as the example to adapt; `.claude/settings.local.json` is gitignored by the repository. The Cursor and Codex manifests are transcribed from the admin script's builders and not observed. Finding DA-51.1 (three glossary rows) closed at acceptance.

**Verification.** `node ~/.claude/skills/impeccable/scripts/context.mjs --target src/ui/App.tsx` prints `PRODUCT.md`, `DESIGN.md`, the surface brief (`reason: slug`; from a component, `reason: mapping`), no `NO_PRODUCT_MD`, no `MANUAL_DETECTOR_REQUIRED`. `hook-admin.mjs status` → enabled with the shared config in the repository. An Edit on `src/ui/components/Logo.tsx` in the worker's session produced `[impeccable@1] Design hook scanned src/ui/components/Logo.tsx. No deterministic design-quality issues found. …` (edit reverted). `detect.mjs --json` over `App.tsx`, `styles.css`, `tokens.css` → `[]`. Gates on the worker branch and on `main` after integration: `backslop lint` no errors; `bun run lint` 94 files; `bun run typecheck` clean; `bun run test` 207 tests (59 new); `bun run perf` green (78.2 ms / 0 / 7.5 ms / 11.3 ms / 15.6 ms). Mutation probes after commit: one hex changed in `DESIGN.md` → 2 tests fail; a colour added to `DESIGN.md` only → 2 fail; a token removed from `tokens.css` → 2 fail. Not verified: the Cursor and Codex hooks; `audit`/`polish` were out of scope. Impeccable is v4.0.4 on this machine while v4.2.0 exists; the artifacts are written to what v4.0.4 reads — the owner decides on the update, then `/impeccable doctor`.

**Documentation in the same pass.** `AGENTS.md`, `README.md` (design artifacts and the hook, two rows in the document table), `docs/reference/08-ui.md`, `docs/GLOSSARY.md`, `CHANGELOG.md`, `.gitignore`, `biome.json`.
