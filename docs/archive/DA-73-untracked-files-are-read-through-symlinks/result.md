# DA-73 · Result

**Closed 2026-09-19.** Completed. `readUntracked` used `stat`, which follows links, and read whatever was at the end of the entry `ls-files --others` named. A link to a file outside the repository put that file's content into the review as an addition; a link to `/dev/zero` reported a size of zero, passed the limit, and `readFile` never returned. `lstat` now decides what the entry *is* rather than what it points at, and a symbolic link is an addition of mode `120000` whose content is its target, read with `readlink` — exactly what git records for a tracked one, so the same link reads the same way on either side of the index. That choice has a consequence beyond the card: a dangling link and a link to a directory are recorded the same way rather than refused, so two of the three warnings `02-git.md` listed are gone and the third — an entry that vanished between the listing and the read — is the one that remains. The reader also requires a regular file before it reads, because a device reports a size of zero and passes any limit.

**Verification.** Three verdicts: a link to a file outside the repository is mode `120000` carrying the target's path, and the target's content appears nowhere in the change set; a dangling link and a link to a directory are recorded the same way, with no warning; a link to `/dev/zero` returns instead of hanging. Reverting `lstat` to `stat` reddens all three. The verdict that covered the dangling link was reworded rather than deleted, and the ENOENT branch it used to exercise now has a verdict of its own, driven by a shim whose `ls-files` prints a name nothing on disk has. One correction belongs in the record. The scope of this task was widened on the worker's report that `ls-files --others` lists a named pipe. **It does not** — the worker misread its own measurement. Re-checked twice: `ls -la` shows the pipe, `git ls-files --others --exclude-standard -z | grep -c fifo` prints `0`. The regular-file guard therefore has no reachable path and no test, which `02-git.md` says in as many words; what a verdict does record is the measured fact — a pipe in the repository, absent from the change set, no warning. A test asserting the warning would have been green from nothing. Worker gates on `fbaf0e1`, clean tree: backslop lint 0, `bun run lint` 0 (176 files), `bun run typecheck` 0, `bun run test` 0 (35 files, 469 tests), `bun run test:bun` 0 (35, 469); `bun run perf` exited 1 on two lines at load average 33.2 and was re-measured at integration. **Integration into `main`:** `1f225e3`, `ac04690` and `176b158` cherry-picked onto the DA-65 commit, plus the two describe comments of `4c4e681` that belong to this task's verdicts — the vanished entry and the entry that is not a regular file. No conflict; `CHANGELOG.md`, `tests/git.test.ts`, `src/core/git/index.ts` and `docs/reference/02-git.md` all auto-merged. Gates of the merged tree: see `docs/archive/DA-98-scan-routes-fan-out-unbounded/result.md`; after this commit `backslop lint` 0 and `bun run typecheck` 0.

**Documentation in the same pass.** The "Untracked files" section of `docs/reference/02-git.md`, rewritten around what a link is and what the one remaining warning is; a `### Fixed` entry in the CHANGELOG. That rewrite did not land with the task and was caught by review. The script carrying it asserted on an anchor whose line wrapping differed; the failure was read as the CHANGELOG hunk beside it, that was fixed by hand, and the section kept saying the opposite of the code — while the worker's result text already claimed it was rewritten. It landed in `176b158`, `DA-73: review fixes`. Recorded because the lesson is not about wrapping: a failed edit was attributed without being read.
