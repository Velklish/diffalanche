# DA-17 · Result

**Closed 2026-09-05.** Completed. The eight write routes over the domain, every write signed `author: config.user`, `role: human` from one constant nothing in a request can change: `POST /api/comments` (201; `repo` checked against the repositories under the root, unknown → 400; the anchor captured from the change set the person was shown, not re-read), `POST /api/comments/:id/replies`, `…/resolve` and `…/reopen` (a `note` written into the thread first; an empty body is `{}`), `POST /api/sessions` (created and made current), `POST /api/sessions/:name/use`, `PUT /api/sessions/:name/base` (the base string read by the domain's `parseBaseArgument`, so the CLI and the API share one grammar; the change set read again against it), `GET /api/export?status=open|all&format=md|json`. `src/server/request.ts` refuses what never reaches the domain — not a JSON object, a severity outside the four, an empty body, a wrong `side`, a body not sent as `application/json` — as 400 `invalid-request` naming the field; domain refusals keep their code through `app.onError`. The API is closed to other pages: `hono/csrf` over `/api/*` plus a 403 `forbidden` for any unsafe method carrying a foreign `Origin`, and the content-type rule. Every write drops the built document so the next `GET /api/review` is current. `refreshRepository` in core now does its read-modify-write of `diff.json` under the session lock, so the CLI's `comment` and the watcher cannot overwrite each other's patches.

Review by an isolated session: 1 major (the write API was open to cross-site requests) and 4 minor (a bodiless verdict was 400; `refreshRepository` wrote without the lock; `repo` unchecked on `POST /api/comments`; `--verbose` missing from the CLI register) — closed in `2c0d002`.

**Verification.** Gates as for DA-12; `tests/write-api.test.ts` 14: the comment signed by the configured user with the anchor read back from disk as the CLI lists it; repository- and review-level comments; reply, resolve with a note, reopen; 404 `no-such-comment`, 400 `invalid-anchor`; the request refusals; a session created, made current, switched back; the base changed and `diff.json` re-read with `base: { mode: "branch" }`; `invalid-name`, `invalid-base`, `no-such-session`; export as markdown and JSON with the status filter; a bad format; a foreign `Origin` → 403; a `text/plain` cross-site write → 403; a same-origin `text/plain` write → 400 naming `application/json`; a same-origin JSON write → 201; the bodiless verdict; and, through `startReviewServer`, a reply written by another process visible in `GET /api/review` without a restart. `tests/change-set.test.ts`: two concurrent refreshes keep both repositories' new files. Mutation probes after commit: the author constant changed → the signature and resolve tests fail; the stale cache kept after a base change → its test fails; `severity()` accepting anything → 4 tests fail; both origin guards removed → the cross-page test fails; the content-type check removed → the JSON-body test fails; the `repo` check removed → its test fails; the lock replaced by a bare call → the concurrent-refresh test loses a file. Perf not re-run: no file under `src/ui`, `perf/`, `vite.config.ts`, `scripts/synth.ts` changed.

**Documentation in the same pass.** `docs/reference/07-server.md` (routes, refusals, who may write, the anchor decision), `05-watcher.md` (both writers of `diff.json`), `06-cli.md`, `CHANGELOG.md`.
