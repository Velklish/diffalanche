# DA-29 · Result

**Closed 2026-09-05.** Completed. `skills/diffalanche-apply/SKILL.md` and `skills/diffalanche-review/SKILL.md` in the Agent Skills format (quoted `description` with EN and RU triggers, no `allowed-tools`, no `version`), each with `references/cli.md` whose command examples and JSON shapes are captured from real runs of the CLI on the small synthetic review. The apply procedure opens at `review list` (the first draft read the wrong current session silently), groups the plan by repository with buckets for repository- and review-level comments, stops at a confirmation gate before any edit, runs the repository's own checks after editing and never reports an unverified fix, replies with `--role agent` (one or two sentences when fixed, full reasoning when declined), never resolves. The review skill opens line-anchored comments through `comment` with a heredoc body. `package.json` ships `skills/` as-is (`files: ["dist", "skills"]`, the DA-1.3 decision; `npm pack --dry-run` lists the five files). README's Agent skills section leads with the clone path; the npm path is marked as available from the first release. `docs/reference/10-skills.md` records the format, the shipping decision, and what a harness must provide (a skills directory loaded by name, a shell, file edits; no hooks, no configuration).

Review by an isolated session: 1 major (a fifth `status` value that does not exist) and 7 minor (a stale `npm pack` block, `npx` given a path it lacks, an unpublished `npm install -g`, no bucket for non-file comments, plain YAML scalars with `: ` that strict parsers reject, `--body -` with nothing on stdin, a refusal quoted without its prefix) — all closed in `050b4dd`. Finding DA-29.1 (a line anchor's context mixes both sides of the diff) queued at the top.

**Verification.** The apply skill run end to end in the worker's own session on the small fixture with three unanswered human comments in two repositories, CLI from source: grouped plan, the gate answered by the orchestrator standing in for the reviewer, two edits, one reasoned decline, three replies; `list --unanswered --json` → `[]`, nothing resolved (the transcript is in the run's task files as `DA-29-apply-transcript.md`). The review skill on `repos/services/quotes-worker` opened two comments, one line-anchored (`c_qlggx7`, line 16). Gates on the worker branch at `050b4dd` and on `main` after integration: `backslop lint` no errors; `bun run lint` 115 files; `bun run typecheck` clean; `bun run test` 245 tests; `bun run perf` green (7.3 ms per frame); relative links in the eight touched files intact; both frontmatters parse with js-yaml (before the fix they failed at the `: `). Mutation probe: `skills` removed from `files` → `npm pack --dry-run | grep -c skills/` → 0, restored → 5. Not verified: the gate branch "the human changes the plan" (nobody has), an edit verified by a real toolchain (the fixture has none), and the ADR-004 github URL in both skills 404s until the branch reaches origin.

**Documentation in the same pass.** `README.md` (Agent skills), `skills/README.md`, `docs/reference/10-skills.md` (new), `docs/reference/README.md`, `CHANGELOG.md`, `package.json`.
