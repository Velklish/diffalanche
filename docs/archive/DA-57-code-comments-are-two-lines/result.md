# DA-57 · Result

**Closed 2026-09-11.** Completed. A code comment — `//`, `/* */` or JSDoc, in `.ts`, `.tsx`, `.css`, `.yml` and shell, across `src/`, `tests/`, `e2e/`, `perf/`, `scripts/` and `.github/` — is at most two lines; knowledge that does not fit moves to its `docs/reference/` section or to an ADR, and the comment that remains is the pointer. Markdown carries no limit. The decision, the count it rests on and the three alternatives weighed against it are [ADR-011](../../adr/adr-011-comment-length.md); the rule is in the "Rules for every change" list of `AGENTS.md` with a link to it; both the ADR row in `docs/README.md` and the `CHANGELOG.md` entry landed in the same pass. The repository is not in compliance — 720 blocks by the ADR's own command, 743 counting JSX and workflow comments — so the rule binds new and edited code until [DA-58](../../backlog/queue/DA-58-comment-sweep-to-two-lines.md) empties the count and turns it into a gate.

**Verification.** `npx github:Velklish/backslop#v0.4.0 lint` → exit 0, "lint: no errors" (the ADR is indexed in `docs/README.md` and every link resolves). `bun run lint` → exit 0 and `bun run typecheck` → exit 0 on the same tree. The count command in the ADR reproduces its own number: `720`. No code changed, so the suites were not the subject of this task; they were run as a baseline for the audit that produced it — `bun run test` three times and `bun run test:bun` twice, exit 0 each, 464 tests over 35 files.

**Documentation in the same pass.** `docs/adr/adr-011-comment-length.md` (new), `docs/README.md` (ADR table), `AGENTS.md` (rules list), `CHANGELOG.md` (Unreleased → Changed). No `docs/reference/` section applies: the rule governs how the repository is written rather than how the product behaves.
