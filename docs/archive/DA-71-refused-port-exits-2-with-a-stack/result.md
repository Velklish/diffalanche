# DA-71 · Result

**Closed 2026-09-18.** Completed. `serve.ts` words the two ways a listening socket is refused and then threw them as a bare `Error`. Nothing between there and the exit changes the class, and `run()` maps only `UsageError`, `DomainError` and `StorageError` to the one-line exit 1 — so the helpful sentence arrived with a stack trace under it and exit 2, the code `06-cli.md` defines as "anything the tool did not expect". A failure `serve.ts` words on purpose is not that, and the remedy is a flag the person types. The decision recorded is **exit 1**, carried by a class of the server's own: `ListenError`, declared beside `listenError()` where the two sentences are, and added to the `instanceof` list in `src/cli/run.ts` — one line, in the same list DA-66 extends. `UsageError` was the other candidate and was rejected: it lives in `src/cli`, and importing it into `src/server` inverts the layering. The fall-through stays a plain `Error` — an `EPERM` or an `EAFNOSUPPORT` is exactly what exit 2 is for, and catching the whole `startReviewServer` call would have swallowed those too. Which of the two sentences a privileged port gets turned out to depend on the runtime, Node reporting `EACCES` and Bun `EADDRINUSE`, and that is now in the reference rather than assumed.

**Verification.** `tests/cli.test.ts`, describe "a listening socket the environment refuses". The first verdict holds a port with a socket of its own and asserts the whole of stderr is exactly `diffalanche: port <n> is already in use: …` and one line, so a stack behind it would be a second line; the second asserts a privileged port gives exit 1 and one of the two worded sentences, and returns without asserting when the run is root. Mutation probe by the worker after the commit: `new ListenError(` back to `new Error(` at `src/server/serve.ts:152` and `:157` — exit 1, 28 of 30 passed, both verdicts red. `bun run test:bun` is what found the runtime difference: the second verdict first asserted the `EACCES` sentence exactly and was green under Node and red under Bun on the same machine; the fix is in the verdict and in the reference, not in the code. Worker gates on `3be7f1c`, all six green: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (481/481), `bun run test:bun` exit 0 (481/481), `bun run perf` exit 0. **Integration into `main`:** `2805eb7`, `e350e74` and the DA-71 half of the comment-fix commit `3be7f1c` cherry-picked onto the DA-64 commit. `CHANGELOG.md` merged by entry; `tests/cli.test.ts` resolved by hand — the new describe sits where the track put it, after "serve on a review that cannot be read", and the DA-64 verdict beside it was kept whole. `src/server/serve.ts` differs from the track head only by the watcher track's own three hunks (`onFallback` and the two `await watcher.close()`), which are its, not lost. Integration gates ran once on the merged tree after the last task of the track; the numbers are in `docs/archive/DA-91-piped-cli-output-crashes-on-epipe/result.md`; after this commit: `backslop lint` 0, `typecheck` 0.

**Documentation in the same pass.** `docs/reference/06-cli.md`: the exit-1 row widened to a refusal from the environment the tool worded on purpose, with the three sentences it prints and an explicit paragraph that the set is closed and everything outside it keeps exit 2. `docs/reference/07-server.md`: both sentences, the shape the CLI prints them in, and the runtime difference on a privileged port. `CHANGELOG.md`, `### Fixed`.
