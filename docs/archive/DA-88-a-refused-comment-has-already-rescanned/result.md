# DA-88 · Result

**Closed 2026-09-18.** Completed. `assertAnchorLevels` is exported from `src/core/domain/comments.ts` — with its argument type narrowed to `AnchorLevels = Pick<NewComment, "repo" | "path" | "line" | "endLine">`, so a caller does not assemble a whole comment just to check an anchor — and is called in `src/cli/commands/comment.ts` ahead of `refreshRepository`, in the same place `assertAnchorInScope` already was. A forgotten `--path` or a transposed `--line 4 --end-line 2` therefore no longer spawns the git processes for that repository and rewrites `diff.json` before the refusal: that was the one exit 1 of `comment` that broke the invariant `docs/reference/06-cli.md` states. `addComment` keeps its own check, because it has callers besides the CLI and the level rule belongs to the comment rather than to one entry point. Of the task's three candidates the middle one was taken: widening the CLI guard to `path !== null` copies the condition into two files and still leaves `--line 0` and a reversed range open. Every `assertAnchorLevels` case reachable from the command line is closed — `--line` without `--path`, a reversed range, `--line 0`, and `--end-line` without `--line`; the last of those did not rewrite anything before the fix either (the guard required `line !== null`) and stands in the test as the boundary.

**Verification.** New case `comment > refuses an anchor that is not a level, before it reads the repository again` — four sets of flags, each stamping the mtime of `diff.json` into 2020 and checking it afterwards, plus the bytes, the exit code, the refusal text and a comment count of zero. Mutation probe by the worker on `1c0b0b6` with the call removed: exit 1, 16/17, assertion `expected 1789761067271.2395 to be 1577836800000`; tree clean before and after. **The verification method the task proposed turned out to be useless and was replaced**: it asked for a byte comparison of `diff.json` before and after, the first draft of the test did exactly that, and the probe against that draft came back **green** (exit 0, 17/17) although the refusal really had rescanned and rewritten the cache — on an unchanged repository `refreshRepository` writes the same bytes back, and the fixture is in exactly that state. The test was rewritten onto mtime, which is what the reproduction in the task itself used. The neighbouring test `tests/cli-comments.test.ts:144` has the same flaw; it is about someone else's case and is green on the merits, so it was not touched — filed as [DA-88.1](../DA-88.1-byte-comparison-cannot-see-a-rewrite/task.md), in triage, with that evidence. Worker gates on `a1734c7`, clean tree: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 — 477 tests over 35 files. Integration into `main`: `1c0b0b6` and `4f1442c` cherry-picked onto the DA-101 commit, `CHANGELOG.md` merged by entry. Gates on the merged tree of the whole track, this commit's code: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (35 files, 477 tests), `bun run test:bun` exit 0 (35 files, 477 tests). `bun run perf` is measured once through the run's lock on the committed head and its numbers are in the approver's gate record, the machine carrying five parallel worker sessions at the time.

**Documentation in the same pass.** `docs/reference/06-cli.md` — the anchor levels are described as the third check ahead of re-reading the repository, with what was violated before the fix. `docs/reference/04-domain.md` — why the check is exported and why the domain keeps its own. `CHANGELOG.md`, `### Fixed`. The task's own proposal to "leave `06-cli.md:234-235` as it is and check the finished fix against it" was carried out: the promise was not weakened.
