# DA-13 · Result

**Closed 2026-09-05.** Completed. `src/cli` is a dispatcher plus one module per command, each carrying the option definitions that both `util.parseArgs` is configured from and `--help` is printed from (`spec.ts`, `args.ts`, `context.ts` with lazily memoised config and session, `output.ts`, `errors.ts`, `commands/{serve,review,diff,version}.ts`). `review new|use|list|base` over the domain (`list --json` prints the `SessionList` with storage's warnings; `new` defaults to `head`); `diff` scans the whole root against the session's base with hunks, writes `diff.json` through storage, prints the same set as a unified patch (default; `# <repo> (<branch>, against <ref>)` lines, `# <path>: binary|too-large, listed without content`; for reading, not `git apply`) or as `--json` byte-identical to `diff.json`; `--repo` narrows output only and a path no repository sits at is refused before anything is written; group prefixes print their usage. Global flags `--review`, `--data-dir`, `--root` after the command; `--root` must be an existing directory, `--data-dir` may not yet exist. Exit codes 0 / 1 (one line prefixed `diffalanche: ` on stderr for usage, domain, and storage errors) / 2 (unexpected, stack trace); JSON to stdout unmixed. `serve` gains `--open` (platform opener via `node:child_process`) and stays on the Phase 0 spike until DA-16's `startReviewServer` is on `main` — named in its `about` line and in 06-cli.md. The change-set walk lives in core (`src/core/change-set.ts`: `scanReview(config, base, options) → { cache, found }`, `totalsOf`, `refreshRepository`, `findRepositories`), and `diff.json` records the base it was computed with (`DiffCache.base`; a cache without it reads as never scanned) so a session whose base changed is rescanned rather than patched. The value lists `SEVERITIES`, `ROLES`, `SIDES`, `COMMENT_STATUSES` are exported once from `src/core/storage/types.ts`. SPEC section 3 and 7 and the glossary now name the repository id with its `roots` entry (`repos/group/service-api`).

Review by an isolated session, three rounds over the track: round one found one major (an unmatched `--repo` exited 0 with an empty review) and twelve minor; round two confirmed closure and added seven minor on DA-14 and the shared shapes; round three confirmed closure and added seven small items (structural base comparison, the two Node floors, `list --repo` against the comments, one message helper, `globalRows()`, a measured extra walk kept at 2 % of the refresh); all closed in `7b89448`, `30f7700`, `19e1dd1`, `077233f`. Finding DA-13.1 (the unit suite never runs on Bun's runtime) queued after DA-15; its `engines` half is settled here (`>= 22` for the bundle, 22.18 as the documented development floor with a skip in the tests that spawn the CLI from source).

**Verification.** Gates on the worker branch at `077233f` and on `main` after integration: `backslop lint` no errors; `bun run lint` 92 files; `bun run typecheck` clean; `bun run test` 175 tests (cli: every row of the task's verification list through `run()` on a real fixture root — `review new` with `branch:origin/main` writes the base and `current`, `list --json`, `diff --json` byte-equal to `diff.json` with hunks, `--base branch:` refused without a session directory, `--help` naming every section 8 command; plus unknown command/flag, `--repo` narrowing output not cache, `--json`+`--patch` refused, warnings on stderr in patch mode, a taken name, a directory without `review.json` warned, the directory flags, exit 2 on EISDIR). Mutation probes after commit: `--repo` writing the narrowed cache → its test fails; exit 2 for user errors → 7 fail; the `found` check dropped → its test fails; the base comparison dropped → its test fails (the first version of that test proved nothing because the fixture had no remote; rewritten to two `ref` bases); `--root` not required to exist → its test fails; a mode-only `sameBase` → its test fails. `bun run perf`: green three times and red three times on the same UI during the day, the red runs at load average 30–40 with five sessions on the machine; `git diff --stat 367e6dd..HEAD -- src/ui perf vite.config.ts scripts/synth.ts` is empty across the ten commits, so the gate is accepted on the fast gates plus that evidence (the server track filed a finding for the quiet-machine run).

**Documentation in the same pass.** `docs/reference/06-cli.md` (rewritten), `02-git.md` (the change-set section), `docs/reference/README.md`, `README.md` (Commands, the two Node floors), `docs/SPEC.md` and `docs/GLOSSARY.md` (repository id), `CHANGELOG.md`, `package.json`.
