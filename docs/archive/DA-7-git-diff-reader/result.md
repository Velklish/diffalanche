# DA-7 · Result

**Closed 2026-09-05.** Completed. `src/core/git/` replaces the spike reader: `run.ts` runs the `git` binary through `node:child_process` with global and system config at the platform's null device (helpers `revParse`, `currentBranch`, `defaultRemote`, `remoteDefaultBranch`, `mergeBase`, `diff --no-color --no-ext-diff -U3`, `untrackedFiles`); `patch.ts` splits `git diff` output into one patch per file and parses each with `gitdiff-parser` (the parser of the chosen renderer, imported directly because importing `react-diff-view` into core would pull React into the CLI bundle) into the SPEC section 7 shape — `hunks` with `header` and `lines` carrying `type`, `content`, `oldLine`, `newLine` — beside the raw `patch` the renderer reads; `index.ts` exports `resolveBase(cwd, spec)` and `readRepositoryChange(root, repoPath, spec, { maxFileBytes, hunks })`. Base resolution per mode with a warning on every fallback: `head` (no commits → `base: null`), `branch` (merge base of HEAD and `base.branch`, else the remote default branch, else `head`), `ref` (unresolved → the repository is skipped). `RepositoryChange.base` is `ResolvedBase | null` (mode the resolution ended at, ref, sha); `RepositoryChange.warnings` and `ReviewBundle.warnings` carry scanner and base warnings. Binary files, files over 512 KiB, and unreadable untracked entries (dangling symlink, directory link, deleted between listing and read) are listed with `omitted` or skipped with a warning; untracked files are additions built as the patch git would print. Files are ordered by code point (`src/core/order.ts`, shared with the scanner). The review response is read with `hunks: false`: the same diff twice in one response (structure plus string) broke the scrolling budget; `diff --json` and `diff.json` get the structure, and DA-16 must keep the switch.

Review by an isolated session, two rounds: round two found 2 major (lockfile spec drifted from package.json, so `bun install --frozen-lockfile` failed; unguarded `readFile` of an untracked entry failed the whole response), 6 minor (root-repository decision contradicted SPEC §3 decision 3 — reversed in `c54dff9`, a DA-6 follow-up integrated here; `branch` mode not distinguished from the branch tip in tests; an unmethodical number in 02-git.md; untracked too-large counts; `localeCompare`; empty untracked file) and 3 nits — fixed in `7c0da10` and `de9814a` (the untracked size check measured the generated patch again after the file had passed, dropping small files at small limits). Finding DA-7.1 (quoted and tab-padded paths mangle the file id) is taken by the same worker next.

**Verification.** Gates on the worker branch at `de9814a` and on `main` after integration: `backslop lint` no errors; `bun install --frozen-lockfile` after removing `node_modules` — 97 packages, exit 0; `bun run lint` 45 files; `bun run typecheck` clean; `bun run test` 59 tests in 8 files (13 reader cases on hand-made repositories: three modes with fallbacks, merge base asserted against `git merge-base` and not equal to the tip, staged changes, pure rename, mode-change-only file, binary, too-large tracked and untracked, dangling symlink, empty untracked file, line numbers checked against `git show <base>:file`); `bun run perf` green at load 15 (35.8 ms / 0 / 7.3 ms / 10.8 ms / 7.6 ms). Mutation probes after commit, twenty in two rounds, each failing at least one test after two input fixes (`2915758`: a pure rename without content passed; `de9814a`: the untracked size check was never reached).

**Documentation in the same pass.** `docs/reference/02-git.md` (rewritten), `01-scanner.md` (the root itself), `docs/GLOSSARY.md` (`omitted`, `resolved base`), `CHANGELOG.md`, `package.json` and `bun.lock` (`gitdiff-parser`).
