# DA-56.3 · Result

**Closed 2026-09-21.** Completed, folding in DA-55.2. `Update after an edit` had moved from comfortably inside its 300 ms budget to outside it somewhere in the DA-53…56 package, and DA-55.2 suspected the step without being able to prove it, because the two sides had never been measured beside each other. They now have been. A nine-point pass over the main branch in one quiet window on 2026-09-21, one `bun run perf` per point, one shared fixture, `DIFFALANCHE_DATA_DIR=.diffalanche` on every point, load 7.6–10.8 throughout: `1193ab3` before the package 244 ms, `2925a19` (DA-54) 283, `4be4936` (DA-53) 297, **`1079222` (DA-55) 334 — the first failure**, `a8d673b` (DA-56) 344, `c9305e1` (DA-57) 315, `fa004df` (DA-78) 323, `7308efa` (DA-90) 334, `ed81928` the tip 347. The separation is 297 against 334 with thirteen points on the two sides of it, and an earlier three-tree window agrees: `1193ab3` 244 and 267 inside the budget, `ed81928` 352 and 392 outside it. The step is real, it arrived with DA-55, and **which of DA-55's own changes carries it is not settled here** — that is DA-56.6. The same window produced two findings the card did not go looking for: `Scrolling the diff: CPU per frame` is over budget on all three trees at rest, `1193ab3` included, which makes it attributable to no task of this wave (DA-56.4), and the long-task count flips its verdict with the machine rather than the code (DA-69.1).

**Verification.** The measurement *is* the deliverable here, so what is recorded is the method and everything that went wrong with it, rather than a green gate. **Three things went wrong, all named rather than smoothed over.** The first chain of the window burned 87 seconds for nothing: `DIFFALANCHE_DATA_DIR=.diffalanche` is needed on every point and not only on the oldest, because `fixtureEnv()` arrived with DA-54.1 — later inside this range than the card implies — and seven points died with `DomainError: no current review session` at `sessions.ts:208`. The second chain lost its two oldest points to `parseDiffCache`: the shared fixture carried a `diff.json` written by the schema DA-53 introduced, and pre-DA-53 code refuses it, which is exactly where "one shared fixture removes a variable" stops being true. Those two points were re-measured in a third pass with the fixture reset to what the generator writes — `diff.json` deleted, scratch sessions deleted, `current` back on `synth` — so points 0–1 were taken on a reset fixture and points 2–8 were not. The reset does not flatter the number: `1193ab3` gave 244 reset against 286 unreset, and `updateMs` is timed after the document is built, so the first scan falls outside the window it measures. The bisection result survives all three.

**Where the prose of this task lives, and why.** The bisection paragraphs of `docs/reference/11-perf.md` landed inside `47e4934`, the commit labelled **DA-56.4**, rather than in this task's own commit `53da093`. The two share one measurement and one paragraph — DA-56.4's 9.5 ms ceiling is derived from the same nine-point pass whose table this card prints — and splitting them by hunk would have produced an intermediate commit in which a budget number stands with no evidence beside it. This was decided by the orchestrator during the run and is recorded here so nobody re-opens it.

**What this pass opened.** **DA-56.6** — the step is located but not fixed, and locating it inside DA-55 is its own work. **DA-56.4** — the CPU-per-frame line, closed in this same track. **DA-69.1** — the long-task verdict following the machine. All three are in `triage/`; routing them is the orchestrator's move.

**The gates of the merged track.** The full gate list ran once on the tree the seven tasks make — `d76d804`, tree clean, `gates 7, green 6`, with the one red being `bun run perf` refusing to erase a pre-stamp fixture rather than returning a budget verdict. The commands, their exit codes and counts, the fifteen per-commit typechecks and how the track was integrated into `main` are in [the result of DA-63](../DA-63-perf-gate-erases-fixture/result.md); this task points there rather than repeating them.

**Documentation in the same pass.** `docs/reference/11-perf.md` — the nine-point table, the method and its three failures; a `### Fixed` entry in `CHANGELOG.md` recording the measurement and what it attributes.
