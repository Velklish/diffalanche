# DA-100.1 · Result

**Closed 2026-09-23.** Completed. When the opener cannot take the ring, the restore hands it to the header's control of the same ladder — the `BASE` pill, the `SCOPE` pill, search, `Export`, the element `ladderHome()` marks with `data-ladder` — which reopens the same overlay and stays on screen while the review does; `<body>` is not a fallback, because it is where the next `Tab` starts from the top. "Took it" is judged by calling `focus()` and then reading `document.activeElement`, not by `isConnected`, so a detached opener and a disabled one are one case, as the card asked. When neither the opener nor the header's control takes the ring — a whole-root session has no `SCOPE` pill — the restore writes `console.warn("the <ladder> overlay closed and no control could take the focus back")` instead of doing nothing. The card's candidate path was reproduced in Chromium first, with capturing `focusin`/`focusout`, a `MutationObserver` on `disabled` and a `fetch` wrapper in a throwaway spec: select mode → `New task…` → `Create` restored the ring onto `New task…` at +752 ms, the switched review arrived at +1605, `disabled` was set at +1612 and the ring left at +1628 for `<body>`; `Change base` → `Apply` on the no-changes screen restored onto `Change base` at +550 and lost it at +618 when the screen unmounted. The card's hypothesis — the restore runs after the answer — is ruled out: the ring is lost *after* a restore that worked, which became [DA-100.2](../../backlog/triage/DA-100.2-ring-lost-after-the-restore.md) and was fixed in this run.

**Verification.** Playwright, `e2e/scope.spec.ts`: "an opener that has gone hands the ring to the SCOPE pill", "an opener that was disabled hands the ring to the SCOPE pill", "a restore with nowhere to go says so instead of doing nothing". Mutation probes by the worker on `68b4049`, `git status --porcelain` empty before and after: `Overlay.tsx:88` back to `isConnected` + `focus()` reddened exactly the three new cases (exit 1, 3 failed, 8 passed of 11); `takes()` returning `element.isConnected` reddened "was disabled" and "nowhere to go" and left "has gone" green, so the two conditions are tested apart. Verdict names base → batch head `1cef835`: `vitest list` 562 → 566, Playwright `--list` 95 → 98, none removed. Worker gates on `1cef835`, the head of this task's batch: `backslop lint` exit 0 (0 errors, 1 warning — DA-100.2 in triage), `bun run lint` exit 0 (190 files), `bun run typecheck` exit 0, and under `/tmp/da-perf.lock` `bun run test` exit 0 (40 files, 629/629, load 27.9), `bun run test:bun` exit 0 (629/629, load 31.1), `bun run test:ui` exit 0 (98 passed); `bun run perf` declined by its load guard at 6.54 per core. The first locked runs on `1cef835` at load 102 and 99.8 exited 1 (2 failed + a hook; 7 timeouts); each of the 11 red names was run alone twice on `1cef835` and twice on `5a5f8e8`, 44 runs, all exit 0. Worker gates on the track tip `927d582`: `backslop lint` 0, `bun run lint` 0, `bun run typecheck` 0, `bun run test` 0 (41 files, 637/637), `bun run test:bun` 0 (637/637), `bun run test:ui` 0 (104 passed), `bun run perf` declined at 2.69 per core. Review: one isolated pass over `5a5f8e8..227bdf0` for the whole track, critical 0, major 2, minor 3, plus two legacy observations, all closed on the branch; this task's share was the reason the two new cases break the opener by hand — now that `Escape` starts no switch — written in `08-ui.md` by `a17a83b`. **Integration into `main`:** `68b4049` and `db35d65` cherry-picked onto `6f1f85b` as one commit, the first of the track; `CHANGELOG.md` conflicted with the core track's three entries at the head of `### Fixed` and was merged in the order `backslop merge-changelog` gives, both sides kept, the other files applied without a conflict. The integration gates ran once on the merged tree after the last task of the track; the numbers are in the `result.md` of the track's last task, `focus-ring-below-non-text-contrast` in the archive.

**Documentation in the same pass.** `docs/reference/08-ui.md` › the overlay's focus restore (the fallback, how "took it" is judged, the warning, and what DA-100.2 then owned), and the UI tests paragraph on the three new cases; `CHANGELOG.md`, `### Fixed`.
