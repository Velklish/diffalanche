# DA-36.2 · Result

**Closed 2026-09-24.** Completed. A severity the model chose and nobody confirmed no longer votes in later suggestions. Each entry of the embedding index carries the comment's `severitySource`, taken from `comments.json` whenever the session is read again, so an agent's `reply --confirm-severity` reaches the index at its next update without an embedding. The vote lists an `auto` neighbour among the five but leaves it out: the 0.86 line and the weights start at the nearest neighbour that votes, and five `auto` neighbours propose nothing. `GET /api/suggest` and `suggest --json` return `severitySource` with each suggestion — hence two lines in `src/ui/types.ts`, the wire type, and one in `tests/ui-composer.test.ts`; nothing on the page renders differently. One `isSeveritySource` in `src/core/storage/types.ts` serves storage and the index. An index written before the change reads each entry as `manual` and drops its sessions' fingerprints, so the next update reads `comments.json` again, takes every source from it and embeds nothing; the index version stays 2. The finding [DA-36.3](../../backlog/minor/DA-36.3-suggestion-rows-hide-model-severity.md) stays in `minor/` with its Scope filled: a suggestion row does not say its severity was the model's, and `TAB` stores it as `manual`, which votes — a product decision, the owner's. Nothing was pushed. Decisions this acceptance records:

- **An unconfirmed `auto` does not vote** — the orchestrator's ruling during the night: `manual` and `confirmed:<author>` vote as before. A history written with `AUTO` would otherwise confirm itself, and no weight for the model's label has been measured. Written in [09-ml.md](../../reference/09-ml.md) as the orchestrator's decision.
- **The one path where an old `auto` still votes as `manual` is accepted** — the orchestrator's ruling during the night: an index written before the change, beside a session whose `comments.json` cannot be read, keeps that session's entries `manual` until the file is repaired. It is the behaviour before the change, written in 09-ml and tested.

**Verification.** Worker records of run 0923, each on a clean detached gate worktree under the lock: `gates-ml4.json` on `2247345`, before the review fixes — gates 7, green 6 (`test` 859/859, `test:bun` 859/859, `test:ui` 121/121, `perf` update 300 ms: 300/314/281); `gates-ml4-2.json` on `4d41f5c`, after `backslop init` — gates 7, green 5 (`test` 861/861, `test:bun` 861/861, `test:ui` 121, `lint` 0 on 251 files, `typecheck` 0; red: `backslop lint` on DA-99.3's `stat` quote only, which this acceptance turned into `quote:before:`, and `perf` "Update after an edit in one repository" 319 ms: 290/321/319). That line was measured alternately against `29e1872`, three `bun run perf` a side with the load beside each (DA-110): base medians 293 / 300 / 290 ms, branch 286 / 315 / 279 ms — no difference at this resolution, and the task does not touch the update path. Seven mutation probes on `4d41f5c` over five suites (94 verdicts), tree clean before and after each, every one exit 1: every neighbour votes → 92 passed; the source always `manual` → 90; an older index's fingerprints kept → 92; `best` taken over every neighbour → 93; `severitySource` cut from the route's answer → 93, the `GET /api/suggest` verdict red; `confirmed:` without an author accepted → 92; an older entry read as `auto` → 92. Verdict names `29e1872` → `4d41f5c`: none removed, eight added, five of them this task's. Review: one isolated pass over `29e1872..bfddbc9` — critical 0, major 1 (`severitySource` claimed on both doors and tested on neither), minor 5 — all closed in `066843b` and `4d41f5c`. Evidence: `evidence-ml4.md`.

**Integration into `main`.** `8a5d67a 2247345 066843b` cherry-picked without commit onto `29e1872` and squashed into one commit; `main` had not moved from the branch point. No conflicts: git merged `03-storage.md`, `09-ml.md` and `tests/embedding-index.test.ts` between the branch's own commits. One change is this acceptance's: `commentsPath` joins the storage import of `tests/embedding-index.test.ts`. On the branch DA-99.3's `bfddbc9` brought it in before the review fix `066843b` used it, so the commit taken by task alone did not typecheck (TS2304 three times); DA-99.3's commit then takes the branch's import whole. `CHANGELOG.md` checked by hand — the entry once, under Unreleased / Changed, the layout kept; nothing corrected.

**Documentation in the same pass.** `docs/SPEC.md` section 5, [03-storage.md](../../reference/03-storage.md), [06-cli.md](../../reference/06-cli.md), [07-server.md](../../reference/07-server.md), [09-ml.md](../../reference/09-ml.md), `README.md`, `CHANGELOG.md`.
