# DA-26 · Result

**Closed 2026-09-05.** Completed. `src/ui/keys.ts` is the handoff's table in one listener over actions of the store: `⌘K` / `Ctrl+K` and `⇧⇧` (two presses inside 400 ms, outside a field) open global search; `J` / `K` walk the open threads of the whole review in the order the centre panel lists them, wrapping at both ends, and focusing a thread makes its file current through the same `revealThread` a click in the rail uses (moved into `src/ui/reveal.ts` for that); `C` opens the composer on the first added line of the current file; `R` resolves the focused thread; `B` says browsing is Phase 2 (DA-37) in a toast; `⌘⏎` sends; `esc` closes the topmost open thing in one order — search, the header's menus, the picker and the export, a reply field, the composer — and stops at the first, so one `esc` over a modal never discards a comment under it. A letter in an `input` or `textarea` is text, with `⌘⏎`, `esc` and `⌘K` as the exceptions and `⇧⇧` deliberately not one. Three handoff rows are named as not wired with their reason (`↑`/`↓`/`TAB` in the composer — Phase 2 suggestions, DA-35; `⏎` in the base picker — the picker's own field). Global search is handoff section 6: ranking in `src/ui/search.ts` (substring with a boundary bonus, then word overlap, ties by path), a twelve-line preview around the target with deletions kept, `⏎` opening a file at its card and a comment at its thread; two of the handoff's five tags are drawn, the rest named as Phase 2 or not carried by the on-disk format. The header's `⌕ ⌘K` button was still disabled and now opens the modal. Finding DA-26.1 (no overlay contained the tab ring) was fixed on the same branch in the `Overlay` primitive.

**Verification.** `tests/ui-search.test.ts` (16), `e2e/keyboard.spec.ts` (8, every row of the map on the fixture, with `diffalanche list` reading `R` back and `⇧⇧` in the composer staying text). Mutation probes after commit: the boundary bonus removed → 2 fail; `J`/`K` clamping instead of wrapping → 1; `R` on a closed thread → 1. The first probe went green until a third fixture file (`src/restore.ts`, which sorts before `src/store.ts` by path) made the bonus the only thing ordering the result — committed separately as `5054047`. Gates as in DA-25.

**Documentation in the same pass.** `docs/reference/08-ui.md` (the keyboard table, search), `.impeccable/surfaces/src-ui-app-tsx.md` (the keyboard walk done; containment left to DA-26.1), `CHANGELOG.md`; README's keyboard map added by the approver at integration.
