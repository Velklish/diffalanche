# DA-104 · Result

**Closed 2026-09-18.** Completed. The only guard on "the server listens on `127.0.0.1` and nowhere else" reached the server from another of the machine's own addresses, and where the machine had none it returned instead of skipping. What was left was that loopback answers, which every other verdict in the file already establishes, and the run said nothing about having checked less — so a container whose network is deliberately constrained, the one environment where the check is worth most, is where it quietly stopped being a check. Both halves of the decision the entry asked for were taken rather than one. The skip is reported, in the style of `tests/cli-comments.test.ts`, with a sentence naming what was not exercised and where the machine-independent check is; and `startServer` now answers with the hostname the socket is bound to — `server.hostname` under Bun, `info.address` under Node — with `tests/runtime.test.ts` asserting it is `127.0.0.1` without being told to. The entry's third candidate, reading the default out of the source, tests the text rather than the socket; reading it back from the runtime costs the same and tests the socket. A skip alone would have left a loopback-only host with no guard at all. The scan the entry asked for found no other instance of the shape: the four other bare `return`s under `tests/` are loop exits in `watcher.test.ts`'s wait helpers and an early return in `config.test.ts`'s `gitIdentity`, none of which skip the point of a test, and the two in `tests/cli.test.ts` were this run's own and went with the DA-99 review fixes.

**Verification.** Mutation probe by the worker on the submitted sha, `src/server/runtime.ts:27` changed to `0.0.0.0` — exit 1, 22 of 24 passed, both the machine-independent verdict and the off-loopback one red. Second probe, the machine the entry is about: the interface finder forced to `.find(() => false)` — the run reports `Tests 21 passed | 1 skipped (22)`, so the skip is in the report and not counted as a pass. Worker gates on `18ebfd5`: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (491/491, 35 files), `bun run test:bun` exit 0 (491/491); `bun run perf` exit 1 there, two lines over by 0.1 ms and 5 ms at a load average of 13.6, not attributed, and green again on the track's final `75c7bc1` with every line inside budget. **Integration into `main`:** `f788ffa` cherry-picked onto the DA-99 commit; `CHANGELOG.md` merged by entry, everything else auto-merged with no conflict, and `src/server/runtime.ts` and `tests/runtime.test.ts` are identical to the track head afterwards. Integration gates ran once on the merged tree after the last task of the track; the numbers are in `docs/archive/DA-91-piped-cli-output-crashes-on-epipe/result.md`; after this commit: `backslop lint` 0, `typecheck` 0.

**Documentation in the same pass.** `docs/reference/07-server.md`, "The runtime switch": `startServer` answers with the port and the bound hostname, and why that is what makes the claim checkable on any machine. `CHANGELOG.md`, `### Changed`.
