# DA-31 · Result

**Closed 2026-09-05.** Completed, without a push: nothing was tagged, released or published in this run, by the owner's rule that the first release happens when the backlog is empty. `.github/workflows/release.yml` runs on a `v*` tag with `contents: write` and `id-token: write` on the job and `contents: read` at the top: the version comes from the tag and is checked against `package.json` (a `-` in it means the `next` dist-tag and a pre-release), the release notes are that version's own `## [x.y.z]` section of `CHANGELOG.md` and an empty section stops the release before anything is published, one job builds both channels (`bun build --compile` cross-compiles, so a six-cell matrix would rebuild the same UI six times to emit one file each), `SHA256SUMS.txt` is written and checked, the GitHub release is created with the six binaries and the checksums, and `npm publish --provenance --access public --tag <dist-tag>` runs last, because a release can be made again and a published version cannot be taken back. `.github/workflows/ci.yml` gains a `concurrency` group that cancels a superseded run on a pull request and never on `main`, and a comment naming the required set — `check`, `test-bun`, `smoke`, `perf`, `e2e` — as the branch protection rule, which is a GitHub setting and the owner's step, like the `NPM_TOKEN` secret. `scripts/release.ts` is `bun run release <version> [-- --dry-run]`: the version in `package.json`, a clean tree with untracked files counted, a free tag, the changelog section present with Unreleased still above it, the unit suite green, then `git tag -a`; it refuses to move Unreleased itself (that edit would dirty the tree it just checked and the tag would point behind it) and never pushes. `package.json` `files` gains `"!dist/diffalanche-*"`, so the npm tarball carries no binary — the one step past the task's boundary, accepted on the bus.

Review by an isolated session, two rounds: 2 major (the required-checks note named job ids where branch protection matches check-run names — `unit suite on Bun`, one check per `smoke` cell — and a name that never reports blocks every pull request; the shared `concurrency` group cancelling a pending `main` run) and 6 minor (a release step that could not be re-run after a failed publish; a preflight accepting an empty changelog section; no check that the tag is on `main`; two doc counts; `sha256sum -c` verifying nothing until it also counts six lines) — closed in `6c0cec3`, each workflow step re-executed after the change. Finding DA-31.1 (one unit test file flaky under load 18–31) taken by the orchestrator.

**Verification.** Gates on the branch: backslop lint, lint, typecheck, test 310, test:bun 310; the perf gate is not owed (nothing under `src/ui`, `perf/`, `vite.config.ts`, `scripts/synth.ts` in the diff) and hung twice on the loaded machine (DA-25.2's evidence). The workflow `run:` blocks were lifted with a YAML parser and executed as written: tag and version agreeing and disagreeing, `v0.0.0-rc.1` giving `next` and `--prerelease`, the changelog extraction lifting exactly one section for a stable and for an rc, `$PRERELEASE` unquoted under `set -eu` expanding to no argument or one, `bun run build` producing `dist/cli.js` and six binaries (62–99 MiB each), the checksum step writing six lines and verifying them, `npm pack --dry-run` listing 17 files and no binary. `scripts/release.ts` end to end in a throwaway clone: dry run, the real annotated tag, and every refusal fired — no argument, not a version, a version `package.json` does not declare, a tracked edit, an untracked file, a tag that exists, a failing suite; the clone deleted, no tag in this repository.

**Documentation in the same pass.** `README.md` ("Releases"; the Install cross-link), `docs/reference/11-perf.md` ("The release"), `docs/reference/06-cli.md` (how each channel is published), `docs/reference/README.md`, `scripts/README.md`, `CHANGELOG.md` (the conventions paragraph and the entry).
