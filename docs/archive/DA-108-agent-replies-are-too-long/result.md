# DA-108 · Result

**Closed 2026-09-18.** Completed. The reply rules of `skills/diffalanche-apply/SKILL.md` are now a cap: a reply is at most three sentences — one when the finding is fixed (a second only when the fix touched something the comment did not name), three when it is declined (what stands, why, what would change the answer) — with no restating of the comment, no greeting, no closing offer, no headings and no lists, and the check that was run as a clause. The skill shows a paragraph-long reply beside its two-sentence form and a three-sentence decline; the reply example of step 6 and both examples in `references/cli.md` have the same shape. The contract moved with it: `docs/SPEC.md` sections 3 and 9 and `docs/reference/10-skills.md` say the cap. The old wording is gone: `grep -rn "one or two sentences" docs/SPEC.md docs/reference skills README.md` → exit 1.

**Verification.** A dry run by a fresh session given only the skill and two comments (a fixed warning, a declined nit) produced a two-sentence reply for the fix (the second sentence names the test the comment did not ask for) and a three-sentence decline. Gates on `main` after the edit: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0, `bun run typecheck` exit 0, `bun run test` exit 0 (35 files, 464 tests), `bun run test:bun` exit 0 (464 tests). `bun run perf` ran through the run's lock while five worker sessions were building and testing on the same 8-core machine and came out red — CPU per frame 8.8 ms against 8.3, update 573 ms against 300 — with the tree differing from `main` in no file under `src/`, `perf/`, `e2e/`, `tests/` or `scripts/` (`git diff HEAD -- src perf e2e tests scripts` is empty); the quiet-machine baseline of the same code an hour earlier was 7.9 ms and 298 ms. The number is the machine's, not the change's, and the perf gate is re-measured on the merged tree at each wave's integration. A text-only change, so no mutation probe applies. Reviewed by the orchestrator (text only, per the `backslop-batch` review table).

**Documentation in the same pass.** `docs/SPEC.md` (sections 3 and 9), `docs/reference/10-skills.md`, `CHANGELOG.md`. The copies of the skill outside the repository (`skills.local/` in the owner's workspace) were refreshed by hand from `skills/`.
