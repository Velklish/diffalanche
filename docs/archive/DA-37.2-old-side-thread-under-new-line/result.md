# DA-37.2 · Result

**Closed 2026-09-24.** Completed. A thread on the old side sits under the deleted line it names. The file card grouped threads by line number alone and the renderer keyed its rows by the new side's number, so a comment written with `comment --side old` on a deleted line sat under the new-side line of the same number, or nowhere. A row now names its side: a deleted line is an `old` row, every other row is `new`, and a thread on the old side of a context line sits where one on its new side would. The widget of a deleted line with no insertion beside it is in the old side's half, put across the diff as a new side's strip is, and its bar is on the old gutter (`on-old`); a split row that pairs a deletion with an insertion always carries one node both sides share, the old side's slot first, so a thread arriving on the second side mounts beside the first instead of remounting it; an added file's two-cell split row draws its bar on the first cell. The card's `FileCard.tsx` quote guarded exactly the grouping this changed, so it became a `quote:before:` snapshot at this acceptance; the `ReactDiffFile.tsx` quote still matches.

**Verification.** Worker records of run 0923, each on a clean detached gate worktree under the lock: `gates-ui2.json` on `3b952a5` (batch 1) — gates 7, green 6 (`test` 845/845, `test:bun` 845/845, `test:ui` 127/127, `perf` update 277/290/278 ms; red: `backslop lint` on this card's quote and DA-102.1's); `gates-ui2-2.json` on `da3fba0`, the whole track — gates 7, green 6 (`test` 853/853, `test:bun` 853/853, `test:ui` 151/151, `lint` 0, `typecheck` 0, `perf` update 299/304/298 ms; red: `backslop lint`, five errors, none from code — three card quotes this acceptance turned into `quote:before:` and two mentions of DA-55.8, whose card is on `main`). Before the fix `e2e/threads.spec.ts -g "old side"` was red: the old thread's widget in the insertion's cell of the row pairing D6/I6. Mutation probes, tree clean before and after each, each exit 1: `const line = end;` for the old side's row at `5d9c523` → `threads.spec.ts` 11 of 12, red "a thread on the old side sits under the deleted line it names"; the split merge turned off at `a069e85` → 15 of 16, red "a split pair with threads on both sides is one strip, the old side's first"; the added file's selector removed → red "the bar of a thread on an added file is on its only gutter". Review: two isolated passes over the track — critical 0 in both; this task's notes closed in `a069e85` and `380410b`. Evidence: `evidence-ui2-b1.md`, `evidence-ui2-final.md`.

**Integration into `main`.** `5d9c523 a069e85 380410b` cherry-picked without commit onto DA-56.8's commit and squashed into one commit. Git merged `08-ui.md` and `styles.css` with no conflict; `CHANGELOG.md` conflicted in `5d9c523` and was resolved by hand: `main`'s file, and the branch's final entry once, at the top of Unreleased / Fixed, in the branch's layout.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (a row names its side, the bar by side, one node for a split pair, what `threads.spec.ts` reads), `CHANGELOG.md`, `### Fixed`.
