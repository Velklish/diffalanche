# DA-22.1 · Result

**Closed 2026-09-05.** Completed, wider than filed. `--tx3` → `#8f97a2` dark / `#656158` light, and `--ln` → `#848c97` / `#767165`: writing the verification the entry asked for — every token pair the interface uses for text — found the diff gutter at 3.38:1 dark and 2.87:1 light, worse than the pair the finding named, and a line number is what a reviewer copies back into a comment. `tests/design-contrast.test.ts` models where each colour actually sits (`--code` and `--ln` only on a file card; the two translucent washes flattened over the card before the ratio) rather than crossing every colour with every ground; worst pairs now `ln on panel` 4.58 / 4.66 and `tx3 on panel3` 4.70 / 4.82, everything else above 5. Motion: `--dcin-shift` is a token that `prefers-reduced-motion: reduce` takes to `0px`, so an arrival keeps its fade and loses its travel without a second keyframe; `dcpulse` stops with the dot lit. The override lives in `styles.css` beside the rule it overrides — placed in `tokens.css` first, it read well and did nothing, since `styles.css` is imported later with the same specificity; the Playwright check on computed `animation-name` is what said so. The resolved thread steps back by tone (`panel2`, `bd2`, `tx2`) instead of `opacity: 0.55`, which had taken its own body text to about 4.4:1. `tokens.css`, `DESIGN.md` and `.impeccable/design.json` changed in one pass, `tests/design-tokens.test.ts` green.

**Verification.** `tests/design-contrast.test.ts`, `tests/design-tokens.test.ts`, the reduced-motion Playwright check, `bun run test:ui` 57. The shell screenshot baselines did not move: Playwright's comparator ignores per-pixel differences of this size, so the contrast test is the only guard of these values — kept as a gate for that reason (`db294e3` corrects `5666a26`'s message, which claimed a regeneration).

**Documentation in the same pass.** `DESIGN.md`, `docs/reference/08-ui.md`, `CHANGELOG.md`.
