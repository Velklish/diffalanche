# DA-98 · Result

**Closed 2026-09-19.** Completed. `scanReview` and the server's `summarise` and `candidatesOf` each mapped over every repository under the root with an unbounded `Promise.all`, so the number of git processes in flight was decided by how many repositories the folder happened to hold. `mapWithLimit(items, limit, run)` in `src/core/change-set.ts` keeps at most `limit` in flight and returns results in the order the items were given; all three call sites go through it with `SCAN_CONCURRENCY`. The bound is 8, and it was measured rather than picked — the synthetic review, its twenty-one repositories and the sibling worktree the walk counts beside them, twenty-two reads, medians of three runs per width: 802 ms at width 1, 404 at 2, 322 at 4, 261 at 6, 270 at 8, 279 at 12, 279 at 16, and 275 unbounded. Past six it buys nothing and unbounded is no faster than eight, so eight is full speed with a ceiling. A fixed number rather than `availableParallelism` because the performance budgets are held against one machine, and a machine-dependent width would make them mean different things on different ones. **The first-run screen.** `loadScan` dropped a refusal with a bare `return`, so a failing `GET /api/scan` left the three metrics as dashes — which is exactly what they show before anything has been asked. The store keeps `scanFailure`, the screen carries the server's own sentence under the metrics with a retry beside it, and the effect does not ask again on its own after a refusal. A dash means "not asked", which is the only thing it ever meant. The line reuses the warnings bar's idiom — the `SCAN` tag in amber, one quiet mono line — rather than introducing a shape of its own, and rests on product principle 5 rather than on taste.

**Verification.** The bound is asserted in **calls**, in `tests/change-set.test.ts`: forty items through a pool of four, counting what is in flight, with nothing timed. Widening the pool past its limit reddens it every time. Result order and the degenerate widths — a pool wider than the work, and width one — are asserted beside it. `tests/scope-scan.test.ts` asserts the peak in **processes**, off a shim that records each one starting and finishing, and it drives **all three call sites** — the card asks for exactly that, and before this it drove only `scanReview`, so reverting either server site to a bare `Promise.all` passed every gate. The shim also pauses before handing over to the real git: without that the peak is a race the machine decides, and under load an unbounded fan-out never holds enough processes open to be seen. Removing the cap from each site in turn reddens its own case — 52, 61 and 63 against 32. Two vacuous checks were found and fixed on the way here, both worth recording because both had already been reported as working. The first ceiling was `SCAN_CONCURRENCY * 5` and did not redden at all; the peaks were measured explicitly (23 against 50) and it was tightened to `* 4`. The tightened one then reddened when run alone on a quiet machine and **not** in a full suite under load — verified in both directions, 482/482 with the cap removed — which is what moved the guarantee into the call-level verdict above. Two verdicts in `tests/ui-empty.test.ts` cover the first-run screen: the refusal is kept and named, and a later successful scan clears it; restoring the bare `return` reddens the first.

**The gates of the merged git track.** All eight tasks of the track are in `main`, and the full set ran once on the tree they make, by `approver:git`. Five of the six ran on the committed tree of the eight commits, nothing modified: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, no errors; `bun run lint` exit 0, 180 files checked; `bun run typecheck` exit 0; `bun run test` exit 0, 35 files and 554 tests passed; `bun run test:bun` exit 0, 35 files and 554 tests passed. `bun run perf` ran once, through the run's serialising lock, on the same content a moment before the last commit was made — the code, the tests and the perf harness were already final and the only later change is the prose of this file — exit 0, every line inside its budget — first render 94.2 ms of 500, scroll long tasks 0 of 0, CPU per frame 7.8 ms of 8.3, comment form 24.1 ms of 50, file jump 13 ms of 50, session switch 90.5 ms of 100, update after an edit 291 ms of 300 — at a one-minute load average of 26.5 entering the run and 15.5 leaving it. The other seven tasks of the track point here for these numbers. Each task's own commit was gated on its own tree with `backslop lint` and `bun run typecheck`, both exit 0, nine runs of each across the eight commits.

**Integration into `main`.** `85cdea0`, `866aecb`, `93f1efa` and `22ff5bd` cherry-picked onto the DA-95 commit, plus the four parts of `4c4e681` that belong to this task — `src/core/change-set.ts`, `tests/change-set.test.ts`, `tests/scope-scan.test.ts` and the curve's wording in `docs/reference/02-git.md`; that commit's other hunks went to DA-76, DA-65 and DA-73 and are named there. One conflict, in `src/server/review.ts`, and it is a shared-file conflict rather than a semantic one: this task rewrites the `../core/change-set.ts` import list and the server track had added `node:path` above it (DA-72). Both are in, and `src/server/review.ts` is byte-identical to the branch head afterwards. After the whole track: `src/core/git/`, `src/core/index.ts`, `src/core/change-set.ts`, `tests/git.test.ts`, `tests/scope-scan.test.ts`, `tests/change-set.test.ts`, `docs/reference/02-git.md`, `docs/adr/adr-012-git-trust-model.md`, `src/ui/store.ts`, `src/ui/components/FirstRun.tsx`, `src/ui/styles.css`, `docs/reference/08-ui.md`, `tests/ui-empty.test.ts`, `src/cli/run.ts`, `src/server/review.ts`, `docs/README.md` and `CHANGELOG.md` are all byte-identical to the worker's head `22ff5bd`; the only difference in the whole tree is one link in `docs/reference/07-server.md`, rewritten by `backslop archive` when DA-73 left `docs/backlog/active/`.

**The Impeccable pass, in full.** `AGENTS.md` asks for the context loader, then `audit` and then `polish` on the touched surface, and for this result to name what the pass did not fix. *Audit*, over `src/ui/components/FirstRun.tsx` and the `.first-run-refused` block of `src/ui/styles.css`: accessibility 4 — contrast measured in both themes, the line 6.11 dark / 7.24 light, the `SCAN` tag 7.33 / 5.39, all above AA and most above AAA, `role="status"` announces the refusal, and the retry is a real button with the stylesheet's own `:focus-visible` ring; performance 4 — no animation, no layout read/write, one fetch per press; theming 4 — every value a token, `--tx2`, `--mono`, `.tag.scan` in `--warn`, no hard-coded colour; responsive 3 — `flex-wrap` on the row, and see the unfixed item below; implementation integrity 4 — the detector reports `[]`, and the shape is the warnings bar's, which `DESIGN.md` already assigns to scanner refusals, rather than a new one. *Polish* fixed two things it found, both in what this task added: a long refusal could overflow the panel, because the server's sentence carries paths, the panel is a fixed 560 px and an unbroken path has nowhere to break — `overflow-wrap: anywhere`; and the retry had no in-flight state and could be pressed again before it answered, firing a second fetch — it is disabled while the request is in flight, which the stylesheet already dresses (`.ghost:disabled`). **What the pass did not fix**, named rather than left implied: `.first-run-panel` is `width: 560px` and not `max-width`, a fixed width that would break below that viewport — pre-existing, belonging to the whole screen rather than to this change, and `polish` is refinement and not a redesign smuggled in, so it was left, and it is worth a task of its own; the retry is 22 px tall, under the 44 px touch guidance, which is `.ghost.small`, the size the warnings bar's own `dismiss` uses, on a product served on `127.0.0.1` to one developer at a desktop, where changing it here alone would break the consistency it was chosen for; and the reduced-motion guard and the `:focus-visible` treatment are the stylesheet's, pre-existing and correct for this surface, so nothing was added to either.

**Documentation in the same pass.** The change-set section of `docs/reference/02-git.md`, with the helper, the number, the measured curve and what the assertion is on; the first-run paragraph of `docs/reference/08-ui.md`; a `### Fixed` entry in the CHANGELOG.
