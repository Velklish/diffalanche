# DA-12 · Result

**Closed 2026-09-05.** Completed. `src/core/watcher/`: `watchTree` (recursive `fs.watch` where the runtime recurses — decided once per process by a probe that writes inside the data directory, never a reviewed repository — else a 250 ms walk; an `error` on the native watcher falls back to the walk), `startWatcher({ config, scan, bus, activity, … })` with one watch per repository plus one over the data directory, ~100 ms debounce per repository with a 1 s ceiling, rescans serialised in one queue, a repository's entry replaced in `diff.json` under the session lock (a missing cache means a full `scanReview`, never a partial cache; an unchanged entry emits nothing; the cache keeps its `base`), the data directory diffed into comment events with the author from the file, `session-changed` only when name, title, or base change, a reporter whose own throw cannot poison the queue; `createEventBus()` with the typed union `diff-changed`, `comment-added`, `reply-added`, `comment-status`, `session-changed`, `warnings`; the activity ring buffer (200 lines: `changed`, `editing`, `replied`, `commented`, agent writes attributed to the repository's diff changes for two minutes). Ignores: `.git` internals except `HEAD` and `index`, `node_modules`, the `exclude` globs (through `globToRegExp`, now exported from the scanner), and the data directory itself. The "update after an edit" unit test asserts the median of three edits against 300 ms plus one rescan timed in the same conditions; the flat number is DA-25's perf-gate line. Finding DA-12.1 (gitignored build outputs still wake the watcher; `git check-ignore` per burst named as the cost) queued.

Review by an isolated session, two rounds: round one found 4 major (partial cache without `diff.json`; no `error` listener; Bun recursion unverified; a throwing `onError` poisoning the queue) and 5 minor; all closed in `4962c11` and `a0083b4` (the probe memoised; a broken `comments.json` no longer fatal to the watcher's baseline).

**Verification.** Gates on the worker branch at `2c0d002` and on `main` after integration: `backslop lint` no errors; `bun run lint` 125 files; `bun run typecheck` three projects clean; `bun run test` 283 tests in 20 files (`tests/watcher.test.ts`: 16 — the edited repository alone rescanned, `.git/objects` ignored, the session pointer followed, replies diffed, the same bytes emitting nothing, a failing rescan reported and not blocking the next, `editing` attributed to the agent that wrote recently, the budget test). Mutation probes after commit: naive `.git` ignore → 3 tests fail; every repository rescanned → 1; replies not diffed → 1; `sameChange` forced false → 2; partial cache → 1; metadata comparison removed → 1; the try/catch around `onError` removed → the test times out. Perf gate green on the DA-12 commit (29.4 ms / 0 / 7.0 ms / 10.8 ms / 7.4 ms); later runs on this branch and on `main` are DA-16.1's subject.

**Documentation in the same pass.** `docs/reference/05-watcher.md` (new), `01-scanner.md` (the export and the file-level globs), `docs/reference/README.md`, `CHANGELOG.md`.
