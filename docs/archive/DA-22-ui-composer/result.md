# DA-22 · Result

**Closed 2026-09-05.** Completed. Selection and the composer: the store holds `sel { repo, path, side, a, b }` (the file identity added to the handoff's `{ a, b }`, without which a range cannot be tied to one of 300 cards), `dragging`, `composer`, `composerEnd`, severity and body; dragging runs through `react-diff-view`'s own `gutterEvents` / `codeEvents` on the new column only, ends on `mouseup`, `pointercancel`, or window `blur`, ignores non-primary buttons, and `user-select: none` is a class on `.app`; the highlight is the library's `selectedChanges` with `--accBg` and the 3 px accent bar. The composer is a `<form>` in the file card's slot under the last selected line, or a loose block under the card header (file level), the repository header (repository level), or above the centre panel (review level) — the four anchor levels of SPEC section 7, each with its own place to render; entry points are drag, click, `C` on the first added line of the current file (a collapsed card expands), `Comment on file` (added to the handoff's card header in the same commit), `Comment on repo`, and `Comment on review` in the session menu. `⌘⏎` and `esc` are handled on the document; `c` does nothing while a composer or an overlay is open. Posting goes through `POST /api/comments`; the toast is the handoff's Russian string with the real path form (the task's Verification named the English string; the handoff's language wins so one scene keeps one language). A card with an active selection or composer is never unmounted by virtualisation (ADR-008), and is re-evaluated when that ends. `src/ui/types.ts` re-exports the core types.

Review by an isolated session, two rounds: 2 critical (the file-level composer never rendered; the working tree, not the commit, had a red gate — settled by gates on the commit alone in an isolated worktree), 1 major (the threads slice landing ahead of its consumer — kept, tested in DA-23), 10 minor — closed in `12ca173`. Finding DA-22.1 (small text on `--tx3` below 4.5:1; no `prefers-reduced-motion`) taken by the ui-c worker.

**Verification.** Gates on the branch at each commit and on `main` after the wave's integration; `e2e/composer.spec.ts` (8): drag sends the range with `line`/`endLine`, `C`, the file-level form with `line: null` read back through the CLI, a collapsed card, the repository and review levels, the toast, a letter under an overlay staying with the overlay. Mutation probes after commit: `firstAddedLine` returning the hunk start → 2 tests fail; the empty-patch fallback → 1; `composerLabel` without the range → 1; `endLine` always null → the range e2e fails; the overlay guard removed → its test fails. Perf: 7.6 ms per frame and 0 long tasks on a quiet machine at `12ca173`.

**Documentation in the same pass.** `docs/reference/08-ui.md` (commenting, the four levels, the selection fact about `gutterEvents`), `docs/design/HANDOFF.md` §1.4 (`Comment on file`), `.impeccable/surfaces/src-ui-app-tsx.md`, `CHANGELOG.md`.
