# DA-24 · Result

**Closed 2026-09-05.** Completed. The header: the session pill and menu from `GET /api/sessions` (metrics, `CURRENT`, a create form taking name and base in the CLI grammar, `Comment on review`; a labelled region, not an ARIA menu, since it holds a form), switching through `use` and a full re-read (a session is another set of everything; `fromDocument` resets drafts, selection, focus, and per-file collapse state and keeps split/unified only for files that still exist); the base picker with three modes and the branch candidates of the whole root from the new read-only `GET /api/repos/branches` (one `git for-each-ref` per repository; `name` as `branch:<name>` takes it, `remote`, the number of repositories the branch exists in, `default` from `refs/remotes/<remote>/HEAD`, warnings for unreadable repositories; ordered default → repositories → code point), `Apply` → `PUT …/base` and a re-read; the counters `N open` / `N awaiting you` as filters that also show as a chip in the rail; the export modal with the markdown and a rendered view ordered exactly like `exportMarkdown` (checked against `diffalanche export` in the e2e), `Copy .md`; the scanner warnings bar with a per-session dismiss remembered in `sessionStorage` (a reload keeps it away, the next session brings it back); the status-bar context line. One place turns a base into its argument and back (`src/ui/base.ts`, every form checked against the domain parser — a mutation probe had found a second copy that could have sent a different base than the footer showed). The budget line "Switching review sessions" is a gate: the harness makes a second session with forty comments and measures the swap of threads, counters, and badges from the parsed response to the painted frame — 35.6 ms against 100.

Review by an isolated session, one round on this task: 6 minor (the dismiss not remembered, drafts surviving a session switch, `c` under an overlay, ARIA menu roles, the two export tabs in different orders, fixture restore not in `afterEach`) — closed in `bc3a1de` and `016ee4a`, where a probe showed the overlay guard had no test and the fixture restore had silently failed on a bodyless POST refused by the CSRF rule.

**Verification.** `tests/branches.test.ts`, `tests/ui-base.test.ts`, `tests/ui-wire.test.ts` (type-level equality of the UI's copies of server shapes), `e2e/header.spec.ts` (8); mutation probes after commit: all branches reported local → fails; order ignoring `default` → fails; `origin/HEAD` listed → fails; `repositories` typed as string → typecheck fails; `baseArgument` losing the branch → unit and e2e fail; dismiss not persisted → fails; reversed export order → fails; the unchecked restore → 6 tests fail; the overlay guard removed → fails. Perf: session switch 35.6 ms; the scrolling line is DA-16.1's, measured by the orchestrator on a quiet machine at the end of the run.

**Documentation in the same pass.** `docs/reference/08-ui.md` (header, sessions, base, export, warnings, storage choice), `07-server.md` (the branches route), `11-perf.md` (the session-switch line), `docs/backlog/triage/DA-22.1` (DA-24's `--tx3` surfaces), `CHANGELOG.md`.
