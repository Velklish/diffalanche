# DA-64 · Result

**Closed 2026-09-18.** Completed. `startReviewServer` swallows everything but a `DomainError` at start-up and writes a warning, with the reason in the code: a server that refused to start would leave the person with no way to see why. The CLI then read the same document a second time, for the line under the address, and rethrew what the server had just decided to tolerate — and by then the socket was open and the watcher running, so `process.exit` in `src/cli/index.ts` killed both. A hand-edited `comments.json`, which `docs/SPEC.md` section 3 calls an ordinary event, left the person with one line and no interface. `summary()` now answers that case with a line of its own — `the review could not be read: the address above says which file and what is wrong in it` — different from the first-run line because the remedy is different: `review new` fixes a root with no session and fixes nothing here. The file and the fault are named twice over anyway, on stderr as the server starts and by the address, which answers 500 `error: "storage"` with them. The other start-up readers were checked, as the entry asked: `grep -rn "\.document(" src e2e scripts perf` finds four — `GET /api/warnings`, which is a request and already answers through `errorResponse`; the tolerant side in `src/server/serve.ts`; this one; and `perf/harness.ts`, which builds its own fixture and where an unreadable file is a real failure. Only this one had the drift.

**Verification.** `tests/cli.test.ts`, describe "serve on a review that cannot be read", two verdicts driving a real `serve` process on a free port. The first breaks `comments.json` and asserts the process is still running, that stdout carries the address and the new line and not the first-run one, that stderr names the file, and that `GET /api/warnings` answers 500 naming it; the second asserts a root with no session still gets the first-run line. Mutation probes by the worker, both after the commit: restoring `throw error;` at `src/cli/commands/serve.ts:48` — exit 1, 29 of 30 passed, the first verdict red; removing the `instanceof DomainError` branch instead, the naive edit that answers every error with the new line — exit 1, 30 of 31, the second verdict red, which is why that second verdict was added. Worker gates on `3be7f1c`, all six green: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 (481/481, 35 files), `bun run test:bun` exit 0 (481/481), `bun run perf` exit 0 with every line inside budget. **Integration into `main`:** `c727ab6` and `3b53cae` cherry-picked onto the DA-72 commit; `CHANGELOG.md` merged by entry, `docs/reference/06-cli.md` and `tests/cli.test.ts` auto-merged with the neighbouring tracks and no conflict. @@GATES@@

**Documentation in the same pass.** `docs/reference/06-cli.md`: a paragraph under `serve` saying it exits non-zero only for what stops it from starting, with the line it prints instead. `CHANGELOG.md`, `### Fixed`.
