# DA-55.9 · Result

**Closed 2026-09-24.** Completed. The verdict `tests/watcher.test.ts` › a move of current › "says which repositories moved while nobody followed the task, after it follows it" no longer leans on the tests before it. Cause, measured by the worker with debug lines that were not committed: run alone, the verdict's task got a copy of the fixture's `diff.json` that still listed the probe file `armed-0.ts` (status `added`) in the other repository, while a scan of the tree did not. `arm()` waits for the `diff-changed` of the probe file's removal, and the watcher sends it before it writes `diff.json`, so a copy taken right after `beforeAll` could be one write behind; in the whole file the tests in between left the write time to land, under load they might not. The move tests now write each task's `diff.json` from `scanReview(config, { mode: "head" })` of the tree, and wait on no other watcher. Test-only; no code under `src/` changed.

**Verification.** On the base `29e1872` the verdict alone on Node: exit 1, 1 failed, `expected [ 'repos/core/cargos-api', …(1) ] to deeply equal [ 'repos/core/cargos-api' ]` (alone on Bun it passed, exit 0). On `acf912b`: alone on Node exit 0, alone on Bun exit 0, `-t "a move of current"` exit 0 (3/3), the whole `tests/watcher.test.ts` on Node exit 0 (46/46). Mutation probe after the commit, tree clean before and after: `readWhole` in `src/core/watcher/index.ts` pushing every repository → `npx vitest run tests/watcher.test.ts` exit 1, 45/46, red on this verdict. Worker gate records of run 0923 on a clean detached worktree under `/tmp/da-perf.lock`: `gates-core3-2.json` on `55316eb` — gates 7, green 6 (`test` 861/861, `test:bun` 861/861, `test:ui` 121 passed, `perf` 7/7, update after an edit 288 ms); `gates-core3-4.json` on `d456c14`, the whole track — gates 7, green 6 (`test` 863/863, `test:bun` 863/863, `test:ui` 121, `perf` 7/7, update 292 ms, `lint` 0, `typecheck` 0); the one red in both, `backslop lint`, is DA-55.8's and DA-60.3's quote blocks, turned into `quote:before:` when DA-55.8 closed. Review: the second isolated pass, over `ce57b2d..3a76fe9`, and the orchestrator's read of the diff; the pass's two findings were DA-55.8's (the acceptance brief), none on this entry. Evidence: `evidence-core3-b2.md`.

**Integration into `main`.** `acf912b` cherry-picked without commit onto DA-55.8's commit. Git merged `tests/watcher.test.ts` and `11-perf.md` with no conflict; `CHANGELOG.md` was kept as `main`'s file, and the branch's DA-55.9 entry added once at the top of Unreleased / Fixed, in the branch's layout.

**Documentation in the same pass.** [11-perf.md](../../reference/11-perf.md#waits-in-the-suites) (a file a watcher writes is not waited for by its event); `CHANGELOG.md`, `### Fixed`.
