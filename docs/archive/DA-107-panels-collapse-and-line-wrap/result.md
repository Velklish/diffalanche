# DA-107 · Result

**Closed 2026-09-19.** Completed, to the owner's decisions of 2026-09-18 and to the five findings of the isolated review (`9212206`, `d040efa`, `08b09a3`). Each side panel comes off the screen whole: `[` and `]`, the `‹` and `›` in the panel's own top row (`src/ui/components/PanelAway.tsx`), and the stub the header keeps while the panel is gone all call the same store action, and the panel is unmounted rather than narrowed. `.app`'s floor is what the panels still on the screen add up to — 860 px of reading column plus 308 and 392 — so 1560 / 1252 / 1168 / 860 px, three classes and no breakpoint anywhere. A long line wraps inside its code column by default and the file card owns no horizontal scroll; the header's `wrap` / `scroll` toggle, the theme toggle's control with words in it, gives back the columns aligned character by character. Both panels and the toggle live in `localStorage` beside the theme, and `[ ]  panels` joined the status bar's hints. **Of the two heights the card offered, the estimate that knows the width was taken:** `measurePatch` takes the code column in characters and counts `ceil(length / width)` visual rows of 22 px, and `measure.ts` derives that width from the widths `styles.css` fixes — the reading column less the centre's padding, the card's border, the gutters and the cell padding, over 7.2 px per character, JetBrains Mono's 0.6 em at 12 px — recomputed when a panel is hidden and when the window is resized, with no measurement of the DOM at all. That choice forces `word-break: break-all`: it fills each visual line to the column, so the count is the real one, while a break at word boundaries breaks earlier and leaves the estimate short — the drift ADR-008 exists against. The placeholder of a card whose diff is not mounted takes that estimate in the render the estimate changed in; the last measured height is state and is dropped the moment a new estimate arrives, because a measurement belongs to the width it was taken at, and a height written to a ref after the render is a frame late with nothing to re-render the card to read it. **An added or deleted file is drawn with one gutter and one code column even in a split view** — the library calls it `monotonous` — and counting it as two halves overstated those cards by half their height; caught by `the height of an unseen card is close enough that the page does not jump` at a drift of 0.0362 against a 0.02 budget, 0.0033 after the fix. The count is arithmetic and names its own approximations: a tab inside a diff line counts as one character and is not one on the screen, so a file that has tabs claims slightly less height than it takes, and no fixture or repository read so far has one; the reading column is `document.documentElement.clientWidth` and not `window.innerWidth`, which counts a classic scrollbar the centre panel never gets and overstated a code column by about two characters.

**Verification.** `bun run test:ui` 87 passed exit 0 — including five new checks in `e2e/shell.spec.ts` (`[` hides the sidebar and the centre grows by exactly 308, `]` the same with 392, both hidden survive a reload and come back from the header, a 1200 px window with both hidden has `scrollWidth === clientWidth`, and the two keys are silent in a field) and five in the new `e2e/wrap.spec.ts` (a long line wraps and the card owns no sideways scroll, `scroll` gives one row and a horizontal scroll back, the choice survives a reload, an unmounted card claims a new height when a panel goes, and the height a card claimed before it mounted is the height it has). The existing panels test keeps its name and now says what is true with both panels shown: the floor is still 1560. `tests/diff-height.test.ts` grew ten checks over the wrapped row count and the width it is counted against; `bun run test` 474 passed on the worker's tree. `grep -n "Panels-Do-Not-Shrink" DESIGN.md` finds a rule about a panel that is at its width or off the screen. **The Impeccable pass:** context loader, then the mechanical detector over the eight touched files — empty — and `tests/design-contrast.test.ts` with `design-tokens.test.ts`, 62 checks green; audit 19/20 (accessibility 3, performance 4, responsive 4, theming 4, implementation integrity 4). Its one real finding was that `.segment` had no focus ring at all, so the new `wrap` / `scroll` control was keyboard-reachable and showed nothing when focused; polish gave it the `accBd` outline `DESIGN.md` already prescribes, which covers the theme toggle because it is the same class, and gave the header's stub the `dcin` entrance everything that appears gets. **Integration into `main`:** `2604c1d`, `98827d2`, `3ff8554`, `9212206`, `d040efa` and `08b09a3` cherry-picked onto the DA-54.1 commit; the only conflict was `CHANGELOG.md`, where the branch's `### Changed` entry met the two `main` already had — all three kept, this one first. Every other file auto-merged, and `DESIGN.md`, `PRODUCT.md`, `docs/design/HANDOFF.md`, `e2e/diff.spec.ts`, `e2e/shell.spec.ts`, `e2e/wrap.spec.ts`, both screenshot baselines, `src/ui/**` and `tests/diff-height.test.ts` are byte-identical to the track head `8bdec56` afterwards.

**Documentation in the same pass.** `DESIGN.md` (the principles line, the north star, the Layout paragraph, the responsive paragraph, *The Panels-Do-Not-Shrink Rule*, the diff subsection, the *Do* item about fixed widths, and `‹ ›` in the symbol set), `docs/design/HANDOFF.md` (section 1's layout line, header, sidebar, rail, diff lines and status bar, the keyboard map, State Management, and the implementation rule about the threshold and the pre-mount height), `PRODUCT.md` (the panel symbols), `docs/reference/08-ui.md` (The page, The diff, The header, The keyboard map), `CHANGELOG.md` under Changed.

**Not covered.** Touch targets under 44 px (`.panel-toggle` about 15 × 23, `.segment` 26 × 26): deliberate for a desktop tool at this density, named rather than changed. The header's stub shifts its row by about 23 px when it appears, which is the reader's own press and not the layout moving when data arrives. The tab approximation above. The screenshot baselines `shell-dark-darwin.png` and `shell-light-darwin.png` were regenerated because this task changes the header; the 33 and 40 pixel drift they carried on the base `5fdaed9` went with them, and its cause was not investigated.
