# DA-60 · Result

**Closed 2026-09-24.** Completed: the card's eight structural causes, the two reds of the same family its run recorded, and the sweep of fixed waits. No file under `src/` changed. For each item, what would now have to happen for it to redden:

| # | Fix | Red now requires |
|---|---|---|
| 1 | `tests/watcher.test.ts`: each of the three edits is paired with one `rescanRepository` timed right after that edit's own `diff.json` write landed; the assertion is `median(elapsed_i − rescan_i) < 300` | the watcher's own share — debounce, event delivery, check-ignore, queue — over 300 ms in two of three pairs. Residual: a load spike must fall between two adjacent measurements about a second apart, twice, instead of anywhere in a 20 s window |
| 2 | `scripts/smoke.sh` retries on serve's own sentence `port [0-9]+ is already in use` plus the raw errnos; `tests/cli.test.ts` "says it in the words the smoke script retries on" reads the pattern out of the script and matches it against `serve`'s real stderr for a held port | the script's pattern and serve's wording drift apart |
| 3 | `tests/helpers/typescript.ts` `runsTypeScript` / `needsTypeScript`, applied at every `.ts` spawn: cli 5, write-api 1, cli-comments 1, watcher 1 (+1 dependent), events 1 (+1 dependent), storage-concurrency 2 | a new unguarded `.ts` spawn |
| 4 | `e2e/shell.spec.ts`: both screenshot tests `test.skip(process.platform !== "darwin", reason)`; `test:ui:ci` and `--ignore-snapshots` removed, the CI `ui` job runs `bun run test:ui` | a macOS pixel change |
| 5 | `tests/events.test.ts` `arm()` in `beforeAll`: a write repeated until a `diff-changed` frame, the removal waited for by the diff route no longer listing the file | the watch never arms in 15 attempts |
| 6 | `e2e/sidebar.spec.ts`: the chosen row's card is in view (0 ≤ top < 200) on the frame the click produced; the ms are printed and the 50 ms left to `bun run perf` | the first scroll stops being synchronous |
| 7 | `tests/events.test.ts`: the first bytes contain `connected`, not `keep-alive`, and the head is under the imported `HEARTBEAT_MS` | the head held until a heartbeat — the defect's own mark |
| 8 | `perf/harness.ts`: after measuring, `liveUpdate` reset, the file restored, and a wait (60 s deadline) until the card has painted the restore | the restore never painted |

The run's two further reds: the synthetic fixture's "byte-identical" test and the perf/scope hooks hit the default 5 and 10 s — `vitest.config.ts` now sets `testTimeout` 60 s and `hookTimeout` 120 s (git.test's two `10_000` overrides dropped); and `e2e/live.spec.ts` waits for the settle record before it reads the reading position. Also: watcher `settle()` 10 → 20 s, the events reader's deadlines 5 → 20 s, the activity deadline 1 → 20 s. **The sweep of fixed waits** (`tests/`, `e2e/`, `perf/`): converted to conditions — the watcher's pointer move, walk and followed-session baselines, the events replay, three server build windows, three in history.spec, threads.spec, the perf restore; converted to an order — repo-bar's `scrollTo`, keyboard's toast (Playwright `page.clock`), empty.spec; derived from the thing timed — lock-writers' waits are max(150, 2 × the same writer uncontended), the multiple a heuristic; documented floors — storage ×3, sessions ×2, change-set ×1; left as they were — poll loops with deadlines, `setTimeout(done, 0)` yields, smoke.sh's readiness loop. The rule and the table of which assertion owns which budget are "Waits in the suites" in `docs/reference/11-perf.md`.

**The orchestrator's rulings.** Item 4 is accepted as a platform decision: the screenshot assertions declare macOS and skip elsewhere, `test:ui:ci` and `--ignore-snapshots` are gone, and the CI `ui` job runs `bun run test:ui` — **nobody has run the UI suite on Linux; the CI job will be its first run**. Two verdicts keep their test but not their bound: the sidebar's was renamed to what it checks, "choosing a file brings its card into view on the frame the click produced" (the 50 ms stays the gate's), and "carries a reply written by the CLI" lost its < 5 s bound — `docs/SPEC.md` section 6 has no such budget, and delivery, not latency, is what it owns; the latency is printed. Item 1's remainder is accepted: a wall-clock budget under load cannot be made never to fail, and the paired measurement is the limit. The global `testTimeout` 60 s / `hookTimeout` 120 s and `settle()`'s 20 s are deadlines on a hang, not budgets. Item 8's harness code was executed once with `DIFFALANCHE_PERF_IGNORE_LOAD=1`, as a check that it runs — all three repetitions ended on the restore's paint — and its numbers are not evidence.

**Verification.** Mutation probes by the worker, tree clean before and after each: P1 smoke pattern back to the raw errnos → cli.test exit 1, 1 failed | 41 passed; P2 `DEFAULT_DEBOUNCE_MS` 100 → 500 → "rescans the edited repository alone" exit 1 (`expected 546.7… to be less than 300`); P3 the stream's hello not written → exit 1 (`expected ': keep-alive\n\n' to contain 'connected'`); P5 `runsTypeScript` forced false → first red on two dependent tests (fixed in `d09df67`), then exit 0, 125 passed / 13 skipped; P7 a newly followed task's comments replayed → exit 1; P8 the metadata compare skipped for the current session → exit 1. Not probed: item 8 (only `bun run perf` runs it) and item 4 on Linux. The busy-port retry, proved with a real `serve` holding the first port: the branch's `smoke.sh` moved to the next port and passed (exit 0), the base's exited 1 with serve's own sentence. Under load 23–69 on `f725c4f`, `bun run test` and `bun run test:bun` three times each, all exit 0 (41 files, 657). **Verdict names:** `npx vitest list --json` base 587 → 589 on `f725c4f`, none removed, two added (cli.test "says it in the words the smoke script retries on", DA-55.6's server test); Playwright `--list` 104 → 104, with one intended rename since, the sidebar's above. `vitest list` omits a `describe.skipIf(false)` block entirely, which is why the storage-concurrency verdicts skip per test: a listing-based name check would have read two verdicts as removed. Review: two isolated passes; this task's minors of pass one (the verdict name, repo-bar's poll for the panel's pick, the explicit 30 s timeouts, the CHANGELOG lines) closed in `19d6648`, pass two found nothing new in it. Worker gates on `2316d89`, clean, under the lock: `backslop lint`, `lint`, `typecheck` exit 0; `test` exit 0 (41 files, 660); `test:bun` exit 0 (41, 660); `test:ui` exit 0 (104); `perf` exit 1 on "Update after an edit in one repository" only, red on the base `54b2c28` the same way (alternating 321 / 314 / 353 / 329 ms, failure sets identical).

**Integration into `main`.** `9bc10b4 d09df67 f725c4f 19d6648` cherry-picked as one commit onto DA-55.6's. Three conflicts, resolved as worked out in the track's trial merge: `vitest.config.ts` — main's `unit` / `model` projects (`extends: true`) kept, and this task's `testTimeout` / `hookTimeout` put in the root `test:` block, which both projects extend; `e2e/keyboard.spec.ts` — main moved the toast test to the `Copy .md` button, this task put it on `page.clock`: `page.clock.runFor(1_600)`, then `copy.click()`, with the wait for the press's timer below; `e2e/live.spec.ts` — main's name `recorded` and this task's wait for the settle record before the reading position is read, since it guards both checks; main's `expect.poll` on `recorded` after the position check is dropped, because every assertion it guarded (`settled` not null, `corrected`, `grewAfter`) is still asserted after this wait and its condition is the same. `tests/exports.test.ts`: the `KEPT` entry for `HEARTBEAT_MS` removed, since `tests/events.test.ts` now imports it. `CHANGELOG.md` merged by git's three-way pick without a conflict and without `merge-changelog`: the change against main equals the branch's change against its fork, line for line, including the two DA-54.2 sentences rewritten by clean removal. `perf/harness.ts` keeps DA-59's unexported `Options`, ml2's `lag` field and this task's wait for the restore.

**Found at integration and fixed in this commit.** `e2e/keyboard.spec.ts` › "saying the same thing again gives it the whole lifetime" went red in the first integration `bun run test:ui` on `3ce1373` (exit 1, 113 passed, 1 failed: `expect(locator).toBeHidden() failed … Received: visible` after `page.clock.runFor(1_400)`). Two races, both in this one test. **The merge's:** on `Copy .md` the toast's 2.2 s timer is set when the clipboard answers, and `click()` returns before it does — the branch's `b` raised its toast inside the click, main's version waited on the wall clock; merged, a clipboard that answers after `runFor(900)` has started sets the second timer at 2.5 s instead of 1.6 s, and the toast is still up at 3.9 s. **The branch's own**, from `9bc10b4`: `pauseAt(Date.now() + 1)` reads the page's time and pauses 1 ms after it, and the page's clock runs on while the command travels — `clock.pauseAt: Error: Cannot fast-forward to the past`. The resolution as first merged, alone under the lock, five runs: exit 1, 1, 1, 1, 0 — three `toBeHidden`, one `Cannot fast-forward to the past`. The fix, in this file only and with the assertion unchanged: the test counts the toast's 2.2 s timers through a wrapper of `window.setTimeout` and moves the clock only after `expect.poll` has seen the press's timer set (1, then 2); the pause is taken a minute ahead, past the test's own 30 s timeout, and the jump fires each timer pending then at most once — the feed's tick interval among them — which the test does not read. Probes, each alone: the clipboard made to answer three page fetches after the click — red without the fix (exit 1, `toBeHidden`), green with it (exit 0); 50 ms put between reading the time and `pauseAt` — red with `+ 1`, green with `+ 60_000` (numbers in "Integration gate" below).

**Integration gate.** On `3ce1373` (both tasks merged, before the fix above), clean: `backslop lint`, `bun run lint`, `bun run typecheck` exit 0; under `/tmp/da-perf.lock` 23:17:51–23:21:26Z, load 5.4–22.1: `bun run test` exit 0 (52 files, 784), `bun run test:bun` exit 0 (52, 784), `bun run test:ui` exit 1 (113 passed, 1 failed — the toast test above), `bun run perf` exit 1 — measured, over budget on "Update after an edit in one repository" 313 ms (453 / 313 / 308) and "Scrolling the diff: long tasks" 1 (0 / 1 / 2), every other line ok; `e2e/live.spec.ts` alone twice exit 0, 0 (2 passed each); `bun run test:e2e -- -g "review use switches the UI and the CLI at once"` exit 0. The update line is red on the base too, by the worker's alternation of `54b2c28` and `2316d89` under one hold (321 / 314 / 353 / 329 ms, failure sets identical). Both red lines alternated again, `7a26d8b` against this tree with the fix, one `bun perf/run.ts --fixture .perf/fixture --variant default --runs 1` process a run, lock 23:28:18–23:29:45Z, load 8.2–10.1: long tasks base 1 / 1 / 0, tip 0 / 0 / 0; update base 302 / 301 / 352 ms, tip 338 / 400 / 333 ms (medians 302 / 338, spreads 51 / 67 ms, the ranges overlap) — long tasks are DA-69.1's family and no worse on the tip; the update medians differ inside the spreads, which by DA-110 is not a difference, and the orchestrator ruled to land. The tendency is filed as [DA-60.1](../../backlog/minor/DA-60.1-update-line-higher-on-the-tip-by-alternation.md), `minor (hypothesis)`, Scope `11-perf`, with both series and the perf wave's question: whether item 8's wait for the restore by event changes what the update line measures. Neither task changes the edit's path under `src/`. The timer fix of the toast test, same hold 23:25:43–23:28:12Z: the clipboard probe without the fix exit 1 (`toBeHidden`), with it exit 0; the fixed test alone five times exit 0 each; `bun run test:ui` exit 0 (114 passed). **The landed tree** — `6e42d8c`, this commit before the sentences of this paragraph from here on were added to this file — clean: `backslop lint`, `bun run lint`, `bun run typecheck` exit 0; under the lock 23:33:45–23:37:47Z: the pause probe exit 1 with `+ 1` (`clock.pauseAt: Error: Cannot fast-forward to the past`) and exit 0 with `+ 60_000`, the clipboard probe on the final test exit 0, the final test alone five times exit 0 each; `bun run test` exit 0 (52 files, 784), `bun run test:bun` exit 0 (52, 784), `bun run test:ui` exit 0 (114 passed); `bun run perf` exit 1, **declined on load — a gate not run**: "unable to measure: load average 31.744140625 over 8 cores is 3.97 per core, ceiling 2.5 during the run". Its measured run is the one on `3ce1373` above; nothing under `src/`, `perf/` or `scripts/` differs between the two.

**Documentation in the same pass.** `docs/reference/11-perf.md`: "Waits in the suites", the rule and the table of which assertion owns which budget; `docs/reference/05-watcher.md`: the watcher verdict's paired measurement and why; `docs/reference/08-ui.md` and `README.md`: the screenshot specs declare macOS, CI runs the whole suite; `.github/workflows/ci.yml` and `package.json` (`test:ui:ci` removed); `CHANGELOG.md`, `### Fixed`.
