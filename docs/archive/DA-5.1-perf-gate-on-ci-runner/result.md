# DA-5.1 · Result

**Closed 2026-09-05.** Completed with the owner's decision: a named allowance for the runner. The first `perf` job on a GitHub-hosted `ubuntu-latest` runner (run 33983377225 on `main` at `b490494`) measured the same commit a little over twice as slow on every millisecond line as the development machine — CPU per frame 17.3 ms against 7.8 (budget 8.3), the composer 50.5 against 22.6 (budget 50), first render 161 against 87, the session switch 181 against 108 — and zero long tasks in every run; the table is in the task. The gate now holds, on a runner (`GITHUB_ACTIONS=true`), every `ms` ceiling at the specification's number times `RUNNER_ALLOWANCE = 2.5` (`perf/budgets.ts`, with the ratio and its reason in the file), the long-task line at zero, and on a development machine the strict numbers; the printed table names the widened ceiling beside the budget so a green runner is not read as the strict number holding. The multiplier leaves about fifteen percent over the runner's own reading, so a regression of that size is red on the runner and a smaller one is red on the development machine first. The owner chose this over holding the specification's numbers everywhere (a red job until a faster machine exists) and over a report-only job.

**Verification.** `tests/perf.test.ts`: the runner's own readings fail without the allowance and pass with it, the table cell reads `8.3 ms (20.8 on a runner)`, a long task still fails under the allowance, and twice the runner's reading fails. Local `bun run perf` green with the strict numbers; the `perf` job on `main` after this commit is the first of the three green runs the task asked for. Mutation probe after commit: the allowance applied to the `tasks` line → 1 test fails; the allowance ignored → 1 fails.

**Documentation in the same pass.** `docs/reference/11-perf.md` (the gate: which numbers are enforced where), `CHANGELOG.md`.
