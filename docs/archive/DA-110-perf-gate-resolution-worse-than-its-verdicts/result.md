# DA-110 · Result

**Closed 2026-09-24.** Completed: 11-perf states, per line, what one run of the gate resolves against another and what a comparison resolves, measured on two worktrees of one commit; the gate takes five repetitions instead of three, with no warm-up rule; and `bun perf/compare.ts --base <dir>` is the documented answer to "is this branch worse than its base". **No budget was changed.** The resolution, from a pool of 72 samples of `f9cda1b` (series m1, 05:11–05:29 UTC, one-minute 3.5–10.6): the update line 33 ms at three repetitions a side, 25 ms at five — one gate run against another — and 19 ms at nine; CPU per frame 0.2, 0.1 and 0.1 ms; the long-task count, a count, is 0 in 79 of 80 samples and has no resolution to state. The card's quarter was the machine: its tree `5e0ee24`, alternated with `f9cda1b` at about one runnable task per core, read CPU per frame 7.8–7.9 ms and update 275–287 ms against the card's 10.3 and 391 at the same load average — filed as DA-110.1. Five repetitions and not three: the update median lands over its 300 ms budget in 3.1 % of draws at five against 7.0 % at three, for about 26 s a run. The first repetition is kept: it was above the median of the other four in 2–5 of 8 runs per line, not as a rule. The comparison alternates one `perf/run.ts` process of each tree, holds each line's difference of medians against a permutation threshold of that run's own samples (a count by its mean), prints `worse`, `better` or `no difference`, and exits 1 on a `worse`. It refuses fewer than eight runs a side — under eight no split could pass its threshold — and holds a line with a sample the gate would not trust (DA-69) as `not measured`, exit 1. One run names one line of seven different about once in seventeen on identical trees, so a `worse` counts when a second run agrees. Decided during the night, as such: the five repetitions and the comparison procedure are the worker's decision within this card, accepted by the orchestrator; the owner allowed budget changes this night only on a quiet-machine measurement, and none was made.

**Verification.** By the worker on the branch, every series under `/tmp/da-perf.lock` (`evidence-perf1-batch2.md`, `da110-raw-samples-perf1.json`, not re-measured here). The procedure run twice on trees measuring the same code — the branch at `edc7f5c` against two worktrees of `f9cda1b`, 06:19–06:27 UTC: every line `no difference`, both times, the update line −3 (±7) and +4 (±10) ms; again from `d121039`, 06:54–07:02: 27 of 28 lines `no difference` over the four runs, the one `better` a switch line inside the once-in-seventeen rate above. Exact enumeration over every split: six and seven a side cannot reach 1 % (floors 1.30 % and 1.17 %), eight can (0.31 %), which set the minimum in `f5f2a80`; `bun perf/compare.ts --base /tmp --runs 7` → exit 1, `--runs takes at least 8 for a comparison, got 7`. Mutation probes on `f5f2a80`, tree clean before and after (`handover-perf1-6.json` … `-8.json`): `perf/budgets.ts:146`, the threshold at 0 → exit 1, three verdicts of "the comparison of two trees" red; `perf/budgets.ts:151`, the minimum at 6 → exit 1, "refuses fewer than eight runs a side, where no difference could come out as one" red; `perf/budgets.ts:213`, the DA-69 check skipped → exit 1, "leaves a line with a sample it cannot trust not measured, as the gate does" red. Worker gate records on clean trees: `gates-perf1-3.json` on `d121039` — gates 7, green 7; `gates-perf1-2.json` on `f5f2a80`, 07:29–07:33 UTC after a wait for quiet — gates 7, green 7 (`test` 901/901, `test:bun` 901/901, `test:ui` 151 passed, `perf` 7/7 with update 294 ms from 289/298/294/293/297, `lint` 0 on 256 files, `typecheck` 0, `backslop lint` 0 with one warning). The series on the same commit at 07:08–07:17, on a machine other sessions loaded, read `perf` 6/7 — update 329 ms, every line 10–15 % slow — with `bun perf/compare.ts` against `f9cda1b` at nine a side saying `no difference` on every line and `Not evidence` at its end; the quiet series replaced it (`gates-perf1-4.json`). Verdict names `f9cda1b` → `f5f2a80`: vitest 809 → 828, none removed. Review: two isolated passes; pass two's three majors on the comparison — under six runs it could not say `worse`, untrusted samples compared, a five-line header — closed in `d121039` and `f5f2a80`. The integration gate over the three commits of this acceptance is `gates-approver-perf1.json`.

**Integration into `main`.** `4676586 edc7f5c 3ddc8b7 d121039 f5f2a80` cherry-picked without commit onto DA-54.4's commit. The code and the README merged with no conflict; `CHANGELOG.md` conflicted at `d121039` and `f5f2a80` and kept `HEAD`'s text, and then this commit took `docs/reference/11-perf.md` and `CHANGELOG.md` from `f5f2a80` whole, by the orchestrator's ruling. That also lands the parts of DA-54.5's and DA-54.4's reference their own commits kept out — what the 300 s wait bounds, the chain's wait of 5–35 s, the UI suite's reading after the run and its changelog entry. The tree outside `docs/backlog` and `docs/archive` is the branch's `f5f2a80`, file for file.

**Documentation in the same pass.** [11-perf.md](../../reference/11-perf.md#what-the-gate-resolves-and-comparing-two-trees) (the resolution table, five repetitions, no warm-up rule, the comparison procedure and its minimum, the confirming run, the gate's `--runs` default); `README.md`; `CHANGELOG.md`, `### Changed`.

**Filed, not closed.** [DA-110.1](../../backlog/minor/DA-110.1-load-average-cannot-tell-a-measurable-machine.md) (hypothesis, in `minor/` with its Scope filled, landed with DA-54.5's commit, which cites it) — the load average cannot tell a machine the harness measures well on from one it does not.
