# DA-109 · Result

**Closed 2026-09-24.** Completed. `tests/frame-tables.test.ts` holds the three frame tables of the reference — the events of [05-watcher.md](../../reference/05-watcher.md), the stream of [07-server.md](../../reference/07-server.md) and the handlers of [08-ui.md](../../reference/08-ui.md) — to the `WatcherEvent` union, and 08-ui gained the `sessions-changed` row it was missing. Decisions this acceptance records:

- **The union is read with tree-sitter** from `src/core/watcher/bus.ts`, through the helper DA-59's check shares, not with the TypeScript package the card suggested: TypeScript 7.0.2's package root exports only its version, and its AST sits under `typescript/unstable/*`. Each member must be an object literal with a literal `type`; one the test cannot read that way fails it rather than being skipped.
- **Each table carries an anchor comment** naming the union and what the table prints, and `REQUIRED` in the test holds each file's anchor shape: `05-watcher` fields without `type`, `07-server` fields with `type` and the server's frames, `08-ui` names and the server's frames. Rewording an anchor cannot switch the field comparison off. Every `docs/reference/*.md` is scanned, so another table opts in with the same anchor.
- **`activity` and `reload` are an explicit list of server frames**, each owed a row under an anchor that names the server, and each must still be a string in `src/server/events.ts`.
- **A row naming two frames is split**, every backticked name in its first cell being a frame; fields compare as a set, and their order is deliberately not checked.
- The check is a test, so it runs in `bun run test` and `bun run test:bun`, both in `gates` in `backslop.json`, and in the CI `check` job; it has no gate line of its own.

No minor entry is left under this task.

**Verification.** Worker gates, clean trees, under `/tmp/da-perf.lock` (`gates-gates-2.json`, `gates-gates-3.json` in run 0923). On `96dff1b`: gates 7, green 6 — `backslop lint`, `bun run lint`, `bun run typecheck` 0; `bun run test` and `test:bun` 0 (49 files, 737/737); `bun run test:ui` 0 (114/114); `bun run perf` declined at 3.49 per core, not run. On `76e9bc4`: gates 7, green 5 — `test` 0 (740/740), `test:ui` 0 (114/114); `test:bun` 1 on one watcher test, green alone on both sides, and `perf` 1 on the update line, both recorded in [DA-59's result](../DA-59-dead-code-sweep/result.md). On `46fb2ad`: `tests/frame-tables.test.ts` 9/9. Mutation probes, each exit 1 with one failed of nine: a field dropped from a 05-watcher row (Q1), a member added to the union (Q2), the `sessions-changed` row deleted — the state of `d703e72` (Q3), a row for a frame the union lacks (Q4), an anchor reworded (Q5), `"reload"` renamed in `events.ts` (Q6), the 07-server anchor turned to `names` (Q7), the 08-ui `reload` row deleted (Q8). Review: the first isolated pass asked for the anchor shapes and the owed server rows, closed in `76e9bc4`.

**Integration into `main`.** `96dff1b 76e9bc4` squashed onto DA-59's commit, which carries `f789fc1 0279892 46fb2ad`; no conflict in either group. After both, the tree equals the track tip `46fb2ad` outside `docs/backlog` and `docs/archive`, `CHANGELOG.md` included. Integration gates on `4094109`, the two commits before this result's numbers, clean: `backslop lint`, `bun run lint` (219 files) and `bun run typecheck` 0; under `/tmp/da-perf.lock`, 21:56–21:59Z, `bun run test` 0 (49 files, 740/740), `bun run test:bun` 0 (740/740), `bun run test:ui` 0 (114/114). `bun run perf` declined at 4.17 per core, load 33 over 8 cores against a ceiling of 2.5, so it was not run. `bun run typecheck` on DA-59's commit alone, from a detached checkout: 0.

**Documentation in the same pass.** `docs/reference/README.md` (The frame tables mirror `WatcherEvent`), `docs/reference/05-watcher.md`, `07-server.md`, `08-ui.md` (an anchor each, and the 08-ui row), `CHANGELOG.md`.
