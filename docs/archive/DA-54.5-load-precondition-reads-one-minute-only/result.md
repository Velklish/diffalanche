# DA-54.5 · Result

**Closed 2026-09-24.** Completed: the load precondition reads the one- **and** the five-minute averages, both held to the same `LOAD_CEILING` of 2.5 per core, and before the run it **waits** for a quiet machine instead of refusing at once — it reads them every 5 s for up to 300 s (one time constant of the five-minute figure), prints `waiting up to 300 s for a quiet machine: …` and `quiet after <n> s: …`, and declines only when the wait runs out, `… before the run, after waiting 300 s`. After the run the busier end of both averages decides whether the table is evidence, as before. The ceiling, the bypass `DIFFALANCHE_PERF_IGNORE_LOAD` and the place of `perf` at the end of `gates` are unchanged; `backslop.json` is untouched. The other three candidates lost, each with its reason in 11-perf: a tighter five-minute ceiling (a number nobody measured), sampled CPU idle (utilisation stops at 100 %, below the ceiling's scale), and `perf` first in `gates` (`backslop gates` stops at the first red, and a chain on its own does not spoil `perf` — measured, below). The card's own case, 0.77 per core over a minute and 1.56 over five, still passes as quiet, and the reference says why: 2.5 on the five-minute figure is the one-minute measurement carried over, and at about one per core the load average cannot tell a machine the harness measures well on from one it does not — filed as DA-110.1. The precondition is one module, `perf/load.ts`, asked by two gates: `perf` and, since DA-54.4, `test:ui`; the Vitest suites get none, their wall-clock verdicts being DA-60's end.

**Verification.** By the worker on the branch, every series under `/tmp/da-perf.lock` with the load sampled every 5 s (`evidence-perf1-batch1.md`, not re-measured here). The base chain on `f9cda1b` alone on the machine: one-minute peak 32.16 (4.0 per core) inside `test:bun`, five-minute peak 14.33 (1.79), `perf` all ok. Three `backslop gates --keep-going --require-clean` on `fa471a4`, then `4974aaf` and `e166b99`: `perf` never waited in any of the five chains; gates 7, green 7 in four of them, the fifth 7 / 6 on a UI verdict DA-54.4 files; the eight wall-clock verdicts the card names green in all five chains in a row. Live probes with a stubbed `loadavg`: `bun perf/gate.ts` at 6.19 and 25 waited, then `unable to measure: … after waiting 300 s`, exit 1, before the fixture and the browser. Mutation probes on `f5f2a80`, tree clean before and after (`handover-perf1-2.json`, `-3.json`): `perf/load.ts:39`, `tooBusy` ignoring the five-minute figure → exit 1, "declines while the last five minutes were over the ceiling, though the last one is not" red; `perf/load.ts:72`, `waitForQuiet` returning the first reading → exit 1, the three verdicts of "the wait for a quiet machine" red. Worker gate records on clean trees: `gates-perf1-3.json` on `d121039` — gates 7, green 7; `gates-perf1-2.json` on `f5f2a80`, the whole track — gates 7, green 7 (`test` 901/901, `test:bun` 901/901, `test:ui` 151 passed, `perf` 7/7 with update 294 ms, `lint` 0 on 256 files, `typecheck` 0, `backslop lint` 0 with one warning). Review: two isolated passes; this task's major of pass one — the card's case still passes and the changelog said otherwise — closed in `0a29166`. The integration gate over the three commits of this acceptance is `gates-approver-perf1.json`.

**Integration into `main`.** `fbf1e1f` and `0a29166` cherry-picked without commit onto `f9cda1b`. Git merged the code, `CHANGELOG.md` and the "tighter ceiling" bullet of 11-perf with no conflict; the one conflict, 11-perf's paragraph after the chain measurements, kept `HEAD`'s text by the orchestrator's ruling, since the branch had written that place by DA-54.4 first. So this commit carries its code whole and its reference without `0a29166`'s paragraph on what the 300 s wait bounds — about 6 per core, 48 on 8 cores, arithmetic on the damping and not measured; that paragraph lands with DA-110's commit, which takes 11-perf and `CHANGELOG.md` from the branch's tip whole. The DA-110.1 card comes in this commit from `f5f2a80`, because the reference and the changelog cite it. The subject says "never trips perf": the branch's "never trips it" was true of `perf` and not of the UI suite, which waits 5–35 s in a chain (the orchestrator's ruling).

**Documentation in the same pass.** [11-perf.md](../../reference/11-perf.md#the-gate) (the precondition: both averages, the wait, why not the other candidates, what it still does not see); [ADR-013](../../adr/adr-013-perf-gate-off-ci.md) (the fourth option reads both figures and waits); `README.md` (the gate waits up to five minutes on a busy machine); `CHANGELOG.md`, `### Changed`.

**Filed, not closed.** [DA-110.1](../../backlog/minor/DA-110.1-load-average-cannot-tell-a-measurable-machine.md) (hypothesis, in `minor/` with its Scope filled) — the load average cannot tell a machine the harness measures well on from one it does not.
