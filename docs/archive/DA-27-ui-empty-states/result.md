# DA-27 · Result

**Closed 2026-09-05.** Completed. A root nobody has opened a session in is no longer a review that failed: `GET /api/review` refuses it with `no-current-session`, the store reads the refusal by its code (`refusal()` returns `{code, message}`) and shows `components/FirstRun.tsx` under the header — the mark, the explanation, three metric cards from `GET /api/scan` (repositories found, with changes, worktrees; dashes until the scan answers, since a zero would be a claim), a name field with the base in the CLI grammar and `Create`, and the CLI line. `components/NoChanges.tsx` is the other screen: a session whose base resolves to what the working trees hold; it replaces the file cards, names the session, and offers `Change base` (the real base picker) and `Other session` (the real sessions menu). One create path: the first-run screen writes DA-24's `newBase`, the duplicate over the picker draft was deleted. The three base modes sit inline on the first-run screen rather than behind the picker of handoff section 5, because that picker applies a base to a session and here there is none yet. The e2e fixture is now removed before generation: `synth --small` wrote into whatever it found, and one spec failed on a reply another had written on an earlier run.

**Verification.** `tests/ui-empty.test.ts` (6), `e2e/empty.spec.ts` (3): a refusing root shows the first run with the scan's counts (`4 / 2 / 1`), the CLI line and no workspace under it; `Create` lands on the review with the session in the pill; an empty change set shows the no-changes screen naming the session, with `Other session` opening the menu. The store's `refusal` and `loadReview` paths are covered by the DA-25 and DA-26 probes. Gates as in DA-25; shell screenshot baselines regenerated in `6e217b6`.

**Documentation in the same pass.** `docs/reference/08-ui.md` (empty states, the inline base modes), `CHANGELOG.md`; README's first-run paragraph checked by the approver at integration.
