# DA-3 · Result

**Closed 2026-09-05.** Completed. The spike measured diff rendering of the full synthetic review (21 repositories, 300 files, 30 000 lines) against the budgets of `docs/SPEC.md` section 6 and produced [ADR-008](../../adr/adr-008-diff-rendering-verdict.md): no variant meets the budgets without virtualisation; `@git-diff-view/react` misses first render (9 872 ms) and CPU per frame even when virtualised (16.3 ms); the verdict is `react-diff-view` with virtualisation by file card (first render 34 ms, 0 long tasks, 6.4 ms CPU per frame, composer 13.9 ms, file jump 7.6 ms; re-measured after the review at 32.6 / 0 / 6.9 / 12.3 / 7.8). The fallback ADR-002 provided for is taken; ADR-002 carries a “Revised by ADR-008” note. The spike code stays as the Phase 1 skeleton: `src/core` (scanner, change-set reader over the `git` binary with `git diff HEAD` and untracked files as additions, `parseDiff`), `src/server` (Hono, `GET /api/review` in one response, static UI, the runtime switch `src/server/runtime.ts` — the one named exception to the `Bun.*` rule in AGENTS.md), `src/ui` (memoised file cards, refractor highlighting with nine registered grammars and the refractor 5 adapter, virtualisation), `perf/` (Playwright harness on headless Chromium). The CPU-per-frame ceiling is 8.3 ms, the frame of 120 fps.

Review by an isolated session over the whole Phase 0 diff: 2 critical, 4 major, 5 minor; the DA-3 share (base was the index, not HEAD; the Node server resolved before listening; the `Bun.*` rule contradicted the code; reference sections 01, 02, 07 missing; pure renames dropped; refractor registered every grammar; an invalid `font` shorthand) is fixed in `235e78a`.

**Verification.** Gates on the worker branch head `1d28ba8` and again on `main` after integration: `backslop lint` no errors; `bun run lint` 40 files; `bun run typecheck` exit 0; `bun run test` 29 tests in 6 files; `bun run perf` green (table above). Mutation probes after commit: untracked files removed from the change set → 2 tests fail; header lines counted as changes → 2 tests fail; `git diff HEAD` → `git diff` → the staged-changes test fails; resolving before listen → both server tests fail. Machine: MacBook Pro 18,3, M1 Pro, Playwright 1.63.0, Chrome Headless Shell 153.

**Documentation in the same pass.** `docs/adr/adr-008-diff-rendering-verdict.md` (new) and `docs/README.md` row; `docs/adr/adr-002-stack-and-delivery.md` revision note; `AGENTS.md` (the runtime-switch exception); `docs/reference/01-scanner.md`, `02-git.md`, `07-server.md`, `08-ui.md` (new), `11-perf.md`, `docs/reference/README.md`; `README.md`; `CHANGELOG.md`.
