# DA-54.4 · Result

**Closed 2026-09-24.** Completed: `bun run test:ui` is `bun e2e/quiet.ts`, which asks the load precondition of `perf/load.ts` before Playwright and again after it. Before: the one- and five-minute averages and the wait of up to 300 s that DA-54.5 gave `perf`, and on a machine that stays busy `unable to measure: … before the suite` with exit 1, before the build, the fixture and the browser. It is a step of the script and not a Playwright global setup, because Playwright 1.63 starts `webServer` before a global setup would run. In between, Playwright runs with its arguments as given and its exit code passed through; a run a signal ended leaves with 128 and the signal's number and names it, rather than passing for a busy red. After: a red run whose busier end was over the ceiling keeps its exit code and says `unable to measure: … during the suite` under the report. The bypass is the same `DIFFALANCHE_PERF_IGNORE_LOAD` — one variable for one precondition — and `**Not evidence.**` prints only when the load is over the ceiling; on a GitHub-hosted runner the precondition is off. The body is `runSuite` with its spawn and its readings injectable, tested by behaviour. How a red is proved to be the machine's is written once, for every gate, in 11-perf. In a `gates` chain the UI suite is the gate that waits, 5–35 s over five chains; `perf` after it does not.

**Verification.** By the worker on the branch, every series under `/tmp/da-perf.lock` (`evidence-perf1-batch1.md`, `evidence-perf1-batch2.md`, not re-measured here). Live probes with a stubbed `loadavg`: at 6.19 and 25, `bun e2e/quiet.ts` waited and declined `… before the suite, after waiting 300 s`, exit 1; the real `bun run test:ui` with the stub in a temporary `bunfig.toml` declined with no build and no Playwright; busy for three reads → `quiet after 15 s`, exit 0; a quiet start, busy end and red run → exit 1 with the `during the suite` line, a quiet red and a busy green without it; `bun run test:ui -- e2e/shell.spec.ts -g "theme survives"` → `1 passed`, the arguments through; a SIGTERM mid-run → `playwright ended on SIGTERM`, exit 143. Mutation probes on `f5f2a80`, tree clean before and after (`handover-perf1-4.json`, `-5.json`): `package.json:36`, `test:ui` without the precondition → exit 1, "is asked by `bun run test:ui` before Playwright starts the build and the server" red; `e2e/quiet.ts:44`, the arguments dropped → exit 1, "hands Playwright its arguments as given and leaves with Playwright's exit code" red. Chains: three `backslop gates --keep-going --require-clean` on `fa471a4` — 7 / 7, 7 / 7, and 7 / 6 on the verdict filed as DA-54.6; `4974aaf` and `e166b99` 7 / 7. Worker gate records on clean trees: `gates-perf1-3.json` on `d121039` — gates 7, green 7; `gates-perf1-2.json` on `f5f2a80` — gates 7, green 7 (`test:ui` 151 passed, no wait at a one-minute 5.54). Verdict names `f9cda1b` → `f5f2a80`: Playwright 151 → 151, none removed. Review: two isolated passes; this task's major of pass one — the suite read the load only before the run — closed in `76cd642`, and pass two's runner and signal findings in `90272e6`. The integration gate over the three commits of this acceptance is `gates-approver-perf1.json`.

**Integration into `main`.** `fa471a4 4974aaf e166b99 76cd642 90272e6` cherry-picked without commit onto DA-54.5's commit. The code, 08-ui, ADR-013 and the README merged with no conflict. Three conflicts kept `HEAD`'s text by the orchestrator's ruling, since the branch had written those places by DA-110 in between: at `76cd642` 11-perf's paragraph on the chain's wait (5–35 s over five chains) and its rule's origin ("where the rule came from and not a proof by it"), and `CHANGELOG.md`'s entry; at `90272e6` `CHANGELOG.md`'s entry again. So this commit carries its code whole, and its reference and changelog entry as `fa471a4` wrote them — "asks the load precondition first", without the reading after the suite and the signal. Both land in DA-110's commit, which takes 11-perf and `CHANGELOG.md` from the branch's tip whole. DA-54.6 moves from `triage/` to `queue/` in this commit as a major, a gate verdict that flaps (the orchestrator's ruling).

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (the UI tests: the precondition before and after, the wait, the bypass); [11-perf.md](../../reference/11-perf.md#a-red-the-machine-caused) (a red the machine caused, written once for every gate); [ADR-013](../../adr/adr-013-perf-gate-off-ci.md) (the same precondition around `bun run test:ui`); `README.md`; `CHANGELOG.md`, `### Changed`.

**Filed, not closed.** [DA-54.6](../../backlog/queue/DA-54.6-all-files-ring-walk-lost-its-first-focus.md) (queue, major) — the ring walk of all files lost its first row's focus once in three gate chains, green alone and under load on both trees since.
