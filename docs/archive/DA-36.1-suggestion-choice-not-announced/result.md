# DA-36.1 · Result

**Closed 2026-09-24.** Completed. The comment form says what it changes without the focus moving. One visually hidden `role="status"` in the form, driven by the store's `said`, speaks the row a key chose — `подсказка N из M · SEVERITY · <first line>` — and the model going away, once: the server's sentence and `AUTO недоступен`, with `, отправится WARNING` only when the 503 moved the severity from `AUTO`. An answer arriving while the reader types, the later 503s of the pauses, `TAB` (it takes the row just said) and a form opened while the model is away (it opens on `WARNING` with the sentence in its panel) say nothing. A new node for each saying lets the same row said again be heard again. `said` empties wherever the chosen row is dropped — new rows, a text too short, a failed ask — except when the same answer carried the model's going away. The shape is the worker's, written in [08-ui.md](../../reference/08-ui.md) ("What the form says aloud"); the card's `Composer.tsx` quote still matches. What a screen reader speaks was not heard: no screen reader was run; the live region's text was read back from the page.

**Verification.** Worker record `gates-ui2-2.json` on `da3fba0`, the whole track, on a clean detached gate worktree under the lock — gates 7, green 6 (`test` 853/853, `test:bun` 853/853, `test:ui` 151/151, `lint` 0, `typecheck` 0, `perf` update 299/304/298 ms; red: `backslop lint`, five errors, none from code — three card quotes of other tasks this acceptance turned into `quote:before:` and two mentions of DA-55.8, whose card is on `main`). Mutation probe at `79f0bff`, tree clean before and after: `set({ sugIdx: next, said: raise(said) })` → `set({ sugIdx: next })` → `tests/ui-composer.test.ts` exit 1, 14 of 15, red "suggestions while typing > says the row a key chose, and nothing when an answer arrives (DA-36.1)". `e2e/composer.spec.ts` reads the status text in two existing verdicts. Review: two isolated passes over the track — critical 0 in both; this task's note 7 (`said` kept a sentence about rows that were gone) closed in `5c06937`. Evidence: `evidence-ui2-final.md`.

**Integration into `main`.** `79f0bff 5c06937` cherry-picked without commit onto DA-102.1's commit and squashed into one commit. Git merged `CHANGELOG.md`, `08-ui.md`, `src/ui/store.ts` and `tests/ui-composer.test.ts` (DA-36.2 on `main` added a line to it) with no conflict; `CHANGELOG.md` was then set by hand as for every task of the track: `main`'s file, and the branch's final entry once, at the top of Unreleased / Fixed, in the branch's layout.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) ("What the form says aloud"), `CHANGELOG.md`, `### Fixed`.
