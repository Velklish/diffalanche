# DA-56.7 · Result

**Closed 2026-09-23.** Completed, on two owner decisions, both given on 2026-09-23. First **C**: `accBd` moves in lightness only, `#6c79b7` dark and `#6977ba` light, and stays the ring. The review then found the ring still under 3:1 where it is drawn inside or beside a state — on a chosen segment (`bd`, 2.81 / 2.82) and beside the dark theme's chosen file (`sel`, 2.56) — and showing nothing on the two controls that rest on an `accBd` border; the owner's second decision, **(c)**: every focus ring is drawn in `acc`, and `accBd` keeps C for the frames of a state. So every `:focus-visible` rule in `src/ui/styles.css` that drew `accBd` draws `acc`, and none names `accBd` any more; the two filled controls, the primary button and `Resolve`, keep a ring of their own. No token was added, since `DESIGN.md` already has `acc` as the one accent. `.ghost.accent:focus-visible` outranks its resting `accBd` border, and `.sev-chip:focus-visible` follows `.sev-chip.on`, which cleared the ring of a focused chosen chip at the same specificity. `DESIGN.md`'s focus rule and its frontmatter, `.impeccable/design.json`'s tonal ramp, `tokens.css` and `08-ui.md` say the same rule. Measured with the test's formula, the ring `acc` against its seven grounds: dark `bg` 6.06, `panel` 5.78, `panel2` 5.64, `panel3` 5.15, `bd` 4.33, `sel` 3.94, `accBg` 5.34; light 5.95, 6.49, 5.74, 5.29, 4.50, 5.15, 5.59. The frame `accBd` at C: `panel` 3.75 dark / 4.07 light, `accBg` 3.46 / 3.50. Before, `accBd` `#3f4a80` / `#aab2d8` was 1.63–2.00:1. The frames of a focused thread and of an agent's reply are `accBd` and took C with it; the card's Out of scope was corrected at this acceptance to say so.

**Verification.** `tests/design-contrast.test.ts` › "the contrast of the marks that carry a state without being text" › "clears WCAG 1.4.11 in the dark theme" and "… in the light theme", with no recorded exceptions left: the focus ring (`acc`) on the seven grounds above and the state frame (`accBd`) on `panel` and `accBg`. Playwright: `e2e/composer.spec.ts` › "the chosen severity chip shows the ring when the keyboard is on it", `e2e/keyboard.spec.ts` › "the highlighted search hit changes colour when the keyboard is on it", `e2e/empty.spec.ts` › "the accent button of the no-changes screen changes colour under the keyboard". Mutation probes by the worker, `git status --porcelain` empty before and after each, each exit 1 with only its own case red: `accBd` back to `#3f4a80` / `#aab2d8` in `tokens.css` reddened both theme cases ("state frame on panel 1.86 / 2.00"); `.palette-hit:focus-visible` back to an `accBd` outline reddened the search-hit case (13 of 14 passed); `.ghost.accent:focus-visible` dropped from its selector reddened the `Change base` case (6 of 7); `.sev-chip:focus-visible` put back before `.sev-chip.on` reddened the chosen-chip case. The light shell's screenshot baseline changed by exactly the 188 px of the `Change base` border on the no-changes screen (`#aab2d8` → `#6977ba`), read in the diff image before the baseline was updated in `927d582`; the dark baseline stayed inside the comparison's tolerance and was left. A keyboard walk by the worker, 40 `Tab` stops per theme with the computed outline or border read at each, found every stop drawn in `acc` except the file card's caret, which draws Chromium's own `outline: auto` — DA-56.8; screenshots of the chosen segment and the chosen file in both themes show the ring inside `bd` and around `sel`. Those screenshots stay in the worker's job directory and are not in the repository. Impeccable's detector over `Overlay.tsx`, `Header.tsx`, `tokens.css`, `styles.css` and `ReactDiffFile.tsx`: 0 findings. Verdict names base → track tip `927d582`: `vitest list` 562 → 574, Playwright `--list` 95 → 104, none removed; DA-56.2's two names lost "but for the recorded exceptions" when the exceptions went. Worker gates on `927d582`: `backslop lint` exit 0 (0 errors, 2 warnings — DA-100.2 and this entry in triage, the approver's), `bun run lint` exit 0, `bun run typecheck` exit 0, and under `/tmp/da-perf.lock` `bun run test` exit 0 (41 files, 637/637), `bun run test:bun` exit 0 (637/637), `bun run test:ui` exit 0 (104 passed); `bun run perf` declined by its load guard at 2.69 per core. On `91d92c4` it measured: every line in budget except "Update after an edit in one repository", median 430 ms against 300 (runs 448 / 430 / 311), a line the base `5a5f8e8` is red on the same day (572 / 321 / 349 ms, measured by another track). Review: one isolated pass over `5a5f8e8..227bdf0` for the whole track, critical 0, major 2, minor 3, plus two legacy observations; this task's share — the ring under 3:1 on `bd` and `sel`, the chosen chip's cleared ring, the tonal ramp still holding the old `accBd` — was closed in `536a3e1`, `91d92c4` and `b21ad30`, and `927d582` carried the new baseline. **Integration into `main`:** `227bdf0`, `536a3e1`, `91d92c4`, `b21ad30` and `927d582` cherry-picked as one commit onto DA-100.2's, the last of the track; `CHANGELOG.md` and `docs/reference/08-ui.md` auto-merged without a conflict.

**Integration gates.** Once for the whole track, on `8a17f66` — this commit before these numbers were written into its `result.md`, the only difference: `backslop lint` exit 0 (no errors), `bun run lint` exit 0 (191 files), `bun run typecheck` exit 0, and in one hold of `/tmp/da-perf.lock`, 19:48:11–19:51:03Z, `bun run test` exit 0 (41 files, 655/655; load 4.81 at the start, 23.95 at the end), `bun run test:bun` exit 0 (41 files, 655/655; 31.52 at the end), `bun run test:ui` exit 0 (104 passed). `bun run perf` exit 1 and no verdict: it measured three runs and then declined, "load average 23.03 over 8 cores is 2.88 per core, ceiling 2.5 during the run"; the table it printed as an indication has every line in budget but "Update after an edit in one repository", 414 ms against 300, and "Scrolling the diff: long tasks", 3 against 0 — the first is red on `5a5f8e8` too, the second is the line DA-69.1 is about. No perf verdict exists for this track. `bun run typecheck` exited 0 on each of the six commits of the track, from a detached checkout. Outside `docs/backlog`, `docs/archive` and `CHANGELOG.md`, the 23 files the branch touched are byte-identical to `927d582` and none of them is the core track's; `CHANGELOG.md` holds every line both tracks added to it, 58 and 46, and no entry twice.

**How `CHANGELOG.md` was merged.** `backslop merge-changelog` 0.9.0 does not take an entry whose bold heading wraps onto a second line for an entry of its own, so it read DA-100.1 and DA-76.2 as the tail of the entry before them: at `68b4049` and at `ae80572` it printed a conflict mark over "`review scope set` gives a scope to a session that has none" with that entry twice, and the second revision, the one that carries the new entry, was kept. It also puts a blank line between every entry of the file; the commits keep its content and order and the file's own layout, and `diff -B` between its resolved output and the committed file is empty at each of the two conflicts and at `38b35d7`.

**Documentation in the same pass.** `DESIGN.md` (the focus rule, its Do's and Don'ts, the frontmatter), `.impeccable/design.json`, `docs/reference/08-ui.md` (the ring is `acc`, the frames are `accBd`, the contrast groups); `CHANGELOG.md`, `### Changed`.

**Filed, not closed.** [DA-56.8](../../backlog/minor/DA-56.8-card-caret-draws-the-browser-ring.md), `minor`: the file card's caret draws Chromium's own ring rather than `acc`, found by the keyboard walk and left as found.
