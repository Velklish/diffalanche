# DA-28 · Result

**Closed 2026-09-05.** Completed. `e2e/acceptance.spec.ts` is the acceptance list of `docs/SPEC.md` section 10 as eleven named tests, one per line that involves the UI, each thin — the criterion sentence, with the depth left to the spec that owns the screen — and run against the **shipped binary**: `e2e/acceptance.config.ts` builds it (`bun run build -- --target current`), generates its own fixture, starts `./dist/diffalanche-<platform>-<arch> serve` on a genuinely free port (a child binds 0 and reports it; `DIFFALANCHE_E2E_PORT` overrides, validated on both branches), runs, and stops it. `bun run test:ui` keeps its dev-server path; the two suites share nothing but `dist/`, which is said where it matters, because `scripts/build.ts` empties that directory. The fixture (`e2e/fixture.ts`) calls `generate()` and `PROFILES` exported from `scripts/synth.ts` — no repository re-described — and adds on top a repository cloned from an upstream outside `repos/`, on a feature branch one commit ahead of `origin/main` with a clean tree, so `branch` mode has something to show; inside it a worktree at `nested/inner`, ignored upstream through `/nested/`, so the other half of the nesting rule ("a submodule or worktree nested inside a repository is not listed") is checked by the same test as the submodule. A CI job `e2e` on ubuntu and macOS builds, generates, installs Chromium and runs the suite, and its summary step lists the eleven criteria. `docs/reference/11-perf.md` gains a table of all five CI jobs (there was none) and says why `test:ui` is not a job and why `test:e2e` is not a local gate. `playwright.config.ts` ignores the acceptance spec, since both configurations scan `e2e/`.

Review by an isolated session, two rounds: 2 major (`open()` not waiting for the live stream, so a CLI write between first paint and `onopen` was lost — the `review use` criterion then failed on a race; the claim that the two suites can run at once, false because of the shared `dist/`), 8 minor (the "run directly" check through `pathname`, the missing nested worktree, README and 08-ui intros, an unused export now asserted in the branch test, a shadowed `open`, the unvalidated port, `test:e2e` and `gates`) — closed in `0945898`.

**Verification.** `bun run test:e2e` 11/11 against the binary (about 10 s warm, 70 s from cold), `test:ui` 57, `test` 394, `test:bun` green once DA-25.3 landed; the CI job verified by parsing the YAML and executing its summary step verbatim; nothing pushed. Mutation probes after commit, each reddening exactly its criterion: the scanner walking into a found repository (the nesting test), `remoteDefaultBranch` always null (the branch test), `untrackedFiles` empty (the untracked test — the cutoff probe, since that test derives its target from `git status`), both `session-changed` emit sites removed (the `review use` test). A first probe of the scanner changed nothing at the fixture's depth and was reported as dead before being made live. Gates on `main` after integration: backslop lint, lint, typecheck, test, test:bun, test:e2e, test:ui.

**Documentation in the same pass.** `docs/reference/08-ui.md` (the acceptance suite: running, debugging, the criteria table naming the deep spec for each), `docs/reference/11-perf.md` (the CI jobs table, the `e2e` job, the gate note), `README.md` (`bun run test:e2e`), `CHANGELOG.md`.
