# DA-63 · Result

**Closed 2026-09-21.** Completed. `prepare` in `perf/gate.ts` ran `rmSync(fixture, { recursive: true, force: true })` on whatever `--fixture` named and only then spawned `scripts/synth.ts`, so `assertOverwritable` — the guard written for exactly this hazard — lived in the process that ran *after* the deletion and never saw the path. `bun run perf -- --fixture .` from the repository root would have taken the working tree and its `.git`: `existsSync(".")` is true, `existsSync("./.diffalanche/current")` is false, and the erase ran with no prompt and no output before it. The guard now lives in the gate's own process, `perf/fixture.ts`, as `assertErasable`, and it is asked **twice** — once at the top of `main`, so a mistyped `--fixture` is refused before the machine is even read for load, and once inside `prepare`, because the check belongs to whoever erases rather than to whoever remembered to call it. What it accepts: an empty directory, or one carrying a `synth.json` this generator wrote. What it refuses whatever it contains: the repository, every directory above it, and the home directory. The mark it reads is `synth.json` and **not** `.diffalanche/` — `.diffalanche/` is what the tool writes into any folder somebody reviews, which makes it the sign of the thing the guard protects rather than a licence to erase it. Two follow-ups sit on the same task: `a009129` asserts the refusal by its frame rather than by its message, so rewording the sentence cannot silently pass the test, and `7d7e9e1` gives the subprocess test a timeout of its own.

**Verification.** Both refusals were driven by hand on the branch, with the directory listed afterwards to prove nothing was deleted: a fixture holding one foreign file exited 1 with `… is not empty and holds no .diffalanche/ from an earlier run` at `assertErasable (perf/fixture.ts:47)` and `ls -a` still showed `not-ours.txt`; `--fixture .` from the repository root exited 1 at `assertErasable (perf/fixture.ts:36)` with `… is the repository, an ancestor of it, or the home directory` and `git status --porcelain` showed only the files then being edited. Mutation probe 2 of the track, on `a009129`: removing the never-a-fixture branch at `perf/fixture.ts:40` made the run exit 1 and reddened `refuses the repository, its ancestors and the home directory`, `git status --porcelain` empty before the probe and after the restore. The verdicts live in `tests/perf.test.ts`.

**The guard fired on a live machine the same evening, unplanned.** In the integration gate run of this track the seventh gate did not reach the load precondition at all: `assertErasable (perf/fixture.ts:39)` refused `.perf/fixture` — a fixture directory built on 2026-09-05 by the generator that predates the stamp — and `bun run perf` exited 1 after **279 ms**, before the fixture, before the browser and before the machine was read for load. The directory was left untouched, all 38 MB of it, which is the whole point of the guard. This is the behaviour the message itself describes — a fixture from a generator older than `synth.json` is refused once, and deleting it by hand is the answer — so it is not a defect and not a budget verdict, and DA-54.3's load precondition has nothing to do with it. The deletion was deliberately **not** performed by the approver: two other workers were live on the same machine at the time, one of them about to take two `bun run perf` readings back to back as evidence, and erasing the fixture under them would have either broken that run or, worse, given it two incomparable points. The orchestrator holds that deletion until the machine is quiet.

**The gates of the merged `perf` track.** All seven tasks of the track are in `main`, and the full gate list ran once on the tree they make — `d76d804`, tree clean, by `approver:perf`, under the run's machine lock from 20:28:07Z to 20:30:45Z. `npx github:Velklish/backslop#v0.9.0 lint` exit 0, no errors, warnings 5 — all five are findings sitting in `triage/` whose parent tasks are now closed, which is the approver's triage move and not a defect of the code. `bun run lint` exit 0, biome checked 184 files, no fixes applied. `bun run typecheck` exit 0. `bun run test` exit 0, 36 files and **586 of 586** tests. `bun run test:bun` exit 0, 36 files and **586 of 586**. `bun run test:ui` exit 0, **95 of 95**, 79.9 s. `bun run perf` exit 1 after 279 ms, the refusal described above. **`gates 7, green 6`.** Three of those — `test`, `test:bun` and `test:ui` — the worker had declared in `gatesNotRun`, having run them on the parent commit `0f275a6`; they ran here on the tip itself, which closes that debt, and the tally of 586 rather than the worker's 585 is the test the tip adds for a half-written stamp. **Per-commit build:** every one of the fifteen commits typechecks standing alone, re-verified by the approver from a detached checkout on the landed shas — `364f5a0 4a4b061 05c9cdb 53da093 5b6f54b 39a5ae2 47e4934 a009129 7473c0e 18dd1d6 8e70c82 7d7e9e1 3477642 0f275a6 d76d804`, exit 0 on each, `COMMITS_CHECKED=15 FAILED=0`, the checkout returned to `main` with an empty `git status`. The other six tasks of the track point here for these numbers.

**The integration into `main`, and the decision reversed on the way.** The track was first landed by `git cherry-pick ed81928..d76d804`, which is what the approver's brief prescribed; the fifteen commits replayed cleanly, no conflict, and the resulting tree was byte-identical to the branch. It was then **reversed to `git reset --hard ed81928` followed by `git merge --ff-only d76d804`**, before any gate ran on it and before anything was pushed. The reason is evidence, not style: every record this track hands over is keyed by commit sha — the gate record carries `tree: d76d804…`, the handover record its own, the fifteen per-commit typecheck lines theirs, the mutation probe its own — and a cherry-pick makes all of them point at commits that are not on `main`, leaving the link from evidence to code resting on a correspondence table somebody has to keep. A fast-forward was available because `git merge-base main d76d804` was `ed81928` exactly, and it makes that table unnecessary: the shas in the records *are* the commits on `main`. `main` went `ed81928` → `d76d804`, fifteen commits in branch order, tree clean, `git rev-list --count ed81928..HEAD` = 15, `HEAD^{tree}` = `2164cc1e4c5766cf2b3882c03ef9c13f5f0d4a92`. Fifteen commits in one branch rather than one squashed commit per task is a deliberate departure from this repository's rule, taken by the orchestrator: two of the fifteen are review rounds spanning six and four tasks, so a per-task squash would mean splitting them by hunk, and every commit carries its task number in its subject.

**Documentation in the same pass.** `docs/reference/11-perf.md` — what the gate refuses to erase and why the mark is `synth.json` rather than `.diffalanche/`; a `### Fixed` entry in `CHANGELOG.md`.
