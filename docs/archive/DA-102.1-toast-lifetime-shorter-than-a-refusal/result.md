# DA-102.1 · Result

**Closed 2026-09-24.** Completed. A toast lives as long as its text takes to read: 2.2 s for a text of up to 60 characters — every answer the handoff draws, the save toast for a session name of up to 15 characters among them — and 50 ms more for each character past them, up to 10 s, which a text of 216 characters reaches. DA-102's storage refusal, 116 characters, now stays 5 s; the store's note on a raised toast says its own lifetime. The DA-105 test "saying the same thing again gives it the whole lifetime" counts the 2.2 s timer and now grants the clipboard: refused, `Copy .md` says a 72-character sentence that lives 2.8 s. The card's `Toast.tsx` quote guarded exactly the constant this changed, so it became a `quote:before:` snapshot at this acceptance. Decisions this acceptance records:

- **The lifetime grows with the text, 2.2 s the floor for the short answers the handoff drew** — the orchestrator's ruling during the night. `docs/design/HANDOFF.md` («Тосты») and `.impeccable/design.json` were updated with it so the handoff and `DESIGN.md` do not disagree; the owner may reverse it. The 60-character threshold, 50 ms a character (20 characters a second) and the 10 s cap are the worker's, written in [08-ui.md](../../reference/08-ui.md).

**Verification.** Worker records of run 0923, each on a clean detached gate worktree under the lock: `gates-ui2.json` on `3b952a5` (batch 1, this task's first commit) — gates 7, green 6 (`test` 845/845, `test:bun` 845/845, `test:ui` 127/127, `perf` update 277/290/278 ms; red: `backslop lint` on this card's quote and DA-37.2's); `gates-ui2-2.json` on `da3fba0`, the whole track — gates 7, green 6 (`test` 853/853, `test:bun` 853/853, `test:ui` 151/151, `lint` 0, `typecheck` 0, `perf` update 299/304/298 ms; red: `backslop lint`, five errors, none from code — three card quotes this acceptance turned into `quote:before:` and two mentions of DA-55.8, whose card is on `main`). Mutation probe at `3b952a5`, tree clean before and after: `over * PER_CHARACTER_MS` → `over * 0` → `e2e/keyboard.spec.ts` exit 1, 13 of 14, red "a long refusal stays on screen longer than a short answer"; `tests/ui-toast.test.ts` exit 1, red "a toast's lifetime > grows by 50 ms a character past sixty, and stops at 10 s". Review: two isolated passes over the track — critical 0 in both; this task's notes (the handoff disagreeing with `DESIGN.md`, the session name's length) closed in `5494ec6`. Evidence: `evidence-ui2-b1.md`, `evidence-ui2-final.md`.

**Integration into `main`.** `3b952a5 14d46b7 5494ec6` cherry-picked without commit onto DA-37.2's commit and squashed into one commit. Git merged `CHANGELOG.md`, `08-ui.md` and `.impeccable/design.json` with no conflict; `CHANGELOG.md` was then set by hand as for every task of the track: `main`'s file, and the branch's final entry once, at the top of Unreleased / Fixed, in the branch's layout.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (the lifetime, its numbers and whose decision it is), `docs/design/HANDOFF.md` («Тосты»), `DESIGN.md`, `.impeccable/design.json`, `CHANGELOG.md`, `### Fixed`.
