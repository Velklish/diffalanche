# DA-114 · Result

**Closed 2026-09-23.** Completed: all five entries are fixed, and one verification line of DA-76.3 is closed with a reason instead of being met. One line per entry:

- **DA-76.3** — fixed. `parseDiff` returns `{ files, notes }` and `PatchOptions.notes` is gone; `readOne` destructures both and puts the notes ahead of the untracked warnings, as before, and `readUntracked` takes `.files`. The entry's verification line "a caller that does not take the notes out of `parseDiff` does not compile" cannot hold for any return shape and is closed with that reason: `parseDiff(raw).files` compiles and is the right call for a caller of one file (`readUntracked`). What stopped compiling are the calls that took the result for an array — `bun run typecheck` refused the old call sites in `tests/change-set.test.ts` and `tests/git.test.ts` with TS2488 and TS2339 until they were changed. Internal contract only: the package ships the CLI and has no `main` or `exports`. Test: `tests/change-set.test.ts` › parseDiff › "returns the half of a type change it could not list with the files, not into a sink".
- **DA-85.1** — fixed. New `WatcherOptions.onWalk`, called once after every tree is ready when a tree polls from construction — the probe said the runtime cannot recurse, or `watch` refused a tree — and not for an explicit `recursive: false`, the decision the entry left open: the walk was asked for, and a line would tell the operator what they chose. `serve` prints "a recursive watch was not available: trees are walked on a timer, and updates are slower", the takeover line is not said after it, and nothing is rescanned. Tests: `tests/watcher.test.ts` › "a watcher that walks from the start", three verdicts, two of them added for review #2.
- **DA-61.1** — fixed. A repository read is five git processes wherever the cost is named; `grep -rn "four git processes" src/ tests/ docs/reference/` exits 1, and so do "instead of four", "the four a rescan" and "the four of the rescan". The 190 ms figure stays, with the reason now in `02-git.md`: the fifth process starts beside the base resolution in one `Promise.all`. The comment blocks this touched were brought to two lines (ADR-011).
- **DA-67.1** — fixed. `reply`, `resolve` and `reopen` go through `updateSession` and take the scope from `draft.review` inside the lock, with no extra read. Test: `tests/lock-writers.test.ts` › "a thread answered while the scope widens" › "is found by %s" for all three. The entry's `quote:` block guarded exactly the code this changed, so it became a `quote:before:` snapshot at this acceptance.
- **DA-99.1** — fixed. `exists()` and `readText()` word `EACCES`, `EPERM` and `ENOTDIR` as a `StorageError` ("could not be read: …"); `EISDIR` is left out on purpose, so the "exit code 2" describe of `tests/cli.test.ts` keeps a case that reaches it. Test: that describe › "is not what a file where a session directory should be gets: that is one line and 1".

**Verification.** Mutation probes by the worker, each after its commit, `git status --porcelain` empty before and after: `patch.ts` returning `notes: []` reddened the parseDiff verdict; `reply` reading the scope before `updateSession` again reddened "is found by reply"; `readError` bypassed in `exists`/`readText` reddened the ENOTDIR verdict; `options.onWalk?.()` removed and `options.recursive !== false &&` removed each reddened the walk describe; on `a60440e`, `const walking = options.recursive !== false;` reddened "says nothing when every watch it built is working" (exit 1, 1 failed of 3; unmutated exit 0, 3 passed). Verdict names base → branch tip: `vitest list` 562 → 575, none removed; Playwright `--list` 95 = 95. Worker gates on the branch tip `a60440e`: `bun run lint` exit 0 (189 files), `bun run typecheck` exit 0, `bun run test` exit 0 (643/643), `bun run test:bun` exit 0 (643/643), `bun run test:ui` exit 0 (95 passed), `backslop lint` exit 1 on one error, the DA-67.1 quote closed here; `bun run perf` not run, declined by its own load guard at 8.07 per core. Review: one isolated pass over `5a5f8e8..183f463`, critical 0, major 2, minor 5; this batch's share — #2 and #4 (`onWalk` and its line), #5 (`04-domain.md` on where the comment writers read the scope), #6 (`03-storage.md` naming the reads it words) and #7 (the DA-76.3 verification line above) — was closed in `2eca91d` and `a60440e`. **Integration into `main`:** `4f688e4`, `95602d7`, `21371ee`, `f2edf09`, `2d9c6cc`, `1aae345`, `183f463` and the review fixes `2eca91d` and `a60440e` cherry-picked as one commit onto DA-55.5's, the last of the track; `CHANGELOG.md`, `docs/reference/02-git.md`, `03-storage.md`, `07-server.md`, `src/core/watcher/index.ts` and `tests/change-set.test.ts` auto-merged without a conflict, and outside `docs/backlog` and `docs/archive` the merged tree is byte-identical to the branch tip (`git diff a60440e HEAD` empty, `CHANGELOG.md` included). Integration gates, once for the whole track, on `7209c48` — this commit before these numbers were written into its `result.md`, the only difference: `backslop lint` exit 0 (no errors), `bun run lint` exit 0 (189 files), `bun run typecheck` exit 0, and in one hold of `/tmp/da-perf.lock` at a load average of 25–40 on 8 cores, `bun run test` exit 0 (40 files, 643/643), `bun run test:bun` exit 0 (40 files, 643/643), `bun run test:ui` exit 0 (95 passed). `bun run perf` was not run: its own load guard declined, "load average 21.35 over 8 cores is 2.67 per core, ceiling 2.5 before the run". No perf number exists for this track — neither the worker nor the acceptance found the machine under the ceiling.

**Documentation in the same pass.** `docs/reference/02-git.md` (five processes, and why the 190 ms stays), `03-storage.md` (the reads that word an errno and the two that do not), `04-domain.md` › "Reading comments back", `05-watcher.md` (`onWalk`, both causes, and the exemption for `recursive: false`), `06-cli.md` (the worded read refusal), `07-server.md` (five processes); `CHANGELOG.md`, `### Fixed`.

**Filed, not closed.** [DA-99.2](../../backlog/minor/DA-99.2-config-and-listing-reads-rethrow-raw-errno.md), `minor`, parent DA-99.1, scope 03-storage: `config.json` and the `reviews/` listing still rethrow a raw errno, both reproduced at exit 2 with a raw `ENOTDIR`. It waits in `minor/` for the next batch of its scope. Left as found, not filed: `updateComments` in `src/core/storage/index.ts` has no caller in `src/` any more, only tests.
