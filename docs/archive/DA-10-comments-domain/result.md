# DA-10 · Result

**Closed 2026-09-05.** Completed. `src/core/domain/comments.ts`, `anchors.ts`, `counters.ts`, `export.ts`: `addComment`, `reply`, `resolve`, `reopen`, `get`, `list` over the storage write path (`updateSession`), each refusing a missing session with `DomainError("no-such-session")`; ids `c_` + six base36 characters (retried on collision), replies `r_` + one above the highest counter in the thread (a hand-edited thread cannot yield two `r_3`); `captureAnchor` looks the line up in the structured change set (`FileChange.hunks` from core-a's reader), stores `lineContent`, the hunk header, and three lines of context each side, and refuses a line outside the diff naming the nearest hunk and a line comment on an `omitted` file; `side` defaults to `new` on a line anchor and `null` above it; `resolve`/`reopen` refuse any role but `human` and record a `note` as a reply before the status change; derived state `unanswered` / `awaiting` (open threads only) and counters per file, repository, and review with the worst severity over open comments; filters by status (`all` by default), repository, severity, unanswered; markdown export grouped by repository as HANDOFF section 9, with the base written in its argument form (`head`, `branch`, `branch:<name>`, ref) rather than a bare branch name — the bare name cannot tell the three modes apart — and replies quoted line by line. Repository sections, files, and comments are ordered by code point (`src/core/order.ts`) so npm-on-Node and the Bun binary export identically. The fixture generator's anchor context is three lines now. The four remaining `DomainError` codes are documented.

Review by an isolated session, two rounds over this task: round two found one major (`localeCompare` in the export, which reordered real fixture data) and three minor (a multi-paragraph reply fell out of its block quote; a missing session answered four different ways; the in-lock `createSession` loser surfaced a StorageError) — fixed in `4a4d834`. Storage follow-ups accepted with this task: `updateSession` as the single write path, the stale-lock takeover verified by token after the rename, ENOENT during acquisition treated as a failed attempt, race tests ordered with gates (`1fe4086`, `4d27077`).

**Verification.** Gates on the worker branch at `4a4d834` and on `main` after integration: `backslop lint` no errors; `bun run lint` 71 files; `bun run typecheck` clean; `bun run test` 135 tests in 15 files (comments: all four anchor levels round-trip through storage with the right nulls; anchor capture checked against an independent parse of `git diff -U3`; `resolve`/`reopen` by an agent refused and the comment unchanged; `unanswered`/`awaiting` flip on every message and both clear on resolve; the export snapshot `tests/snapshots/export-small.md`; the reviewed fixture repository byte-identical after every write; two concurrent creates of one name → exactly one succeeds, the loser carries `session-exists`). `bun run perf` green (36.9 ms / 0 / 7.2 ms / 6.8 ms / 7.5 ms at load 13.7). Mutation probes after commit: role check removed, context 3→2, worst severity over all instead of open, export without replies, `localeCompare` restored, first-line-only quote, session check dropped from `list` and `get`, in-lock catch disabled — each fails at least one test.

**Documentation in the same pass.** `docs/reference/04-domain.md` (comments, anchors, counters, export, the export-base deviation from the handoff, the nine error codes), `docs/reference/03-storage.md` (`updateSession`, the takeover guarantee), `docs/GLOSSARY.md`, `scripts/synth.ts`, `CHANGELOG.md`.
