# DA-59 · Result

**Closed 2026-09-24.** Completed. The second `GET /api/repos/branches` registration is gone, every export in the repository has an importer or a reason to stay public, and a test keeps the count at zero. Decisions this acceptance records:

- **The standing check is a test, `tests/exports.test.ts`**, because Biome 2.5.12 has no rule for an export nothing imports. It runs inside `bun run test` and `bun run test:bun`, both already in `gates` in `backslop.json`, and in the CI `check` job's `bun run test` step, so it has no gate line of its own and `ci.yml` is unchanged. It reads the import and re-export graph of every TypeScript file git would commit with tree-sitter, the symbol index's runtime (`tests/helpers/typescript-syntax.ts`), because TypeScript 7.0.2's package root exports only its version. A name a barrel re-exports needs an importer of its own — the first review pass found that a barrel hid 11 dead exports — and the sweep converged in two passes, a third finding nothing.
- **Entry points are roots:** `bin`, `main`, `exports` and the scripts of `package.json`, a page's `<script src>`, and the plain strings of the top-level `.ts` files of `scripts/`, `e2e/`, `perf/` and the root `*.config.ts`. An entry point's re-exports are its published surface; its own declarations are held like any other. A template string is not read, so `e2e/fixture.ts`, started by a template in `e2e/acceptance.config.ts`, is not found as an entry; it re-exports nothing.
- **`KEPT`, each with its reason in the test:** `useGrammarSource`, `embeddedAssets` and `EmbeddedAsset`, which the entry `scripts/build.ts` generates for the compiled binary imports; `HEARTBEAT_MS`, which the watch track's `tests/events.test.ts` imports; and `ModelFile`, which the ml2 track's DA-41 imports. The last two are dropped by the approver of whichever track of each pair lands second. A reason that stops being true fails the test beside it.
- **The duplicate route:** a test over `app.routes`, method `ALL` aside, fails on a method and path registered twice. A guard goes through `use`, because `app.get(path, guard, handler)` would read as two registrations; [07-server.md](../../reference/07-server.md) says so.
- **What went:** on `d703e72` the check found 74 exports without an importer — 27 of the audit's 30 (three had gained importers since: `WatcherOptions`, `TreeWatcherOptions`, `PREVIEW_LINES`) and 47 it did not name; 71 lost only `export`, `Connection` in `src/ui/live.ts` was deleted, and one re-export statement took two names. The review round's barrel sweep then found 158: 111 names out of barrels, the 10 of `src/ui/index.ts` with the file, 36 exports dropped, and `useGrammarSource` kept. `ModelFile` was made private in the first sweep and is exported again for DA-41. `type WatcherEventType` in `src/core/watcher/bus.ts` was deleted, unused once private.
- **Impeccable `audit` and `polish` were not run**, by the orchestrator's decision: the `src/ui` edits drop `export` only, plus the unused `Connection` type and the barrel `src/ui/index.ts`. The context loader ran on each touched file. `bun run test:ui` compares the shell's screenshots with unchanged baselines, and those two are the suite's only pixel comparisons — they show the empty shell, not the diff cards; the cards and the measure constants are covered by the functional specs only.

No minor entry is left under this task.

**Verification.** Worker gates, clean trees, under `/tmp/da-perf.lock` (`gates-gates.json`, `gates-gates-3.json` in run 0923). On `f789fc1`: gates 7, green 6 — `backslop lint`, `bun run lint`, `bun run typecheck` 0; `bun run test` and `test:bun` 0 (48 files, 728/728); `bun run test:ui` 0 (114/114); `bun run perf` 1, "Update after an edit in one repository" 309 ms against 300. On `76e9bc4`: gates 7, green 5 — `test` 0 (49 files, 740/740), `test:ui` 0 (114/114); `test:bun` 1, 739/740, `tests/watcher.test.ts` › `stops the comment events and leaves the rest of the chain running`, "the watcher never caught up" at load 43, green alone twice on `76e9bc4` and twice on `d703e72`; `perf` 1, the same line at 313 ms. The perf line was accepted by structure: this task changes no executed code on that path — a dropped `export` on a type is erased, on a value it changes only the module's binding, and the removed route was never matched — and the line sat at its budget before this task, 290/299/324 ms at the browse landing (DA-39.1). On `46fb2ad`: `backslop lint`, `lint`, `typecheck` 0; `tests/exports.test.ts` 8/8, `tests/server.test.ts` 36/36. Mutation probes, each red as expected: an unimported export and a removed import (`are each imported by another file, or kept with a reason`), the route registered again (`registers each method and path once`, which is also red on `d703e72` with the duplicate still there, exit 1 twice); a barrel nothing imports (B1) red, the same barrel as a `package.json` script (B2) green, a server-barrel name without an importer (B3) red. `npx vitest list` against `d703e72`: no verdict removed. Review: two isolated passes over the track; the first found the barrel hole (major), the second the `ModelFile` import of DA-41 (major) and an entry-point promise the reference made and the code did not keep (minor); all closed in `0279892` and `46fb2ad`. **Integration into `main`:** `f789fc1 0279892 46fb2ad` squashed onto `d703e72`, with no conflict; the integration gates are in DA-109's result, the commit that lands on top of this one.

**Documentation in the same pass.** `docs/reference/README.md` (Checks that read the code), `docs/reference/07-server.md` (Routes), `CHANGELOG.md`.
