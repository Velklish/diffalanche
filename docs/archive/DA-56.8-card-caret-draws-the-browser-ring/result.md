# DA-56.8 · Result

**Closed 2026-09-24.** Completed. Every keyboard stop the walk reaches draws the system's ring, `acc`. The first walk found the file card's collapse caret and the base picker's `ref` field on the browser's own ring, the two hunk controls and global search's `ещё совпадения` still on `accBd`, an overlay's panel on the browser's ring as it takes the focus from a key, and the search field with no ring at all; each has its `:focus-visible` rule in `src/ui/styles.css` now, and the two containers that hold the ring for what they contain — an overlay's panel and the search field's row — are recorded in `DESIGN.md` and in `.impeccable/design.json` as `container-focus`. `e2e/focus.spec.ts` walks the page, the base picker in each of its modes, the export dialog, global search, the comment form and a reply, the sessions menu with its delete question, select mode and `New task…`, `all files` and browse mode, the scope editor, the footer's `reconnect`, the page with both panels away and the empty screens, in both themes — 18 verdicts. A ring there must be a change from the same control at rest, never `outline: auto`, and `acc`. Decisions this acceptance records:

- **The claim is what was walked** — the orchestrator's ruling during the night: what the walk cannot reach in its fixture (the scope editor's confirmation, the suggestion rows, which never take the focus, the scrim's close target) is listed in [08-ui.md](../../reference/08-ui.md), and the CHANGELOG, 08-ui and the surface brief no longer say "every stop".

**Verification.** Worker records of run 0923, each on a clean detached gate worktree under the lock: `gates-ui2.json` on `3b952a5` (batch 1) — gates 7, green 6 (`test` 845/845, `test:bun` 845/845, `test:ui` 127/127, `perf` update 277/290/278 ms; red: `backslop lint` on two card quotes of other tasks); `gates-ui2-2.json` on `da3fba0`, the whole track — gates 7, green 6 (`test` 853/853, `test:bun` 853/853, `test:ui` 151/151, `lint` 0, `typecheck` 0, `perf` update 299/304/298 ms; red: `backslop lint`, five errors, none from code — three card quotes this acceptance turned into `quote:before:` and two mentions of DA-55.8, whose card is on `main`). Mutation probes, tree clean before and after each, each exit 1: the caret's rule removed at `567e40c` → `e2e/focus.spec.ts` 2 of 4 red ("every stop on the page draws the system's ring", both themes); `.picker-ref:focus-visible` removed at `19e65db` → 3 of 18 red, the two overlay verdicts on `outline auto` in the browser's blue (the third, the scope editor's in light, was a race the probe found and `a81b6d7` fixed). Before the fix all four first verdicts were red. Review: two isolated passes over the track, `660190b..567289a` and `567289a..75b459a` — critical 0 in both; every major and minor closed, this task's in `19e65db`, `a81b6d7` and `fcf4214`. Evidence: `evidence-ui2-b1.md`, `evidence-ui2-final.md`.

**Integration into `main`.** `567e40c 19e65db a81b6d7 fcf4214` cherry-picked without commit onto `1819567` and squashed into one commit. Git merged `.impeccable/design.json`, `08-ui.md` and `styles.css` with no conflict; `CHANGELOG.md` conflicted in `567e40c` and `19e65db` (`main` took DA-36.2, DA-55.7, DA-99.2 and DA-99.3 since `660190b`) and was resolved by hand: `main`'s file, and the branch's final entry once, at the top of Unreleased / Fixed, in the branch's layout. The two `reconnect` verdicts of `e2e/focus.spec.ts` read the footer DA-96.1 adds, so by reading they cannot pass on this commit alone; they do from DA-96.1's commit on. The acceptance's integration gate is on the last of the track's seven commits; each of the seven is typechecked on its own.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (what the walk reaches and what it does not), `DESIGN.md` (a control with no rule, the two containers), `.impeccable/design.json`, `.impeccable/surfaces/src-ui-app-tsx.md`, `CHANGELOG.md`, `### Fixed`.
