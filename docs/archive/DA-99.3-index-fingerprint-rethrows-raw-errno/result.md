# DA-99.3 · Result

**Closed 2026-09-24.** Completed. A `comments.json` the embedding index may not `stat` is a warning in storage's words, not a raw errno at exit code 2. The session is reachable — the listing reads only `review.json` — so a `comments.json` that is a link through a file (`ENOTDIR`) or into a directory without search permission (`EACCES`) stopped `suggest` and `index rebuild`; a probe on Bun and on Node gave the same code to `stat` and to `readFile`, with `error.path` on the link. `fingerprint` now throws storage's `readError`: a refusal it can word marks the session refused, the fast path is not taken, the fingerprint comparison skips that session and its file is read. The read is refused the same way, so the update names the file in its warnings and keeps the entries and the fingerprint it had, as it already did for a broken `comments.json`. A session whose `stat` was refused and whose read was not — the permissions changed between the two — is indexed without a fingerprint and read again at the next update. An error `readError` has no words for is rethrown as before. Nothing was pushed.

**Verification.** Reproduced through the CLI with the model, `suggest --body …` and `index rebuild` in a temporary root with `--data-dir` and `comments.json` a link through a file: on `29e1872` both exit 2 with a raw `Error` and its stack (`at async fingerprint (…/src/core/ml/index/index.ts:45:24)`); on the branch both exit 0 with `diffalanche: …/reviews/alpha/comments.json: could not be read: a file is in the way of one of its parents; its comments were not indexed again`. Worker records of run 0923, on a clean detached gate worktree under the lock: `gates-ml4-2.json` on `4d41f5c`, after `backslop init` — gates 7, green 5 (`test` 861/861, `test:bun` 861/861, `test:ui` 121, `lint` 0 on 251 files, `typecheck` 0; red: `backslop lint` on this entry's `stat` quote only, which this acceptance turned into `quote:before:`, and `perf` "Update after an edit in one repository" 319 ms: 290/321/319, measured alternately against `29e1872` with no difference at this resolution — base medians 293 / 300 / 290 ms, branch 286 / 315 / 279 ms, DA-110). Four mutation probes on `4d41f5c` over five suites (94 verdicts), tree clean before and after each, every one exit 1: the raw `throw error` back → 91 passed; the fast path taken with a refusal → 93; the fingerprint compared for a refused session → 93; a fingerprint kept for a refused session that was read → 93. Verdict names `29e1872` → `4d41f5c`: none removed, three of the eight added are this task's, one of them skipped under root. Review: one isolated pass over `29e1872..bfddbc9` — the minor finding on this task, a `null` fingerprint for a file just read when its `stat` was refused, closed in `4d41f5c`. Evidence: `evidence-ml4.md`.

**Integration into `main`.** `bfddbc9 4d41f5c` cherry-picked without commit onto DA-36.2's commit and squashed into one commit. No conflicts: git merged `03-storage.md`, `09-ml.md` and `tests/embedding-index.test.ts` with DA-36.2's text. The entry's `stat` block turned from `quote:` into `quote:before:` — it quotes the code this task changed. `CHANGELOG.md` checked by hand — the entry once, under Unreleased / Fixed, the layout kept; nothing corrected.

**Documentation in the same pass.** [03-storage.md](../../reference/03-storage.md), [09-ml.md](../../reference/09-ml.md), `CHANGELOG.md`.
