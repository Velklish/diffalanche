# DA-40.1 · Result

**Closed 2026-09-24.** Completed in the shape that strands a window; the stale menu row is filed. A window showing a task by `?review=` that is deleted elsewhere — by the CLI or by another window — answers `no-such-session` by going where `current` points, as the window that deleted it does: it reads the history again, says the server's sentence in the toast with `— окно перешло на current`, and replaces the address, so `Back` does not lead to a task that cannot be shown. Only a task the window was showing moves it: an address whose name never opened keeps its failure screen and its way back, and a press on a stale menu row lands on the screen that names the task, without the `?review=` toast and without `switchSession` claiming the switch. A window on `current` that finds `current` gone still fails — it has no task of its own to leave. The card's `store.ts` quote still matches. The finding [DA-40.2](../../backlog/minor/DA-40.2-menu-keeps-a-task-deleted-elsewhere.md) stays in `minor/` with its Scope filled: a sessions menu open in another window keeps the row of a task deleted elsewhere until it is opened again, since the watcher has no frame for a session that disappeared. The watcher's baseline gap its Evidence also names — a delete that is the first burst after a window subscribed sends no `session-changed` — is [DA-55.8](../DA-55.8-first-write-after-a-window-opens-is-baselined/task.md)'s, which the core3 track closes; until it lands, `e2e/history.spec.ts` writes a comment and waits for it in the rail before it deletes. Decisions this acceptance records:

- **The window whose own task is gone moves to `current`; a frame for a session that disappeared is DA-40.2's, not this track's** — the orchestrator's ruling during the night. The toast's wording, the replaced address and the case of a window on `current` are the worker's, written in [08-ui.md](../../reference/08-ui.md).

**Verification.** Worker record `gates-ui2-2.json` on `da3fba0`, the whole track, on a clean detached gate worktree under the lock — gates 7, green 6 (`test` 853/853, `test:bun` 853/853, `test:ui` 151/151, `lint` 0, `typecheck` 0, `perf` update 299/304/298 ms; red: `backslop lint`, five errors, none from code — three card quotes of other tasks this acceptance turned into `quote:before:` and two mentions of DA-55.8, one of them in DA-40.2, which resolve on `main`). The worker's series caught this task twice: on `4c3279d` `test:ui` went red on `e2e/scope.spec.ts > a task the data directory has not names itself and offers a way back` — the first cut moved a typed name that never opened — fixed in `e5e4292`; on `fcf4214` a full run went red once on `e2e/history.spec.ts > a task deleted elsewhere takes a window on it to the current one` — the baseline gap above — fixed in the spec by `8760259`. Mutation probes at `ed859d4`, tree clean before and after each, each exit 1: the condition without `session?.name === shown` → `tests/ui-deleted-task.test.ts` 2 red ("answers a press on its stale row with the screen that names it, not a claimed switch", "leaves a typed address of a task that never opened on its failure screen") and `scope.spec.ts -g "names itself"` red; `=== shown` → `=== "never"` → the unit's "takes a window on it to current, and says which task went" and `history.spec.ts -g "deleted elsewhere"` red. Review: two isolated passes over the track — critical 0 in both; this task's notes closed in `ed859d4`, `8760259` and `da3fba0`. Evidence: `evidence-ui2-final.md`.

**Integration into `main`.** `4c3279d e5e4292 ed859d4 8760259 da3fba0` cherry-picked without commit onto DA-96.1's commit and squashed into one commit. Git merged `CHANGELOG.md`, `08-ui.md` and `src/ui/store.ts` with no conflict; `CHANGELOG.md` was then set by hand as for every task of the track: `main`'s file, and the branch's final entry once, at the top of Unreleased / Fixed, in the branch's layout.

**Documentation in the same pass.** [08-ui.md](../../reference/08-ui.md) (a task deleted elsewhere, what a window on it does, what DA-40.2 and DA-55.8 leave), `CHANGELOG.md`, `### Fixed`.
