# DA-101 · Result

**Closed 2026-09-18.** Completed. A hunk with no line numbers on the side being asked about is no longer infinitely far away: `distanceTo` returns `null` and `nearest` skips such hunks. The "this file has no hunks" branch is decided on `file.hunks.length` and kept, because the case is reachable — `src/core/git/patch.ts:70-91` returns `hunks: []` with `omitted: null` for a change set read without hunks and for a change of nothing but a file's mode. The third case now has a refusal of its own: `repos/core/cargos-api/src/gone.ts is deleted and its hunks have lines on the old side only, so line 2 cannot be anchored on the new side; anchor it on the old side`. Of the task's two wordings the first was taken: it names the side that carries the lines and the side to retry on, where the second ("stay generic and name the nearest hunk measured on the side that does have numbers") answers about a place nobody asked about. The `--side` flag was deliberately kept out of the text: `addComment` is called by the server too, where the side arrives as a body field.

**Verification.** Three new cases in `tests/comments.test.ts` — a deleted file with side `new`, an added file with side `old`, and a file with no hunks at all. Mutation probe by the worker on `342d6b0`: the call site returned to its old ternary on `closest === null` → exit 1, 19/21, both directions red on the assertion about the text rather than about the exit code. A second probe on the same commit — `distanceTo` reverted to `+Infinity` — came back **green** (exit 0, 21/21), and that is recorded as it stands: the behaviour is carried by moving the branch at the call site, while the `nearest` edit buys readability no test can tell apart, because a strict `<` against a `bestDistance` starting at `+Infinity` never selects such a hunk anyway. Tree clean before and after both probes. Worker gates on `a1734c7`, clean tree: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run lint` exit 0 (176 files), `bun run typecheck` exit 0, `bun run test` exit 0 — 477 tests over 35 files. Integration into `main`: `342d6b0` cherry-picked onto the DA-83 commit, plus the `tests/comments.test.ts` half of the review commit `a1734c7` (its `docs/reference/04-domain.md` half belongs to DA-83); `CHANGELOG.md` merged by entry. Gates on the integration commit: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, `bun run typecheck` exit 0; the full six-gate run of the merged tree is recorded on the last commit of this track (DA-88).

**Documentation in the same pass.** `docs/reference/04-domain.md` — the third answer is described beside the existing promise about the nearest hunk, with its exact wording, where it is reachable from the command line (`comment` anchors on side `new` by default), and which file keeps "has no hunks". `CHANGELOG.md`, `### Fixed`. Review moved the helper `oneSided` in `tests/comments.test.ts` below `mixedSides`: it had landed between `mixedSides` and its own docblock, which left that comment reading as the wrong function's.
