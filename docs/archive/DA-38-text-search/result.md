# DA-38 · Result

**Closed 2026-09-23.** Completed. Global search finds text in the working trees: a query of two characters or more also goes to `GET /api/search/text`, which runs `git grep -n -z -I -F -i --untracked` over every repository of the review, inside the task's scope, and answers a page of hits with five lines on each side; the rows carry a `text` tag after the ranked ones, the preview shows the line between its neighbours, and `⏎` opens the file in browse mode (DA-37) at that line. Decisions this acceptance records: the query is a fixed string with case folded, over tracked and untracked files, ignored and binary ones out; 50 hits a page with `next`, the next page asked once and asked again after a failed read; at most three lines of a file and 500 hits in all, git stopped at the first hit past the cap; a file that exhausts its share — 100 of its lines passed over — is set aside, git runs again without it and the search goes on after it, up to twenty such files; `capped` is said on its own row under the last page (`совпадений больше, чем показано, — уточните запрос`). No minor entry is left under this task.

- **Found at integration and fixed in this commit.** The palette's field took the focus through an inline callback ref, which React calls again on every render; before DA-38 the palette rendered only on a keystroke, and since text and symbol results land later, each landing took the focus back from the hit the keyboard was on. Main's `e2e/keyboard.spec.ts` › `the highlighted search hit changes colour when the keyboard is on it` (DA-56.7) caught it in the integration gate on `f9691e9` — Expected `rgb(139, 154, 224)`, Received `rgb(221, 225, 231)`, the field's text colour, no ring; green alone twice — and a probe that waits 1.5 s on the hit ended with the focus in the `search` field on `f9691e9` (exit 1) and on the hit on `3b338a9` (exit 0). The ref is now a stable `focusField`; `e2e/search-text.spec.ts` › `the hit the keyboard is on keeps the focus when the text hits land after it` holds the text answer until the keyboard is on a hit: on `f9691e9` without the fix exit 1 (the hit loses the focus to the late renders before the answer is released), with it exit 0 twice.

**Verification.** Worker gates on the track tip `a572e79`, clean (`gates-browse-4.json`): gates 7, green 6 — `backslop lint` 0, `bun run lint` 0 (206 files), `bun run typecheck` 0, `bun run test` 0 (44 files, 678/678), `bun run test:bun` 0 (678/678), `bun run test:ui` 0 (104/104); `bun run perf` 1, declined at 3.84 per core. On `89845f0`, the task's first commit, all seven gates were green, `perf` measured: first render 103.5, switching 71.3, update 284 ms (medians of 3). Mutation probes: on `89845f0` the per-file cap in `src/core/git/grep.ts` removed turns red `git grep over one working tree keeps a few lines of each file, and stops at the limit saying there was more`, 1 of 8; on `7a39004` a file never set aside turns red `git grep over one working tree sets aside a file of one word repeated, so a file after it is still read`, 1 of 9. `tests/search-text.test.ts` holds `.git/index` and `HEAD` byte for byte across a search. Review: the two isolated passes of the track (see DA-37's result); findings #4 and #9 of the first pass closed in `a2f484a`, the second pass's page-guard finding in `a57a39a`. **Integration into `main`:** `89845f0 a2f484a a57a39a` squashed onto DA-37's commit. `CHANGELOG.md` conflicted at the head of `### Added` again and was resolved by hand with both sides kept; everything else merged cleanly. The fix of the focus defect above is the one change of product code this acceptance made, allowed by the orchestrator of run 0923 for that defect and this commit only. The integration gates and the comparison with `a572e79` are in [DA-37's result](../DA-37-browse-repo/result.md): all exit 0 on `68c1468`, `bun run test:ui` 114/114 with the new test among them.

**Documentation in the same pass.** `README.md`, `docs/reference/02-git.md` (Text search), `07-server.md` (Text search), `08-ui.md`, `CHANGELOG.md`.
