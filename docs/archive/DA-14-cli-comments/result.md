# DA-14 · Result

**Closed 2026-09-05.** Completed. `list [--status open|resolved|all] [--repo] [--severity] [--unanswered] [--json]` (default `open`; the table `id · severity · status · where · author · first line`, or a bare `Comment[]`; `--repo` is checked against the `repo` values the session's comments carry, never the file system, so a renamed or removed repository still lists), `show <id> [--json]`, `reply <id> --body <text|-> [--author] [--role]` (`-` reads stdin; the first word of the output is the new id), `comment [--repo R] [--path P] [--line N] [--end-line M] [--side new|old] --severity S --body <text|-> [--author] [--role]` (the anchor from the change set with the named repository read again first, so a comment right after an edit points at the line that is there now; `--repo` is checked against the root before any read or write; without `--repo` the comment is review-level — an extension of the SPEC section 8 table), `resolve <id> --role human [--note] [--author]`, `reopen <id> --role human [--note] [--author]` (`--note` on `reopen` is a second extension; any other role is refused by the domain and changes nothing), `export [--status open|all] [--format md|json]` (`md` is `exportMarkdown` verbatim, `json` is `{ review, comments }`). Defaults `--author agent`, `--role agent`; a whitespace-only body is refused; every write goes through the domain and its lock, so two CLI processes and the UI interleave without losing a message.

Review by the same isolated reviewer over rounds two and three: nine plus seven small items (a failed command rewrote `diff.json`; docs claiming the server used `scanReview`; the change-set section placed under 02-git; duplicated value lists; a fallback id string; the Node floor; shared fixture roots) — all closed in `30f7700`, `19e1dd1`, `077233f`.

**Verification.** Gates as for DA-13 (175 tests; `tests/cli-comments.test.ts`: `comment` on a line stores `lineContent` equal to the edited text; `list --unanswered --json` returns a human's comment and an agent `reply` takes it out; `resolve` without `--role human` exits 1 with the comment untouched; `resolve --role human` takes it out of `open` and writes `--note` into the thread; `export` byte-equal to `exportMarkdown` for `open` and `all`; the four anchor levels; `--body -` from stdin; a severity that is not one; a line the diff does not have (exit 1, nothing written, `diff.json` bytes unchanged); `show`; an unknown id; `reopen`; two `reply` processes started with `Promise.all` on two comments both land — the test skips below Node 22.18 because it starts the CLI from TypeScript source). Mutation probes after commit: `comment` skipping the refresh → its test fails; `verdict` hard-coding the human role → its test fails; `list` defaulting to `all` → its test fails; the `list --repo` check removed → its test fails. Measured on the full synthetic review: the extra repository walk in `comment` is 3 ms next to a 145 ms git read, 230 ms for the whole command.

**Documentation in the same pass.** `docs/reference/06-cli.md`, `README.md`, `CHANGELOG.md`.
