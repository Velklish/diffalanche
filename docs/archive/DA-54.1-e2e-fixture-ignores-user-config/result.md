# DA-54.1 · Result

**Closed 2026-09-19.** Completed. `fixtureEnv()` in `src/core/config/index.ts` is now the one place the two lines `vitest.config.ts` already carried live: `DIFFALANCHE_DATA_DIR` empty, which counts as unset, and `XDG_CONFIG_HOME` at a directory nothing writes to. `vitest.config.ts` puts it in `test.env`; `e2e/playwright.config.ts` and `e2e/acceptance.config.ts` put it on their own `process.env`, because the fixture server is not the only reader — a spec that checks what reached the disk shells out to the CLI from its worker, and a worker still carrying the developer's config looks for the session elsewhere; `perf/gate.ts` hands it to every child as an explicit `env`. The acceptance configuration was in scope beyond the card's text: it has the same defect and the card's verification names `bun run test:e2e`. The readiness probe of both Playwright suites moved from `/api/review` to `/`: the review route answers 404 for exactly as long as the data directory is resolved elsewhere, so Playwright polled it for two minutes and then reported a timeout that named the wait and hid the cause, while `/` is up as soon as the server is; `e2e/server.ts` now prints the data directory it resolved. Two specs that followed `DIFFALANCHE_DATA_DIR` by hand — the workaround this card owns — name the directory with `dataDirOf` instead, because an empty string is not nullish and their `??` had been silently dropping `.diffalanche` from the path the moment the variable was set to `""`. **The `perf` gate had a second half, found by running the card's own verification:** Bun hands a child process the environment the process *started* with, so the assignment `perf/gate.ts` made to `process.env` reached nothing and a repetition still resolved from the user config — `no current review session` over a fixture that had one. Measured both ways (`node`: the child sees it; `bun` 1.3.14: it does not) and written into `docs/reference/11-perf.md`. The serialising wrapper of the run had hidden this by exporting the same variables in the shell.

**Verification.** With `{ "dataDir": ".agents/diffalanche" }` in `~/.config/diffalanche/config.json` and no variable on the command line: `bun run test:ui` 87 passed exit 0, `bun run test:e2e` 11 passed exit 0, `bun run perf` reaching the fixture and reporting budgets. The second condition — a machine with no user config — is the same run after the fix, because the resolution no longer reads that file at all; shown directly by a probe that resolves the data directory under the real `HOME` and under a `HOME` with no diffalanche config, printing `<fixture>/.diffalanche` in both, against `<fixture>/.agents/diffalanche` before the change. The owner's file was not moved. Mutation probe by the worker on `2604c1d`: `src/core/config/index.ts:76`, the fixture's configuration home replaced by the developer's own — `bunx playwright test … e2e/scope.spec.ts` exit 1, 0 of 7 passed, 7 reddened including the expected `a task carries its scope and nothing else, and the pill counts it`; restored, exit 0, 7 passed; tree empty before and after. **Integration into `main`:** `238fba7` and `2458943` cherry-picked onto `1c30b66`; the only conflict was `CHANGELOG.md`, where the branch opened a `### Fixed` section that `main` already had — the entry was placed at the head of the existing list and nothing of `main` was dropped. Every other file auto-merged, and `vitest.config.ts`, `perf/gate.ts`, `e2e/acceptance.config.ts`, `e2e/playwright.config.ts`, `e2e/server.ts`, `src/core/config/index.ts` and `docs/reference/11-perf.md` are byte-identical to the track head `8bdec56` afterwards. `src/core/config/index.ts` was expected to carry a hunk of the server track as well; it does not — `git diff 5fdaed9 1c30b66 -- src/core/config/index.ts` is empty, so nothing of another track was displaced here. Gates of the merged tree: see DA-55.4's `result.md`.

**Documentation in the same pass.** `docs/reference/08-ui.md` (UI tests), `docs/reference/11-perf.md` (a new "The fixture's environment" section with the Bun/Node table), `CHANGELOG.md` under Fixed.

**Not covered.** The user config's `dataDir` is still resolved against the root for a real root, which is DA-52 and out of scope here.
