# DA-34.1 · Result

**Closed 2026-09-24.** Completed. The server's embedder runs on a `node:worker_threads` thread, not on its event loop: `startThreadedEmbedder` (`src/core/ml/embed/threaded.ts`, `worker.ts`) answers with an `Embedder` of the same shape and gives the same bytes as the calling thread. Decisions this acceptance records:

- **A worker thread, by measurement.** With the index rebuilt in a loop on the server's own thread, "Update after an edit in one repository" took 422–435 ms against 272–297 ms without embedding — over its 300 ms budget, because every step of a rescan waits out the run in progress; on a worker thread it took 285–344 ms. Switching sessions stayed inside 100 ms in all three modes. Three runs of each, alternating, on 2026-09-23 at load averages of 7.7–15.7 on 8 cores; `perf/run.ts --embedding <main|worker>` and `--lag` take the measurement again ([09-ml.md](../../reference/09-ml.md#in-the-server)).
- **The thread is held only while it loads or works**, so an idle one keeps no process alive; `close()` holds it again until it has gone. An `error` of the thread — its module not found, the native runtime refusing to load — is a `ModelError`, not an uncaught exception in the process that started it; from DA-35 the route answers 503 while the process lives, `ReviewServer.close` closes the thread, and a dead one is started again by the next request.
- **`terminate()` is never called**: it crashes Bun 1.3.14 with `onnxruntime-node` loaded (`NAPI FATAL ERROR`, 2 runs of 2). The thread exits from inside with `process.exit(0)`, which ends a worker and not the process.
- **The npm bundle carries no runtime and no worker file**: `build:cli --external '*embedder.ts'`, which landed with DA-34's commit, took `onnxruntime-common` in `dist/cli.js` from 19 occurrences to 0, and `index rebuild` from it refuses in one line. DA-41 lifts that flag when it delivers the runtime.

**Verification.** Worker records of run 0923 (`gates-ml2.json`, `gates-ml2-2.json`, `evidence-ml2-batch1.md`, `evidence-ml2-round1.md`). On `2bad563`, clean, under `/tmp/da-perf.lock`: gates 7, green 6 — `bun run test` 690/690, `bun run test:ui` 104/104, `bun run perf` ok with the update at 276 ms (median of 266/276/287); `bun run test:bun` red on the watcher's "stops the comment events and leaves the rest of the chain running" — the same verdict reddened on the gates tree without a line of this track's code (the orchestrator's ruling; the worker's series had it 6 of about 20 full runs on the branch and 0 of 18 on the base, mechanism not found). On `bacb603` the track's gates are in [DA-34's result](../DA-34-embedding-index/result.md). Mutation probes: the worker embedding `text + " "` (`worker.ts`) reddened the one expected verdict of 19; on `bacb603`, the thread's `error` listener renamed to an event that never fires (`threaded.ts:36`) reddened exactly the two expected verdicts of 10 in `tests/suggest.test.ts` — "is refused as a model that is not there, and the process lives on" and "answers GET /api/suggest with 503 and the reason". Review: the isolated reviewer's critical — a thread that failed to load threw in the main thread and took `serve` down — was closed in `fb176b4`.

**Integration into `main`.** `2bad563 fb176b4` squashed onto DA-34's commit. Conflicts: `perf/harness.ts` — `Options` without the `export` DA-59 dropped and with this task's `lag` field; `tests/embedding-index.test.ts` and `tests/embedding-model.test.ts` — the thread's tests as `764523c` placed them, first in the model file, without DA-35's; `09-ml.md` — the thread's sentences of "Tests" and "What is not built" restored, and two forward references to DA-35's route and server left to its commit; `perf/index-scale.ts` as `6f50bc1` has it. `tests/helpers/throwing-worker.ts` lands here with `fb176b4`, and DA-35's test is its first user. Integration gates on `8948f48` — the three commits before these paragraphs were added — clean: `backslop lint`, `bun run lint` (236 files) and `bun run typecheck` 0; under `/tmp/da-perf.lock`, 23:01–23:05Z, load averages 7.0–23.2 on 8 cores: `bun run test` 0 (52 files, 779/779), `bun run test:bun` 0 (779/779), `bun run test:ui` 0 (114/114); `bun run perf` 1 on one line — "Update after an edit in one repository", 320 ms at the median of 313, 320 and 498 against 300, every other line ok. That line sits at its budget on the base (DA-39.1), and nothing of this track runs on an edit: the index is brought up to date by its reader, the thread starts on the first `/api/suggest`, and `index/` is outside the watcher. `bun run typecheck` on each of the three commits from a detached checkout: 0.

**Documentation in the same pass.** [09-ml.md](../../reference/09-ml.md) (In the server, Tests), [11-perf.md](../../reference/11-perf.md) (`--embedding`, `--lag`, `catch-up --worker`), `CHANGELOG.md`.
