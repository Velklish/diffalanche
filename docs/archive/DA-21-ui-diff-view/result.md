# DA-21 · Result

**Closed 2026-09-05.** Completed. `src/ui/components/FileCard.tsx` over `react-diff-view`: header with collapse caret, path (`old → new` for a rename), the open-comment badge in the worst severity's colour, state chips (`new file`, `deleted`, `renamed`, `binary`, `too large` from `FileChange.omitted`, listed with one line of text instead of an empty diff), counts, and the `split` / `unified` segments remembered per file; hunk headers as `Decoration` rows on `--panel2` with the control that hides and restores the three context lines the bundle carries (per file and hunk in `collapsedHunks`, so one toggle re-renders one card), sticky at the left edge because the row is wider than the card — a documented deviation from the handoff's right-aligned control; slots for the range highlight, the composer row, and inline thread widgets in new-side line numbers; `nearbySequences: "zip"` so split columns line up. `@git-diff-view/react`, the second renderer, `variant.ts`, the query switches, and the eight-entry harness list are removed — ADR-008's consequences are met. Virtualisation stays, with the height estimate now counting the rows a card will have (22 px lines, 26 px hunk headers pinned in CSS): a full scroll moves the page height by 0.2 %. The handoff's `white-space: pre` on the diff cost 2.5 ms of CPU per frame because the library's fixed-layout table spilled unwrapped lines out of their cells and was measured every frame; the card computes the widest line while measuring height and gives the table an explicit width in `ch`, which removes the cost.

Review: round two found one major (every mounted card subscribed to the whole hunk-state object) and two minor (`measurePatch` exported without a consumer; the inverted `expandedHunks` convention) — fixed in `b3bd28d` (`src/ui/measure.ts` with a unit test per rule; `collapsedHunks`).

**Verification.** Gates as for DA-19: 106 unit tests including `tests/diff-height.test.ts`; 18 Playwright tests, six for the diff (same line numbers in split and unified, one horizontal scroll container with two equal code columns, the height estimate not moving the page, a file's view surviving unmount, hunk context collapsing and returning, a card collapsing to its header); `bun run perf` green with the handoff's diff styling in place (7.7 ms CPU per frame). Mutation probes: row height 22 → 18 → the height test fails at 16.7 %; `trimContext` never trimming → the context test fails; `measurePatch` counting both sides in split → its test fails; width keeping the marker → its test fails.

**Documentation in the same pass.** `docs/reference/08-ui.md` (diff view, virtualisation, the width fix, the deviation), `docs/reference/11-perf.md` and `README.md` (no variants), `CHANGELOG.md`, `package.json` and `bun.lock` (dependency removed).
