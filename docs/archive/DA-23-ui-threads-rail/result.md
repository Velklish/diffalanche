# DA-23 · Result

**Closed 2026-09-05.** Completed. `ThreadCard` is one component in two places — the rail and the inline widget under the anchored lines — with every handoff state except `orphaned` and the `auto` / `labelled by` markers (later phases): severity chip, anchor label, `awaiting` / `RESOLVED`, body, replies by role, `Resolve` / `Reopen`, a reply field. The rail has two tabs counting open threads (consistent with every other number on screen), the `unanswered` chip, resolved threads dimmed but listed, and scrolls the focused card into view; focus goes both ways through `reveal.ts` (a card in the rail reveals its widget in three scroll steps; a thread on a line hidden by collapsed context has no widget, marker, or height, and revealing it expands the hunks first). Reply, resolve, reopen go through one `write()` with an optimistic update, rollback of the one comment on refusal, and a `busy` flag per thread, so writes on different threads run in parallel. The marker on a commented line is a class on the row (a clickable gutter element cost 0.9 ms per frame — the task's "clicking a line's marker focuses the card" is superseded; the widget header covers anchor → card). The inline widget lives in the diff's fixed-layout table under `contain: layout paint style`, which is what brought the frame cost back from 10.8 ms to 7.8 ms — a containment that decides between a red and a green gate, not a micro-optimisation. `revealCard` also fixed a reading-position drift that made "This file" show the wrong file after a click in the tree.

Review by an isolated session, two rounds: 8 minor on the first (a locator that never matched, a fixed sleep, hidden-line threads lost silently, rail focus not visible, tab counts, a global `busyId`, DA-22.1 not naming the rail, a duplicated CSS rule) — closed in `e3214d8`.

**Verification.** `tests/ui-threads.test.ts` (10), `tests/diff-height.test.ts` (+3 on `hiddenLines`), `e2e/threads.spec.ts` (7) including the hidden-line reveal and the reading position after a tree click; mutation probes after commit: no rollback → 2 fail; `busy` not blocking the same thread → 1; the reply field closing on refusal → 1; `measureThreads` at 0 → the height e2e fails; `revealCard` scrolling once → the current-file e2e fails. Perf on a quiet machine at `fce3761`: 7.8 / 7.8 / 9.0 ms per frame, 0 long tasks.

**Documentation in the same pass.** `docs/reference/08-ui.md` (threads, the marker, the sticky hunk header deviation), `docs/backlog/triage/DA-22.1` widened, `CHANGELOG.md`.
