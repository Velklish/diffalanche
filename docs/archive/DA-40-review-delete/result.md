# DA-40 · Result

**Closed 2026-09-24.** Completed. `diffalanche review delete <name> --role human [--yes]`, `DELETE /api/sessions/:name` and a `Delete` action in the sessions menu remove a review session with its comments. Under the session's lock the directory is renamed to `<dataDir>/.deleted-<uuid>` in one step and then removed; nothing brings it back — every writer of a session is under `withLock`, and a writer that waited on the lock of a deleted session gets `no such review session`, one `NoSuchSessionError`, which the server answers 404 `no-such-session`. Deleting the current session moves `current` to the remaining session updated last, or clears it, and checks again after the write. The embedding index drops a deleted session at its reader's next update. The server forgets a held document whose session a burst of the data directory no longer lists, or lists with another `createdAt` — also after a delete from the CLI. In the menu the question takes the row's place: the count of comments, `Отмена` focused, `Удалить` red; a window on the deleted task moves to `current`. Two minor entries stay in `minor/` with their Scope filled: [DA-40.1](../DA-40.1-deleted-task-open-elsewhere/task.md) (a window open on a task deleted elsewhere shows the failure screen, not `current`) and [DA-55.9](../DA-55.9-watcher-moved-verdict-leans-on-the-file/task.md) (the watcher verdict "says which repositories moved while nobody followed the task, after it follows it" fails alone and under load, green inside its file). Nothing was pushed. Decisions this acceptance records:

- **`review delete` refuses a role other than human**, as `review close` does — the orchestrator's ruling during the night: a destructive action is a person's; a person confirms on a terminal, or passes `--yes` without one. Without a terminal and without `--yes` the command refuses; `Ctrl-C` and `Ctrl-D` at the question are a no.
- **The index is left to its next reader** rather than rewritten by the delete — the worker's decision by measurement: the next update after a delete took 1.1–1.9 ms over 200 + 200 comments and 32.7–37.8 ms over 5000 + 5000, and a delete-time hook would make the domain a second writer of `index.bin`.
- **The confirmation is in the row, not a dialog** — the worker's decision: the menu is a popover that closes on a press outside, and the row is the context.
- **Still open, written in [03-storage.md](../../reference/03-storage.md):** `current` is written without a lock, so two deletes of the current session at once, or a delete racing `review use`, leave the last writer's value; a process killed between the rename and the removal leaves a `.deleted-<uuid>` directory that no listing shows.

**Verification.** Worker records of run 0923, all clean: `gates-wave3-4.json` on `8c13bb8` — gates 7, green 6 (`test` 815/815, `test:bun` 815/815, `test:ui` 120; `perf` all lines in budget but "Update after an edit" 311 ms against 300, measured alternately under the lock against `1fff4f6`: base 299 / 294, branch 334 / 292 ms — no difference this resolution can show, DA-110); `gates-wave3-5.json` on `c8fcd5a` — gates 7, green 6 (`test` 818/818, `test:bun` 818/818, `test:ui` 121; the same `perf` line at 311 ms), and on `df50b7e`, which only removed one dead line, `lint` 0, `typecheck` 0, `tests/cli.test.ts` 46/46 on Node and on Bun. The watcher red on `416d156` is DA-55.9: alone on `416d156` exit 1 twice and on `1fff4f6` exit 1 twice with the same assertion, the whole file 45/45 on both. Mutation probes, each reddening its expected verdict: `src/core/domain/sessions.ts:224` and `src/core/storage/index.ts:237` on `8c13bb8`, `src/server/review.ts:302` on `c8fcd5a`, `src/cli/stdin.ts:16` on `df50b7e`; a probe on `c8fcd5a` found the `SIGINT` listener dead, removed in `df50b7e`. Verdict names `1fff4f6` → `df50b7e`: vitest 736 → 750, Playwright 118 → 121, none removed. Review: one isolated pass over `1fff4f6..8c13bb8` — critical 0, major 1 (a delete from the CLI beside a running server left the held document of the deleted task, served again if the name was reused), minor 7 — all closed in `416d156`, `c8fcd5a`, `df50b7e` and read by the orchestrator in the diff. Evidence: `evidence-da40-wave3.md`, `evidence-da40-r1-wave3.md`.

**Integration into `main`.** `8c13bb8 416d156 c8fcd5a df50b7e` cherry-picked without commit onto `21ff73b` (DA-34.2) and squashed into one commit. One conflict, `docs/reference/06-cli.md` in `8c13bb8`, the sentence on what is not written yet: `main`'s text is kept — `model pull` without `--embedding` and `insights` are not written, `model status` and `model pull --embedding` exist — with `review delete` taken out of it, as the branch did. Merged by git without conflict, meeting DA-41, DA-36 and DA-34.2: `README.md`, `docs/SPEC.md`, `07-server.md`, `09-ml.md`, `skills/diffalanche-review/references/cli.md`, `src/cli/run.ts`, `src/server/app.ts`, `src/server/serve.ts` (DA-40's session listing handed to the server beside DA-34.2's model process, in separate hunks of `startReviewServer`), `tests/embedding-index.test.ts`, and `CHANGELOG.md`, whose merge is `main`'s file with exactly the branch's DA-40 entry at the top of Unreleased's Added section. `backslop merge-changelog --ours 21ff73b --theirs df50b7e --base 1fff4f6` reported ours 83, theirs 83, merged 84 and would have broken the file: it dropped the DA-40 entry, added blank lines between entries, and wrote two `backslop:conflict` marks with the DA-35 and DA-34 entries twice; its output was not used. Of the 36 files DA-40 changed outside `docs/backlog`, `docs/archive` and `CHANGELOG.md`, 26 equal `df50b7e`, 9 differ from it only by the lines `main` wrote since `1fff4f6`, and `06-cli.md` by the conflict above; no file outside DA-40's set moved. Integration gates on this tree, clean: `backslop lint` 0, `bun run lint` 0 (249 files), `bun run typecheck` 0; under `/tmp/da-perf.lock` 02:05:31–02:08:41Z, one-minute load 4.8–25.8: `bun run test` 0 (55 files, 842/842), `bun run test:bun` 0 (842/842), `bun run test:ui` 0 (121 passed), `bun run perf` 0 — every line in budget, "Update after an edit in one repository" 283 ms at the median of 286, 269 and 283 against 300. `bun run typecheck` on the final commit from a detached checkout: 0.

**Documentation in the same pass.** `docs/SPEC.md` sections 5, 8 and 9, [03-storage.md](../../reference/03-storage.md), [04-domain.md](../../reference/04-domain.md), [05-watcher.md](../../reference/05-watcher.md), [06-cli.md](../../reference/06-cli.md), [07-server.md](../../reference/07-server.md), [08-ui.md](../../reference/08-ui.md), [09-ml.md](../../reference/09-ml.md), [10-skills.md](../../reference/10-skills.md), `DESIGN.md`, `README.md`, `skills/diffalanche-apply/references/cli.md`, `skills/diffalanche-review/references/cli.md`, `CHANGELOG.md`.
