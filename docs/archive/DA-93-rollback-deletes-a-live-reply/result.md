# DA-93 · Result

**Closed 2026-09-19.** Completed. The third of the card's candidates — `patchThread` marks the thread as touched and `write` skips the rollback when it was overtaken — and it turns out to be not only the smallest but the **correct** one, which is visible only from the code: `patchThread` replaces the whole comment with the server's own read, so by the time the refusal arrives the optimistic draft is already gone. Skipping the rollback therefore leaves exactly the server's truth, not "the optimistic reply on screen until something replaces it" as the card's wording expects. `patched` is a count of live frames per thread, read on both sides of the request, and it lives beside `write` because `write` already takes the optimistic function. The card's third question, answered where it is asked: `patchThread` **does not** respect `busy`, on purpose. A live frame is the server's own read and the store takes it; what the two entry guards beside it protect is a second *write* on a thread already being written, which is a different question — the comment there says so, because as it stood it read like a forgotten third guard.

**Verification.** A case in `tests/ui-threads.test.ts` drives the card's sequence exactly: a reply sent against a fetch that has not resolved, `patchThread` with a server copy carrying the agent's answer, then the fetch rejecting. Afterwards the agent's reply is still there, `r_pending` is not, and `awaiting` is 1. On the worker's tree `bun run test` 479 passed, `bun run test:bun` 479, and `tests/ui-live.test.ts` still passes unchanged, which is the card's other requirement. Mutation probe on `f9f97f0`, tree empty at both ends: the rollback made unconditional again — exit 1, 10 of 11, the expected case red on the missing reply and not on the toast. **Integration into `main`:** `f9f97f0` and `0cb7a46` cherry-picked onto the DA-94 commit with no conflict, plus the `src/ui/store.ts` half of the two-prefix commit `5fbd6d8` — a shortening of this task's own comment, placed by content because that commit carries the prefixes DA-55.4 and DA-94 and neither owns the line. `src/ui/store.ts` and `tests/ui-threads.test.ts` are byte-identical to the track head `8bdec56` afterwards. Gates of the merged tree: see DA-55.4's `result.md`.

**Documentation in the same pass.** `docs/reference/08-ui.md`: "as it was" now has its subject — before **this write**, not before everything since — with the rule about an overtaken write and the paragraph on why `patchThread` has no `busy` guard. `CHANGELOG.md` under Fixed.

**Not covered.** Nothing from the card.
