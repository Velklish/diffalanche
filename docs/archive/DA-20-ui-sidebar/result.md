# DA-20 · Result

**Closed 2026-09-05.** Completed. The sidebar tree is built from the store's change set and open-comment counters: repositories with their files, expand and collapse per repository (`collapsedRepos`), keyboard focus order (filter, repository, its files, with `:focus-visible` on rows), a substring filter over repository and file paths (lower-cased; a repository whose own path matches keeps all its files) with the match count in the field and an empty-result line, selection scrolling the centre panel to the card and setting `repo` and `path`, and the current file following the reading position through one passive scroll listener and a single `elementFromPoint` hit test 120 ms after the scroll settles (an observer over three hundred cards cost too much per frame). Styles per HANDOFF section 1.3: 28 px file indent, `--sel` on the selected file, `--panel3` on the active repository, ellipsis on long paths, the badge in the worst severity's colour. The footer is a static `watching` until DA-25; the `all files` tab is not rendered until DA-37.

Review: round two found three major that belonged to this task (tree unstyled, stale screenshot baselines, no documentation or tests) and round three four minor (07-server.md behind the merge, README's `test:ui` sentence, the count wording, the UI-tests list) — closed in `1424b96` and `ebb4b92`.

**Verification.** Gates as for DA-19; seven Playwright tests for the navigation (the tree lists repositories with changes and no clean worktree; a badge equals the open comments on the file computed from `/api/review`; the filter narrows tree and count and says when nothing matches; choosing a file brings its card into view within 50 ms; the current file follows the reading position; a repository collapses and expands; the keyboard walks filter, repository, files). Mutation probes: filter ignoring the file path → the filter test fails; the scroll never selecting → the reading-position test fails. A/B on the same machine: the tree costs about 15 ms of first render and nothing per frame. Badges are checked against `/api/review` rather than `list --json` (DA-14 did not exist yet); DA-28 switches the check to the CLI.

**Documentation in the same pass.** `docs/reference/08-ui.md` (Navigation section, the UI-tests list), `README.md`, `CHANGELOG.md`, the screenshot baselines.
