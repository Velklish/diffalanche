# DA-39 · Result

**Closed 2026-09-23.** Completed. Global search finds definitions by name: `src/core/ml/symbols` reads the functions, classes, methods and types of TypeScript, TSX, JavaScript, C#, Python, Go, Rust and Java in every repository of the review, and `GET /api/search/symbols?q=` answers the twenty best by a fuzzy match of the name, inside the task's scope; the rows carry the `symbol` tag, the preview is the line that defines it, and `⏎` opens the file at the definition. `grammars` in `config.json` adds a language (`docs/SPEC.md` section 7). Decisions this acceptance records: [ADR-015](../../adr/adr-015-symbol-index-binding.md), **Accepted** by the orchestrator under the task brief because the rules force it — `web-tree-sitter` over WASM, since ADR-008 rules out the native binding; VS Code's WASM grammars from `@vscode/tree-sitter-wasm`, bundled, since `docs/SPEC.md` section 3 decision 11 ships the grammars with the tool; the index built in the background once a review is read (`GET /api/review`), since section 6 forbids loading it lazily on the first question. One WASM runtime a process: a second `Parser.init` under a parser in use corrupted the shared memory, `RuntimeError: memory access out of bounds` in 1 run of 3 of `tests/symbols.test.ts`, and after the fix 5/5 under Node and 3/3 under Bun. A failed read drops the repository, so the next question builds it again. Each bundled language's query compiles against its grammar in a test, and a language that fails costs its own files and is named in `failed`. Sizes, base against branch: +10.6 MiB to the `darwin-arm64` binary (62 503 506 → 73 566 546 bytes), +0.96 MiB packed to the npm tarball (318 374 → 1 327 118 bytes, `dist/grammars/` 10.3 MiB unpacked). Left open in `minor/`: [DA-39.1](../../backlog/minor/DA-39.1-update-waits-for-the-symbol-index.md), filed by this acceptance.

**Verification.** Worker gates on the track tip `a572e79`, clean (`gates-browse-4.json`): gates 7, green 6 — `backslop lint` 0, `bun run lint` 0 (206 files), `bun run typecheck` 0, `bun run test` 0 (44 files, 678/678), `bun run test:bun` 0 (678/678), `bun run test:ui` 0 (104/104); `bun run perf` 1, declined at 3.84 per core. Indexing time is recorded in `docs/reference/09-ml.md` on a busy machine (300 generated files 494 ms Node / 346 ms Bun) and bounded by `SYMBOL_INDEX_BUDGET_MS`, 20 s. Mutation probes: on `5405f90` a file that is gone kept in the index turns red `keeping it fresh reads again the files it is told changed, and forgets one that is gone`, 1 of 5; on `7a39004` a frame with no files or a missing path no longer rebuilding turns 2 of 11 red; on `a572e79` a failed read no longer dropping the repository turns red `keeping it fresh drops a repository whose read failed, and reads it again on the next question`, 1 of 12. Both delivery channels answered a symbol end to end on the worker's machine: `node cli.js serve` from a copy of `dist/` with no `node_modules`, and the compiled binary with the WASM embedded. Review: the two isolated passes of the track (see DA-37's result); findings #2, #3, #5 and #10 of the first pass closed in `910afb4`, the second pass's major (a failed read) in `2c39394`. **Integration into `main`:** `5405f90 910afb4 2c39394` squashed onto DA-38's commit. `bun.lock` conflicted on neighbouring entries and keeps both (`bun install --frozen-lockfile` exit 0); `docs/README.md` keeps main's ADR-014 row beside ADR-015's; `docs/reference/09-ml.md` had its opening paragraph rewritten by both sides and now says that both the embedding model and the symbol index are built, with main's embedding sections and then the branch's `The symbol index` section, the tip's text; `CHANGELOG.md` was resolved by hand. The integration gates and the comparison with `a572e79` are in [DA-37's result](../DA-37-browse-repo/result.md): all exit 0 on `68c1468`. `Update after an edit` was 306 ms over its 300 in the first series and alternated 279 / 299 ms against `3b338a9` (spreads 7 / 34, load 1.4–1.7 a core) — a tendency, not proof, filed as [DA-39.1](../../backlog/minor/DA-39.1-update-waits-for-the-symbol-index.md): every `diff-changed` has the index read the changed files again with tree-sitter on the server's event loop, on the update's own path.

**Documentation in the same pass.** ADR-015 (new, Accepted) and its row in `docs/README.md`, `docs/SPEC.md` section 7 (`grammars`), `README.md`, `docs/reference/03-storage.md`, `07-server.md` (Symbols), `08-ui.md`, `09-ml.md` (The symbol index), `11-perf.md`, `CHANGELOG.md`.
