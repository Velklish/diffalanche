# DA-56.4 · Result

**Closed 2026-09-21.** Completed. `Scrolling the diff: CPU per frame` was budgeted at **8.3 ms**, the frame of 120 fps, and measured **8.5–9.1 ms** over nine commits of the main branch on a quiet machine — with no trend, and with no commit of that range responsible, including `1193ab3`, which predates the whole DA-53…56 package. A budget that no commit in living memory meets gates nothing: it is red on every tree, so it stops distinguishing a regression from the floor. The owner's decision of 2026-09-21 splits the two roles the number was carrying. **The gate** is set at **9.5 ms** — about four percent over the worst reading taken where the load precondition (DA-54.3) allows the gate to answer at all — which still catches a regression of the size the line was written for. **The target** stays 8.3 ms: `docs/SPEC.md` section 6 keeps it, now with the measured number and its date beside it, and closing the gap is DA-56.5. `RUNNER_ALLOWANCE` was recomputed in the same pass, **2.5 → 2.1**. It is a ratio against what a runner measured, so a budget moving without it would have carried the CI ceiling from 20.8 to 23.8 against an unchanged 17.3, turning fifteen percent of headroom into thirty-eight. The multiplier is **tuned to this one row, the tightest**, and the other six lines get whatever it produces — which is what makes moving any other millisecond budget leave it alone. That last part is a correction made in the second review round: the reviewer's table showed the margin at 2.1 running from +15 % on CPU per frame to several times over on first render, so the earlier wording "a budget change recomputes the multiplier" was true for one line and misleading for six. The owner's call was to keep one multiplier and say which line it serves; `docs/reference/11-perf.md` and ADR-013 now do.

**Verification.** The nine-point pass is DA-56.3's, shared with this card and taken in one quiet window of 2026-09-21 with one `bun run perf` per point, load 7.6–10.8 throughout: 8.5, 8.8, 9.1, 8.9, 8.9, 8.8, 8.7, 8.8, 8.7 ms from `1193ab3` to `ed81928`. The fifteen-pair load table behind DA-54.3 gives the other half of the picture and is why the new ceiling is set on readings taken below 2.5 per core: under that ceiling the band is 8.6–9.1, above it 8.6–10.2. The prose for both cards therefore lives in this card's commit `47e4934`, by the orchestrator's decision — the 9.5 ceiling is derived from the same table DA-56.3 prints, and splitting them by hunk would have left a number without its evidence beside it. **The honest part of this record**: in the track's own final measurement, taken in a 90-second window under the machine lock, CPU per frame came out at a median of 10.2 ms over three repetitions (10.2, 10.2, 10.0) — **over even the new 9.5 ceiling**. That run is itself the subject of DA-54.5: the one-minute average read 6.19 and the precondition answered, while the five- and fifteen-minute averages stood at 12.50 and 19.12 and the machine was still draining the previous worker's gate chain, and the numbers came out about 1.2 ms worse than the same branch measures when the machine is genuinely idle. The number that settles the line is the wave's final measurement on the merged tree, which the orchestrator takes on a quiet machine; it is not this card's to take.

**What this pass opened.** **DA-56.5** — the scroll does not fit the 120 fps frame, and why is not known; closing the 9.5-to-8.3 gap is that card's work, not this one's. It is in `triage/`; routing it is the orchestrator's move.

**The gates of the merged track.** The full gate list ran once on the tree the seven tasks make — `d76d804`, tree clean, `gates 7, green 6`, with the one red being `bun run perf` refusing to erase a pre-stamp fixture rather than returning a budget verdict. The commands, their exit codes and counts, the fifteen per-commit typechecks and how the track was integrated into `main` are in [the result of DA-63](../DA-63-perf-gate-erases-fixture/result.md); this task points there rather than repeating them.

**Documentation in the same pass.** `docs/SPEC.md` section 6 — the target kept with the measured figure and its date; `docs/reference/11-perf.md` — the two numbers, which is the gate and which the target, and which row the multiplier serves; `docs/adr/adr-013-perf-gate-off-ci.md` — the same on the CI side; a `### Changed` entry in `CHANGELOG.md`.
