# DA-52 · Result

**Closed 2026-09-06.** Completed. `loadConfig` in `src/core/config/` resolves the data directory from the first source that names one: `--data-dir` against the current directory, as before; `DIFFALANCHE_DATA_DIR` against the root; `dataDir` of the user config `$XDG_CONFIG_HOME/diffalanche/config.json` (`~/.config` when the variable is unset or empty) against the root; else `<root>/.diffalanche`. An empty variable counts as unset. Only `dataDir` is read from the user config, and a value that is not a string is refused with the file and the field, through the same `asString` that checks `config.json`. The environment and the configuration home are a third parameter of `loadConfig` (`ConfigSources`), so a test never reads the developer's own; `vitest.config.ts` also pins `DIFFALANCHE_DATA_DIR` to empty and `XDG_CONFIG_HOME` to an untouched directory for every suite, because the CLI suites call `loadConfig` without the parameter. `--help` names the order under `--data-dir`. Nothing else changed: `context.ts` still checks only the flag's path, since the directory the variable or the file names is the one place the tool creates.

**Verification.** Gates on `main`: `backslop lint` no errors; `bun run lint` 166 files (after `biome check --write` on the four touched source files); `bun run typecheck` clean; `bun run test` 32 files, 409 tests (config suite 20: the variable relative to the root, the user config relative to the root, the user config found through `XDG_CONFIG_HOME` with no parameter, flag above variable, variable above user config, empty variable ignored, non-string `dataDir` refused naming the user config file); `bun run test:bun` green; `bun run perf` green. README/CLI parity test green with the new flag text. Mutation probe after the commit: see the follow-up note in this file.

**Documentation in the same pass.** `README.md` (global flags table, "Where the data lives"), `docs/reference/06-cli.md` (flags table and the order paragraph), `docs/reference/03-storage.md` (data directory tree), `docs/SPEC.md` (section 3 decision 5, section 8), `CHANGELOG.md` (Unreleased, Added).
